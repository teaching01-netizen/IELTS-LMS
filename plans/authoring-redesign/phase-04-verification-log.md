# Phase 04 — content canvas and safe row actions

Question header/readiness, neutral sections, collapsed empty material/explanation, single Type selector, field issues and quiet footer replace persistent stage/card chrome. Optional editor reveal latches per question. Response-type and material replacement confirmations retained.

Explicit row IDs share guarded handlers: flush once, immutable target, single-flight guard, confirmation for deletion, preserve active draft for noncurrent deletion. Unit integration tests prove these cases and failed-flush blocking.

Full suite initially reported two stale assertions (Data table button and footer card); corrected to new selector/footer hierarchy and both suites pass (`evidence/phase-04-repair.txt`). Targeted canvas/workspace tests and lint pass. Baseline helper tail restored after discovering filesystem read output capping: complete source is retained, not replaced with a partial read.

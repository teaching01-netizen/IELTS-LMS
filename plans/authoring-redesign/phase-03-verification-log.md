# Phase 03 — navigator

52px rows; one trailing signal; aria-current/selection bar; selection-mode checkboxes; contextual reordering; filter menu; exam-wide count. Fixed real question numbering under filters. Shared reorder fences unchanged.

10 focused tests pass. Full gate initially found two old workbook-rail assertions; updated to overflow menu, then all UI tests passed (`evidence/phase-03-repair.txt`). Lint/build exit 0. Row Duplicate/Delete wiring reserved for Phase 04 guarded mutation handlers.

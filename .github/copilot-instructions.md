

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.0 -->
<!-- Updated: 2026-05-10T02:38:39.787Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
src/components/blocks/AcceptedAnswersEditor.tsx ← ../utils/acceptedAnswers
src/features/builder/hooks/useBuilderRouteController.ts ← ../../types, ../../types/domain
src/components/blocks/TFNGBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, InsertedImagesEditor
src/components/blocks/MatchingBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, InsertedImagesEditor
src/components/blocks/MultiSelectMCQBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, InsertedImagesEditor
src/utils/insertedImages.ts ← types
src/components/blocks/InsertedImagesEditor.tsx ← ../types, ../utils/idUtils, ../utils/imageUrl
src/utils/referenceImagePlacement.ts ← types
src/components/student/QuestionNavigator.tsx ← providers/StudentRuntimeProvider
src/utils/examIdCollisionCheck.ts ← services/examAdapterService, types
src/components/blocks/ShortAnswerBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, AcceptedAnswersEditor, ../utils/acceptedAnswers
src/components/blocks/NoteCompletionBlock.tsx ← ../types, ../utils/idUtils, ../utils/blankPlaceholders, ../utils/boldMarkdown, AcceptedAnswersEditor
src/components/blocks/MapLabelingBlock.tsx ← ../types, ../constants/uiConstants, ../utils/idUtils, ../utils/boldMarkdown, ../utils/imageUrl
src/components/blocks/DiagramLabelingBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, ../utils/imageUrl
src/components/blocks/FlowChartBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, InsertedImagesEditor
src/components/blocks/ClassificationBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, InsertedImagesEditor
src/components/blocks/MatchingFeaturesBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, InsertedImagesEditor
src/components/blocks/ClozeBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, AcceptedAnswersEditor, ../utils/acceptedAnswers
src/utils/subAnswerTreeSlots.ts ← types, acceptedAnswers, idUtils, subAnswerTree, tableCompletion
src/components/builder/SubAnswerTreeEditor.tsx ← ../types, ../utils/idUtils, ../utils/subAnswerTree, ../utils/subAnswerTreeSlots
src/components/proctor/ProctorApp.tsx ← ../features/proctor/contracts, ../features/auth/authSession, ProctorDashboard, AlertPanel
src/components/proctor/StudentDetailPanel.tsx ← ../types, ui/Badge, ui/Button, ConfirmModal
src/services/gradingFilters.ts ← types/grading
src/components/admin/GradingSessionList.tsx ← ../types/grading, ../services/gradingService, ../services/developmentFixtures, ../utils/csvExport
src/features/admin/routes/ExamsRoute.tsx ← ../../types, ../../types/domain, AdminContext
src/components/admin/AdminExams.tsx ← ui/StatusBadge, ../types, ../app/performance/lazyLoad, ../types/domain, ../utils/examStats
src/components/admin/ExamSettingsDrawer.tsx ← ../types, ../constants/examDefaults, ../types/domain, ../services/examDeliveryService, scoring/BandScoreMatrix
src/components/student/WarningOverlay.tsx ← ui/Button
src/constants/examDefaults.ts ← types
src/features/builder/components/SecurityTab.tsx ← ../../types
src/components/admin/AdminScheduling.tsx ← ../types, ../types/domain, ../services/examRepository, ../services/examDeliveryService, ../utils/scheduleUtils
src/components/admin/ScheduleSessionModal.tsx ← ../types, ../types/domain, ../services/examRepository, ../services/examDeliveryService
src/types/domain.ts ← types, studentAttempt
src/utils/gradingSeedData.ts ← services/gradingRepository, services/gradingService, services/examRepository, types/domain, logger
src/components/student/StudentAppWrapper.tsx ← ../types, ../types/domain, ../types/studentAttempt, ../features/student/hooks/useStudentSessionRouteData, StudentApp
src/components/student/StudentHeader.tsx ← ui/LoadingMark, highlightPalette
src/components/student/ProtectedInput.tsx ← ../types, ../services/studentAuditService, providers/StudentAttemptProvider, answerUndoRedoGuard
src/components/student/ProtectedSelect.tsx ← providers/StudentAttemptProvider
src/components/student/ProtectedChoiceInput.tsx ← providers/StudentAttemptProvider
e2e/prod-load/live-multi-user-runner.ts ← exam-url, user-source, live-dashboard-server
load-runner/k6/prod-auto-submit-200.js ← prod-load-helpers
load-runner/k6/prod-resume-100.js ← prod-load-helpers
load-runner/k6/prod-section-transition-200.js ← prod-load-helpers
load-runner/k6/prod-start-exam-200.js ← prod-load-helpers
load-runner/k6/prod-submit-storm-200.js ← prod-load-helpers
src/components/StimulusPane.tsx ← types, StimulusImageEditor, utils/builderEnhancements, utils/imageUrl, utils/sanitizeHtml
src/components/admin/QuestionTracebackPanel.tsx ← ../types, ../types/grading, gradingReviewUtils, gradingAnswerUtils
src/components/blocks/SentenceCompletionBlock.tsx ← ../types, ../utils/idUtils, ../utils/blankPlaceholders, ../utils/boldMarkdown, AcceptedAnswersEditor
src/components/blocks/TableCompletionBlock.tsx ← ../types, ../utils/boldMarkdown, AcceptedAnswersEditor, ../utils/acceptedAnswers, ../utils/tableCompletion
src/components/workspaces/WritingWorkspace.tsx ← ../types, ../constants/examDefaults, PromptTemplateLibrary, scoring/GradingRubricPanel, ../utils/builderEnhancements
src/utils/imageUrl.ts ← audioUrl
src/utils/sanitizeHtml.ts ← imageUrl
src/utils/tableCompletion.ts ← types, blankPlaceholders, idUtils, acceptedAnswers, completionPromptText
src/features/builder/components/ValidationSummary.tsx ← ../../types/domain, ../../types
src/features/builder/hooks/useReviewRouteController.ts ← ../../types, ../../types/domain
src/features/builder/routes/BuilderRoot.tsx ← ../../hooks/useUndoRedo, ../../hooks/useKeyboardShortcuts, ../../types, ../../utils/latestOnlyAsync, ../../constants/examDefaults
src/features/builder/routes/ExamReviewRoute.tsx ← components/PublishActions, components/ValidationSummary, hooks/useReviewRouteController, ../../types
src/components/blocks/SingleMCQBlock.tsx ← ../types, ../utils/idUtils, ../utils/boldMarkdown, InsertedImagesEditor
src/components/builder/QuestionBankLibrary.tsx ← ../types, ../services/questionBankService, ui/CollectionLoadingSkeleton
src/components/builder/QuestionDetailModal.tsx ← ../types, ../utils/acceptedAnswers, ../utils/tableCompletion
src/components/questionbank/QuestionDetailModal.tsx ← ../types, ui/Button, ../utils/acceptedAnswers, ../utils/tableCompletion
src/components/workspaces/ListeningWorkspace.tsx ← ../types, QuestionBuilderPane, ../utils/audioUrl, ../utils/idUtils, ../utils/examUtils
src/utils/cloneExamContent.ts ← types, idUtils
src/utils/examUtils.ts ← types, constants/examDefaults, services/examAdapterService, acceptedAnswers
src/utils/validationUtils.ts ← types, blankPlaceholders, acceptedAnswers, tableCompletion, insertedImages
src/app/data/studentSessionQueries.ts ← ../services/backendBridge, ../services/studentSessionTransport, ../types/studentAttempt, queryClient
src/services/studentAuditService.ts ← types, backendBridge, attemptCredentialAdapter, studentSessionTransport
src/services/attemptCredentialAdapter.ts ← backendBridge, studentSessionTransport, types/studentAttempt
src/features/student/hooks/studentSessionMachineAdapters.ts ← ../../utils/studentObservability, studentSessionStateMachine
src/features/student/hooks/studentSessionStateMachine.ts ← liveSnapshotFreshness, ../../utils/studentObservability
src/types/studentAttempt.ts ← types, answers
src/services/backendBridge.ts ← app/api/apiClient, types/domain, types, utils/ttlLruCache
src/app/forms/useFormHandler.ts ← error/errorTypes, error/errorLogger
src/components/admin/gradingAnswerUtils.ts ← ../services/examAdapterService, ../types/answers, ../utils/acceptedAnswers
src/services/policies/examStatusTransitions.ts ← ../types/domain
src/services/examLifecycleService.ts ← examRepository, types/domain, types, utils/examUtils, utils/tableCompletion
src/services/studentAttemptNormalization.ts ← types/studentAttempt
src/services/examRepository.ts ← types/domain, types, backendBridge
src/services/studentIntegrityService.ts ← types, types/studentAttempt
src/features/admin/contracts/index.ts ← ../../types, ../../types/domain
src/features/admin/hooks/useAdminRootController.ts ← ../auth/authSession, ../../types, ../../types/domain, routes/AdminContext
src/routes/index.tsx ← components/AppShell, components/ui/AppLoadingSkeleton, components/ui/ErrorSurface, components/ui/LoadingSurface, features/auth/ActivateAccountPage
src/features/builder/services/previewRuntimeSessionService.ts ← ../../types, ../../types/domain
src/services/studentMutationOutbox.ts ← types/studentAttempt
src/services/gradingService.ts ← backendBridge, gradingRepository, examRepository, gradingFilters, features/builder/services/previewRuntimeSessionService
load-runner/k6/prod-transition-reconciliation-200.js ← prod-load-helpers
src/features/builder/routes/ExamPreviewRoute.tsx ← ../auth/authSession, services/previewRuntimeSessionService, ../../types
src/services/authService.ts ← app/api/apiClient
src/features/auth/authSession.tsx ← ../app/api/apiClient, ../app/data/queryClient, ../app/error/errorLogger, ../services/authService
src/app/data/proctorQueries.ts ← ../services/backendBridge, queryClient
src/types.ts ← types/domain, types/answers
src/components/proctor/ProctorDashboard.tsx ← ../types, ../types/domain, ConfirmModal, ui/LoadingMark, ui/Toast
src/components/proctor/ExamGroupCard.tsx ← ../types
src/features/proctor/contracts/index.ts ← ../../types, ../../types/domain
src/components/student/StudentFooter.tsx ← ui/Button, providers/StudentRuntimeProvider
k6/prod-submit-storm-200.js ← prod-load-helpers
src/components/student/TableCompletionSlotCell.tsx ← ProtectedInput, FormattedText, highlightPalette
src/components/student/browserParityPolicy.ts ← fullscreen
src/components/student/useSplitPaneResize.ts ← browserParityPolicy
src/components/proctor/AuditLogPanel.tsx ← ../types, ui/Badge
src/components/student/providers/StudentNetworkProvider.tsx ← ../../types, ../../utils/deviceFingerprinting, StudentAttemptProvider, StudentRuntimeProvider
src/components/student/providers/blockingStateMachine.ts ← ../../types/studentAttempt
src/components/admin/StudentReviewWorkspace.tsx ← ../types/grading, ../services/gradingService, ../services/gradingRepository, ../services/examRepository, ../types
src/services/gradingRepository.ts ← backendBridge, types/grading, utils/ttlLruCache
src/components/student/providers/StudentKeyboardProvider.tsx ← StudentProctoringProvider, StudentAttemptProvider, StudentRuntimeProvider, StudentUIProvider
src/components/student/useDeferredSelectionHighlight.ts ← highlightSelection
load-runner/e2e/prod-load/live-multi-user-runner.ts ← exam-url, user-source, live-dashboard-server, student-answer-capture, grading-verifier
src/components/student/providers/StudentProctoringProvider.tsx ← ../../types, fullscreen, StudentAttemptProvider, StudentRuntimeProvider
src/components/student/providers/StudentRuntimeProvider.tsx ← ../../types, ../../types/domain, ../../types/studentAttempt, ../../utils/studentObservability, verifiedTerminalState
src/services/studentAttemptRepository.ts ← backendBridge, studentSessionTransport, attemptCredentialAdapter, app/api/apiClient, types/studentAttempt
src/components/student/resolveObjectiveAnswerUpdate.ts ← ../types/studentAttempt
src/components/student/providers/StudentUIProvider.tsx ← highlightPalette, accessibilityScale
src/components/student/answerMutationDebug.ts ← ../app/error/errorLogger
src/components/student/QuestionRenderer.tsx ← ../types, ProtectedInput, FormattedText, ../utils/boldMarkdown, ../utils/imageUrl
src/components/student/providers/StudentAttemptProvider.tsx ← ../../app/data/queryClient, ../../utils/studentObservability, ../../types, ../../types/studentAttempt, answerMutationDebug
src/components/student/StudentWriting.tsx ← ../types, ../utils/writingTaskUtils, ../services/studentAuditService, ../utils/sanitizeHtml, providers/StudentAttemptProvider
src/components/student/StudentApp.tsx ← ui/Button, AccessibilitySettings, HelpModal, Lobby, PreCheck
src/components/student/StudentExamPreview.tsx ← ../types, ../types/studentAttempt, AccessibilitySettings, QuestionNavigator, StudentFooter
src/app/data/answerHistoryQueries.ts ← ../features/answer-history/contracts, queryClient
src/app/data/queryClient.ts ← error/errorLogger
src/components/QuestionBuilderPane.tsx ← types, blocks/TFNGBlock, blocks/ClozeBlock, blocks/MatchingBlock, blocks/MapLabelingBlock
src/components/admin/GradingExportButtons.tsx ← gradingReviewUtils
src/components/admin/GradingSessionDetail.tsx ← ../types/grading, ../services/gradingService, ../services/gradingRepository, ../services/examRepository, ../services/developmentFixtures
src/components/admin/gradingReviewUtils.ts ← ../types, ../types/grading, ../services/examAdapterService, gradingAnswerUtils, ../utils/htmlText
src/components/answer-history/AnswerHistoryPage.tsx ← ../features/answer-history/contracts, ../types
src/components/student/FormattedText.tsx ← ../utils/boldMarkdown, highlightSelection, highlightPalette, highlightPersistence, useDeferredSelectionHighlight
src/components/student/RichTextHighlighter.tsx ← ../utils/sanitizeHtml, highlightSelection, highlightPalette, highlightPersistence, useDeferredSelectionHighlight
src/components/student/StudentListening.tsx ← ../types, QuestionRenderer, SubAnswerTreeQuestionList, ../utils/examUtils, ../services/examAdapterService
src/components/student/StudentReading.tsx ← ../types, ../types/studentAttempt, QuestionRenderer, SubAnswerTreeQuestionList, ../utils/examUtils
src/components/student/SubAnswerTreeQuestionList.tsx ← ../types, ../types/studentAttempt, ../services/examAdapterService, ProtectedInput, FormattedText
src/features/proctor/hooks/useProctorRouteController.ts ← ../../types, ../../types/domain, contracts
src/features/student/hooks/useStudentSessionRouteData.ts ← ../auth/authSession, ../../types, ../../types/domain, ../../types/studentAttempt, ../../utils/studentObservability
src/features/student/routes/StudentEntryRoute.tsx ← ../auth/authSession
src/features/student/routes/StudentSessionRoute.tsx ← ../auth/authSession
src/services/answerHistoryService.ts ← app/api/apiClient, backendBridge, features/answer-history/contracts
src/services/examAdapterService.ts ← constants/examDefaults, types, types/domain, types/answers, examRepository
src/utils/subAnswerTree.ts ← types, acceptedAnswers
```

## changes (last 10 commits — 17 hours ago)
```
src/app/data/answerHistoryQueries.ts          +useAnswerHistoryTargetDetailByAttempt  ~useAnswerHistoryTargetDetail
src/components/QuestionBuilderPane.tsx        +blockHasSubAnswerData  +clearSubAnswerData  ~QuestionBuilderPane
src/components/admin/GradingExportButtons.tsx ~GradingExportButtons
src/components/admin/GradingSessionDetail.tsx ~GradingSessionDetail
src/components/admin/gradingReviewUtils.ts    ~buildWideObjectiveExport  ~buildCsvFilename
src/components/answer-history/AnswerHistoryPage.tsx ~AnswerHistoryPage
src/components/student/FormattedText.tsx      ~FormattedText
src/components/student/RichTextHighlighter.tsx ~RichTextHighlighter
src/components/student/StudentListening.tsx   ~StudentListening
src/components/student/StudentReading.tsx     ~StudentReading
src/components/student/SubAnswerTreeQuestionList.tsx ~SubAnswerTreeQuestionList
src/components/student/highlightSelection.ts  +isInsideHighlightedMark  +flattenNestedHighlightedMarks  ~applySingleRangeHighlight  ~applyHighlightToClonedRange
src/components/student/normalizeReadingPassageText.ts +collectLeafReadableBlocks  ~htmlToPlainText  ~normalizeInlineText  ~htmlToMarkedText
src/features/proctor/hooks/useProctorRouteController.ts ~useProctorRouteController
src/features/student/hooks/useStudentSessionRouteData.ts +normalizeCandidateId  ~normalizeWcodeCandidateId  ~isWcodeCandidateId  ~parseNullableString
src/features/student/routes/StudentEntryRoute.tsx +normalizeAccessCode  +buildStudentRoute  ~validateWcode  ~StudentEntryRoute
src/features/student/routes/StudentSessionRoute.tsx ~StudentSessionRoute
src/services/answerHistoryService.ts          +fetchAnswerHistoryTargetDetailByAttempt  ~fetchAnswerHistoryOverviewBySubmission  ~fetchAnswerHistoryTargetDetail  ~fetchAnswerHistoryOverviewByAttempt
src/services/examAdapterService.ts            ~getStudentQuestionsForModule  ~buildStudentQuestionDescriptors  ~getEnabledModules  ~buildMultiQuestionDescriptor
src/utils/acceptedAnswers.ts                  +splitAnswerVariants  ~sanitizeAcceptedAnswers  ~resolveAcceptedAnswers  ~normalizeAnswerForMatching
src/utils/subAnswerTree.ts                    ~hasSubAnswerTreeMode
```

## backend

### backend/crates/domain/src/auth.rs
```
pub struct User
pub struct UserPasswordCredential
pub struct UserSession
pub struct UserSessionEvent
pub struct PasswordResetToken
pub struct AccountActivationToken
pub struct StudentProfile
pub struct StaffProfile
pub struct AttemptSession
pub struct SessionUser
pub struct LoginRequest
pub struct LoginResponse
pub struct SessionResponse
pub struct PasswordResetRequest
pub struct PasswordResetCompleteRequest
pub struct AccountActivationRequest
pub struct StudentEntryRequest
pub struct IssueAttemptToken
pub enum UserRole
pub enum UserState
```

### backend/crates/infrastructure/src/auth.rs
```
pub struct AttemptTokenClaims
pub enum AttemptTokenError
pub fn hash_password(password: &str) → Result<String, argon2::pass...
pub fn verify_password(password: &str, password_hash: &str) → bool
pub fn random_token(byte_len: usize) → String
pub fn sha256_hex(value: &str) → String
pub fn normalize_user_agent(value: Option<&str>) → Option<String>
pub fn session_idle_timeout(role: &ielts_backend_domain::auth::UserRole, config: &AppConfig,) → Duration
pub fn session_expiry(role: &ielts_backend_domain::auth::UserRole, config: &AppConfig,) → (DateTime<Utc>, DateTime<Utc>)
pub fn sign_attempt_token(config: &AppConfig, claims: &AttemptTokenClaims) → String
pub fn verify_attempt_token(config: &AppConfig, token: &str,) → Result<AttemptTokenClaims, ...
```

### backend/crates/infrastructure/src/authorization.rs
```
pub struct AuthorizationService
impl AuthorizationService
  pub fn can_access_organization_exams(actor: &ActorContext, organization_id: String) → bool
  pub fn can_access_schedule(actor: &ActorContext, schedule_id: String, organization_id: String,) → bool
  pub fn can_access_student_data(actor: &ActorContext, schedule_id: String, student_key: &str, organization_id: String,) → bool
  pub fn can_modify_exam_content(actor: &ActorContext, organization_id: String) → bool
  pub fn can_grade_submissions(actor: &ActorContext, schedule_id: String, organization_id: String,) → bool
  pub fn can_proctor_schedule(actor: &ActorContext, schedule_id: String, organization_id: String,) → bool
```

### backend/crates/infrastructure/src/rate_limit.rs
```
pub struct RateLimitConfig
pub struct RateLimiter
pub enum RateLimitKey
pub enum RateLimitResult
impl RateLimitConfig
  pub fn new(max_requests: u32, window_secs: u64) → Self
  pub fn with_burst(mut self, burst: u32) → Self
impl Bucket
impl RateLimiter
  pub fn new(default_config: RateLimitConfig) → Self
  pub fn with_bucket_cap(default_config: RateLimitConfig, bucket_cap: usize) → Self
  pub async fn check(&self, key: &RateLimitKey) → RateLimitResult
  pub async fn check_with_config(&self, key: &RateLimitKey, config: &RateLimitConfig,) → RateLimitResult
  pub async fn peek(&self, key: &RateLimitKey) → Option<RateLimitResult>
  pub async fn cleanup(&self)
  pub async fn bucket_count(&self) → usize
```

### backend/Dockerfile
```
FROM node:22-bookworm-slim AS frontend
FROM rust:1.88-slim AS backend
FROM debian:bookworm-slim AS runner
EXPOSE 8080
CMD ["/app/start.sh"]
ENV FRONTEND_DIST_DIR
ENV MIGRATIONS_DIR
```

### backend/scripts/idle-ram-window.js
```
async function main()
function printHelp()
async function sampleMetrics(metricsUrl, sampledAt)
function parsePrometheus(text)
function pickMetric(values, names)
function defaultOutputPath()
function loadSamples(filePath)
function printSummary(samples, title)
function printComparison(baselineSamples, tunedSamples, baselineName, tunedName)
function summaryForComparison(samples)
function diffMetric(baseline, tuned)
function summarizeSeries(samples, field)
function valuesFor(samples, field)
function percentile(values, quantile)
function sleep(ms)
```

### backend/scripts/idle-ram-window.md
```
h1 Idle RAM Measurement (API process)
h2 1) Baseline capture (before tuning)
h2 2) Apply API allocator tuning in deployment
h2 3) Tuned capture (after deployment)
h2 4) Compare baseline vs tuned
h2 5) Functional safety checks
code-fence bash
code-fence plain
```

### backend/crates/infrastructure/Cargo.toml
```
table [package]
table [dependencies]
table [dev-dependencies]
table [[test]]
key name
key version
key edition
key rust-version
key publish
key argon2
key base64
key hex
key hmac
key opentelemetry
key prometheus-client
key rand
key serde_json
key sha2
key subtle
key tracing
key tracing-opentelemetry
key path
```

### backend/crates/api/src/routes/health.rs
```
pub struct HealthData
pub struct ReadinessData
pub async fn healthz(State(state) → Json<SuccessResponse<Health...
pub async fn readyz(State(state) → Json<SuccessResponse<Readin...
pub async fn metrics(State(state) → Result<([(axum::http::heade...
```

### backend/crates/infrastructure/src/database_monitor.rs
```
pub struct OutboxBacklogSnapshot
pub struct RelationSize
pub struct StorageBudgetThresholds
pub struct StorageBudgetSnapshot
pub struct GradingProjectionSnapshot
pub enum StorageBudgetLevel
impl StorageBudgetThresholds
impl StorageBudgetLevel
  pub fn as_label(self) → &'static str
  pub fn as_severity_code(self) → i64
pub async fn ping_database(pool: &MySqlPool) → Result<Duration, sqlx::Error>
pub async fn inspect_outbox_backlog(pool: &MySqlPool,) → Result<OutboxBacklogSnapsho...
pub async fn inspect_storage_budget(pool: &MySqlPool, thresholds: StorageBudgetThresholds,) → Result<StorageBudgetSnapsho...
pub async fn inspect_grading_projection_snapshot(pool: &MySqlPool,) → Result<GradingProjectionSna...
```

### backend/crates/api/src/routes/ws.rs
```
pub struct LiveUpdatesQuery
pub async fn websocket_live(State(state) → impl IntoResponse
```

### backend/crates/api/src/lib.rs
```
pub async fn run() → std::io::Result<()>
```

### backend/crates/api/src/live_updates.rs
```
pub struct LiveUpdateHub
impl LiveUpdateHub
impl LiveUpdateHub
impl LiveUpdateHub
  pub fn new() → Self
  pub fn with_config(config: &AppConfig) → Self
  pub fn subscribe(&self) → broadcast::Receiver<LiveUpd...
  pub fn subscribe_all(&self) → broadcast::Receiver<LiveUpd...
  pub fn subscribe_schedule(&self, schedule_id: &str) → broadcast::Receiver<LiveUpd...
  pub fn subscribe_attempt(&self, attempt_id: &str) → broadcast::Receiver<LiveUpd...
  pub fn cleanup_schedule_topic_if_idle(&self, schedule_id: &str)
  pub fn cleanup_attempt_topic_if_idle(&self, attempt_id: &str)
pub fn spawn_live_update_listener(config: AppConfig, hub: LiveUpdateHub, bus: Option<LiveUpdateBusRepository>, instance_id: String,) → Option<tokio::task::JoinHan...
```

### backend/crates/api/src/runtime_auto_advance.rs
```
pub fn spawn_runtime_auto_advance(state: AppState) → Option<tokio::task::JoinHan...
```

### backend/crates/api/src/state.rs
```
pub struct AppState
impl AppState
  pub fn new(config: AppConfig) → Self
  pub fn with_pool(config: AppConfig, pool: MySqlPool) → Self
  pub async fn from_config(config: AppConfig) → Result<Self, sqlx::Error>
  pub fn db_pool(&self) → MySqlPool
  pub fn db_pool_opt(&self) → Option<MySqlPool>
  pub async fn check_exam_rate_limit(&self, route_key: &str, key: &RateLimitKey, config: &RateLimitConfig,) → RateLimitResult
  pub fn publish_live_update(&self, event: ielts_backend_domain::schedule::LiveUpdateEvent)
```

### backend/crates/infrastructure/src/distributed_rate_limit.rs
```
pub struct DistributedRateLimiter
impl DistributedRateLimiter
  pub fn new(pool: MySqlPool) → Self
  pub async fn check_with_config(&self, route_key: &str, key: &RateLimitKey, config: &RateLimitConfig,) → Result<RateLimitResult, sql...
  pub async fn purge_expired(&self, limit: i64) → Result<u64, sqlx::Error>
```

### backend/crates/infrastructure/src/outbox.rs
```
pub struct OutboxEvent
pub struct OutboxRepository
impl OutboxRepository
  pub fn new(pool: MySqlPool) → Self
  pub async fn enqueue(&self, aggregate_kind: &str, aggregate_id: &str, revision: i64, event_family: &str, payload: &Value,) → Result<OutboxEvent, sqlx::E...
  pub async fn enqueue_in_tx(tx: &mut Transaction<'_, MySql>, aggregate_kind: &str, aggregate_id: &str, revision: i64, event_family: &str, payload: &Value,) → Result<(), sqlx::Error>
  pub async fn claim_batch(&self, limit: i64, worker_id: &str, lease_seconds: i64,) → Result<Vec<OutboxEvent>, sq...
  pub async fn mark_published(&self, claim_token: &str, ids: &[Hyphenated],) → Result<u64, sqlx::Error>
  pub async fn notify_published(&self, events: &[OutboxEvent], _channel: &str,) → Result<u64, sqlx::Error>
  pub async fn mark_failed(&self, claim_token: &str, id: Hyphenated, message: &str,) → Result<(), sqlx::Error>
  pub async fn purge_published(&self, limit: i64) → Result<u64, sqlx::Error>
```

### backend/crates/worker/Cargo.toml
```
table [package]
table [dependencies]
table [dev-dependencies]
key name
key version
key edition
key rust-version
key publish
key serde_json
key tracing
```

### backend/crates/worker/src/jobs/grading_projection.rs
```
pub struct GradingProjectionRunReport
pub struct GradingProjectionState
pub struct GradingProjectionTotals
pub struct GradingProjectionCycleSnapshot
pub async fn run_once(pool: MySqlPool, config: &AppConfig,) → Result<GradingProjectionRun...
pub async fn record_failure(pool: &MySqlPool) → Result<u64, sqlx::Error>
```

### backend/crates/worker/src/jobs/outbox.rs
```
pub struct OutboxRunReport
pub async fn run_once(pool: MySqlPool, config: &AppConfig, notify_channel: &str,) → Result<OutboxRunReport, sql...
```

### backend/crates/worker/src/main.rs
```
impl WorkerTrigger
```

### backend/crates/infrastructure/src/migrations.rs
```
pub async fn run_startup_migrations(pool: &MySqlPool, migrations_dir: &Path,) → Result<(), sqlx::Error>
pub fn default_migrations_dir() → PathBuf
```

### backend/crates/infrastructure/src/live_update_bus.rs
```
pub struct LiveUpdateBusRepository
pub struct LiveUpdateEnvelope
impl LiveUpdateBusRepository
  pub fn new(pool: MySqlPool) → Self
  pub async fn enqueue(&self, origin_instance_id: &str, event: &LiveUpdateEvent,) → Result<(), sqlx::Error>
  pub async fn poll_after(&self, last_sequence_id: i64, limit: i64, local_instance_id: &str,) → Result<Vec<LiveUpdateEnvelo...
  pub async fn latest_sequence_id(&self) → Result<i64, sqlx::Error>
  pub async fn purge_older_than_hours(&self, retention_hours: i64, limit: i64,) → Result<u64, sqlx::Error>
```

### backend/crates/worker/src/jobs/retention.rs
```
pub struct RetentionRunReport
impl RetentionRunReport
  pub fn total_rows(self) → u64
pub async fn run_once(pool: MySqlPool) → Result<RetentionRunReport, ...
pub async fn run_once_with_config(pool: MySqlPool, config: &AppConfig,) → Result<RetentionRunReport, ...
pub async fn run_once_with_config_and_budget(pool: MySqlPool, config: &AppConfig, storage_budget_level: StorageBudgetLevel,) → Result<RetentionRunReport, ...
```

### backend/crates/api/Cargo.toml
```
table [package]
table [dependencies]
table [dev-dependencies]
table [[test]]
key name
key version
key edition
key rust-version
key publish
key async-trait
key cookie
key futures-util
key serde_json
key tracing
key tokio-tungstenite
key path
```

### backend/crates/infrastructure/src/idempotency.rs
```
pub struct IdempotencyRecord
pub struct IdempotencyRepository
pub enum IdempotencyLookupStatus
impl IdempotencyRepository
  pub fn new(pool: MySqlPool) → Self
  pub fn with_usable_hours(pool: MySqlPool, usable_hours: i64) → Self
  pub fn with_ttl_policy(pool: MySqlPool, mutation_usable_hours: i64, submit_usable_hours: i64, violation_usable_hours: i64,) → Self
  pub async fn lookup_with_executor(executor: E, actor_id: &str, route_key: &str, idempotency_key: &str,) → Result<Option<IdempotencyRe...
  pub async fn lookup(&self, actor_id: &str, route_key: &str, idempotency_key: &str,) → Result<Option<IdempotencyRe...
  pub async fn store_with_executor(&self, executor: E, actor_id: &str, route_key: &str, idempotency_key: &str, request_hash: &str, response_status: i32, response_body: &Value,) → Result<IdempotencyRecord, s...
  pub async fn store_or_replay(&self, actor_id: &str, route_key: &str, idempotency_key: &str, request_hash: &str, response_status: i32, response_body: Value,) → Result<(IdempotencyLookupSt...
  pub async fn purge_expired(&self, limit: i64) → Result<u64, sqlx::Error>
```

### backend/crates/api/src/routes/grading.rs
```
pub struct SessionDetailQuery
pub struct SessionListQuery
pub async fn list_sessions(State(state) → Result<ApiResponse<Vec<Grad...
pub async fn get_session(State(state) → Result<ApiResponse<GradingS...
pub async fn get_submission(State(state) → Result<ApiResponse<Submissi...
pub async fn get_submission_sections(State(state) → Result<ApiResponse<Vec<Sect...
pub async fn get_submission_writing_tasks(State(state) → Result<ApiResponse<Vec<Writ...
pub async fn start_review(State(state) → Result<ApiResponse<ReviewDr...
pub async fn get_review_draft(State(state) → Result<ApiResponse<ReviewDr...
pub async fn save_review_draft(State(state) → Result<ApiResponse<ReviewDr...
pub async fn mark_grading_complete(State(state) → Result<ApiResponse<ReviewDr...
pub async fn mark_ready_to_release(State(state) → Result<ApiResponse<ReviewDr...
pub async fn release_now(State(state) → Result<ApiResponse<StudentR...
pub async fn schedule_release(State(state) → Result<ApiResponse<ReviewDr...
pub async fn reopen_review(State(state) → Result<ApiResponse<ReviewDr...
pub async fn get_result_events(State(state) → Result<ApiResponse<Vec<Rele...
```

### backend/crates/application/src/validation.rs
```
pub struct ValidationError
pub struct ValidationWarning
pub struct ValidationResult
impl ValidationResult
  pub fn new() → Self
  pub fn is_valid(&self) → bool
  pub fn add_error(&mut self, field: impl Into<String>, message: impl Into<String>)
  pub fn add_warning(&mut self, field: impl Into<String>, message: impl Into<String>)
pub fn validate_exam_content(content: &serde_json::Value, config: &serde_json::Value,) → ValidationResult
```

### backend/crates/domain/src/exam.rs
```
pub struct ExamEntity
pub struct ExamVersion
pub struct ExamVersionSummary
pub struct ExamEvent
pub struct ExamMembership
pub struct CreateExamRequest
pub struct UpdateExamRequest
pub struct SaveDraftRequest
pub struct PublishExamRequest
pub struct CloneExamRequest
pub struct ValidationIssue
pub struct ExamValidationSummary
pub enum ExamType
pub enum ExamStatus
pub enum Visibility
pub enum ExamEventAction
pub enum MembershipRole
impl ExamEntity
  pub fn get_exam_type(&self) → Result<ExamType, String>
  pub fn get_status(&self) → Result<ExamStatus, String>
  pub fn get_visibility(&self) → Result<Visibility, String>
impl ExamType
  pub fn as_str(&self) → &'static str
  pub fn from_str(s: &str) → Result<Self, String>
impl ExamStatus
```

### backend/crates/api/src/routes/proctor.rs
```
pub struct LiveModeQuery
pub struct ProctorSessionQuery
pub async fn list_sessions(State(state) → Result<ApiResponse<Vec<Proc...
pub async fn get_session(State(state) → Result<ApiResponse<ProctorS...
pub async fn refresh_presence(State(state) → Result<ApiResponse<Vec<Proc...
pub async fn end_section_now(State(state) → Result<ApiResponse<ielts_ba...
pub async fn extend_section(State(state) → Result<ApiResponse<ielts_ba...
pub async fn complete_exam(State(state) → Result<ApiResponse<ielts_ba...
pub async fn warn_attempt(State(state) → Result<ApiResponse<ielts_ba...
pub async fn pause_attempt(State(state) → Result<ApiResponse<ielts_ba...
pub async fn resume_attempt(State(state) → Result<ApiResponse<ielts_ba...
pub async fn terminate_attempt(State(state) → Result<ApiResponse<ielts_ba...
pub async fn acknowledge_alert(State(state) → Result<ApiResponse<SessionA...
pub async fn live_mode(State(state) → Result<ApiResponse<ielts_ba...
```

### backend/migrations/0015_operation_write_hardening.sql
```
TABLE student_attempt_answer_slots
INDEX idx_student_attempt_answer_slots_attempt_question ON student_attempt_answer_slots
INDEX idx_student_attempt_mutations_attempt_session_mutation_id ON student_attempt_mutations
```

### backend/migrations/0016_attempt_mutation_id_uniqueness.sql
```
INDEX idx_student_attempt_mutations_attempt_mutation_id ON student_attempt_mutations
```

### backend/migrations/0017_production_hardening.sql
```
INDEX idx_outbox_events_claim_lease ON outbox_events
INDEX idx_outbox_events_claim_token ON outbox_events
INDEX idx_student_attempts_schedule_updated_id ON student_attempts
INDEX idx_student_attempt_mutations_attempt_mutation_id ON student_attempt_mutations
INDEX idx_student_attempt_mutations_attempt_session_mutation_id ON student_attempt_mutations
```

### backend/migrations/0018_exam_day_concurrency_hardening.sql
```
TABLE distributed_rate_limit_counters
TABLE live_update_events
INDEX idx_student_attempts_schedule_submitted_id ON student_attempts
INDEX idx_student_attempts_submitted_updated_id ON student_attempts
INDEX idx_distributed_rate_limit_expires ON distributed_rate_limit_counters
INDEX idx_live_update_events_origin_sequence ON live_update_events
INDEX idx_live_update_events_created ON live_update_events
```

### backend/migrations/0019_violation_id_idempotency.sql
```
INDEX uq_student_violation_attempt_business_id ON student_violation_events
```

### backend/migrations/0022_attempt_submission_ledger.sql
```
TABLE attempt_submission_ledger
```

### backend/migrations/0023_sort_memory_hotpath_indexes.sql
```
INDEX idx_grading_sessions_updated_start_id ON grading_sessions
INDEX idx_session_audit_logs_schedule_ack_action_created ON session_audit_logs
INDEX idx_student_attempts_schedule_candidate_submitted_updated ON student_attempts
INDEX idx_student_attempt_mutations_attempt_received_id ON student_attempt_mutations
```

### backend/migrations/0024_projection_sort_hardening.sql
```
INDEX idx_exam_schedules_updated_id ON exam_schedules
INDEX idx_student_attempts_updated_id_submitted ON student_attempts
```

### backend/migrations/0003_exam_core.sql
```
TABLE exam_entities
TABLE exam_memberships
TABLE exam_versions
TABLE exam_events
INDEX idx_exam_entities_slug ON exam_entities
INDEX idx_exam_entities_org_status_updated ON exam_entities
INDEX idx_exam_entities_owner_updated ON exam_entities
INDEX idx_exam_entities_draft_version ON exam_entities
INDEX idx_exam_memberships_exam_actor_role_active ON exam_memberships
INDEX idx_exam_memberships_actor_role_created ON exam_memberships
INDEX idx_exam_memberships_revoked_at ON exam_memberships
INDEX idx_exam_versions_exam_created ON exam_versions
INDEX idx_exam_versions_exam_draft ON exam_versions
INDEX idx_exam_versions_exam_published ON exam_versions
INDEX idx_exam_versions_parent ON exam_versions
INDEX idx_exam_events_exam_created ON exam_events
INDEX idx_exam_events_exam_action_created ON exam_events
```

### backend/migrations/0004_library_and_defaults.sql
```
TABLE admin_default_profiles
TABLE passage_library_items
TABLE question_bank_items
INDEX idx_admin_default_profiles_org_active ON admin_default_profiles
INDEX idx_admin_default_profiles_org_updated ON admin_default_profiles
INDEX idx_passage_library_org_difficulty_updated ON passage_library_items
INDEX idx_question_bank_org_type_difficulty_updated ON question_bank_items
```

### backend/migrations/0005_scheduling_and_access.sql
```
TABLE exam_schedules
TABLE schedule_registrations
TABLE schedule_staff_assignments
TABLE exam_session_runtimes
TABLE exam_session_runtime_sections
TABLE cohort_control_events
INDEX idx_exam_schedules_org_status_start ON exam_schedules
INDEX idx_exam_schedules_exam_start ON exam_schedules
INDEX idx_exam_schedules_version ON exam_schedules
INDEX idx_schedule_registrations_schedule_actor_active ON schedule_registrations
INDEX idx_schedule_registrations_schedule_access_updated ON schedule_registrations
INDEX idx_schedule_registrations_actor_updated ON schedule_registrations
INDEX idx_schedule_registrations_student_updated ON schedule_registrations
INDEX idx_schedule_registrations_schedule_user_active ON schedule_registrations
INDEX idx_schedule_registrations_wcode ON schedule_registrations
INDEX idx_schedule_staff_assignments_active ON schedule_staff_assignments
INDEX idx_schedule_staff_assignments_actor_role_created ON schedule_staff_assignments
INDEX idx_schedule_staff_assignments_user_role_created ON schedule_staff_assignments
INDEX idx_runtime_sections_runtime_order ON exam_session_runtime_sections
INDEX idx_cohort_control_events_schedule_created ON cohort_control_events
```

### backend/migrations/0006_delivery.sql
```
TABLE student_attempts
TABLE student_attempt_mutations
TABLE student_heartbeat_events
INDEX idx_student_attempts_schedule_phase_updated ON student_attempts
INDEX idx_student_attempts_exam_updated ON student_attempts
INDEX idx_student_attempts_schedule_user ON student_attempts
INDEX idx_student_attempts_wcode ON student_attempts
INDEX idx_student_attempt_mutations_attempt_session_seq ON student_attempt_mutations
INDEX idx_student_attempt_mutations_attempt_received ON student_attempt_mutations
INDEX idx_student_attempt_mutations_schedule_received ON student_attempt_mutations
INDEX idx_student_attempt_mutations_attempt_session_seq_desc ON student_attempt_mutations
INDEX idx_student_attempt_mutations_server_received_at ON student_attempt_mutations
INDEX idx_student_heartbeat_events_attempt_received ON student_heartbeat_events
INDEX idx_student_heartbeat_events_schedule_received ON student_heartbeat_events
INDEX idx_student_heartbeat_events_server_received_at ON student_heartbeat_events
```

### backend/migrations/0007_proctoring.sql
```
TABLE student_violation_events
TABLE proctor_presence
TABLE session_audit_logs
TABLE session_notes
TABLE violation_rules
INDEX idx_student_violation_events_schedule_created ON student_violation_events
INDEX idx_student_violation_events_attempt_created ON student_violation_events
INDEX idx_proctor_presence_active ON proctor_presence
INDEX idx_proctor_presence_schedule_heartbeat ON proctor_presence
INDEX idx_session_audit_logs_schedule_created ON session_audit_logs
INDEX idx_session_audit_logs_target_created ON session_audit_logs
INDEX idx_session_notes_schedule_created ON session_notes
INDEX idx_violation_rules_schedule_created ON violation_rules
```

### backend/migrations/0008_grading_results.sql
```
TABLE grading_sessions
TABLE student_submissions
TABLE section_submissions
TABLE writing_task_submissions
TABLE review_drafts
TABLE review_events
TABLE student_results
TABLE release_events
INDEX idx_student_submissions_schedule_submitted ON student_submissions
INDEX idx_student_submissions_status_updated ON student_submissions
INDEX idx_review_events_submission_created ON review_events
INDEX idx_student_results_submission_updated ON student_results
INDEX idx_student_results_release_status_updated ON student_results
INDEX idx_release_events_result_created ON release_events
```

### backend/migrations/0009_media_cache_outbox.sql
```
TABLE media_assets
TABLE shared_cache_entries
TABLE idempotency_keys
TABLE outbox_events
INDEX idx_media_assets_owner_updated ON media_assets
INDEX idx_media_assets_status_updated ON media_assets
INDEX idx_idempotency_keys_expires_at ON idempotency_keys
INDEX idx_outbox_events_publish_pending ON outbox_events
INDEX idx_outbox_events_aggregate ON outbox_events
INDEX idx_outbox_events_created_at ON outbox_events
```

### backend/migrations/0010_auth_security.sql
```
TABLE users
TABLE user_password_credentials
TABLE user_sessions
TABLE user_session_events
TABLE password_reset_tokens
TABLE account_activation_tokens
TABLE student_profiles
TABLE staff_profiles
TABLE attempt_sessions
INDEX idx_user_sessions_user_active ON user_sessions
INDEX idx_user_session_events_session_created ON user_session_events
INDEX idx_password_reset_tokens_expires ON password_reset_tokens
INDEX idx_account_activation_tokens_expires ON account_activation_tokens
INDEX idx_student_profiles_student_id ON student_profiles
INDEX idx_attempt_sessions_user_active ON attempt_sessions
```

### backend/migrations/0014_student_attempt_presence.sql
```
TABLE student_attempt_presence
INDEX idx_student_attempt_presence_schedule_heartbeat ON student_attempt_presence
```

### backend/tests/support/mysql.rs
```
pub struct TestDatabase
pub struct TestAuthContext
impl TestDatabase
  pub async fn new(migrations: &[&str]) → Self
  pub fn pool(&self) → &MySqlPool
  pub fn database_url(&self) → String
  pub async fn shutdown(self)
impl TestAuthContext
  pub fn with_auth(&self, builder: axum::http::request::Builder) → axum::http::request::Builder
  pub fn with_csrf(&self, builder: axum::http::request::Builder) → axum::http::request::Builder
pub async fn create_authenticated_user(pool: &MySqlPool, role: UserRole, email: &str, display_name: &str,) → TestAuthContext
pub async fn assign_staff_to_schedule(pool: &MySqlPool, schedule_id: Uuid, user_id: Uuid, role: &str,)
pub async fn bind_student_registration(pool: &MySqlPool, schedule_id: Uuid, student_key: &str, user_id: Uuid,)
pub async fn create_student_registration(pool: &MySqlPool, schedule_id: Uuid, user_id: Uuid, student_id: &str, student_name: &str, student_email: &str,) → String
```

### backend/crates/api/src/http/request_id.rs
```
pub struct RequestId
pub async fn request_id_middleware(State(state) → Response
```

### backend/crates/domain/src/grading.rs
```
pub struct GradingSession
pub struct StudentSubmission
pub struct SectionSubmission
pub struct WritingTaskSubmission
pub struct ReviewDraft
pub struct ReviewEvent
pub struct StudentResult
pub struct ReleaseEvent
pub struct MediaAsset
pub struct UploadIntent
pub struct GradingSessionDetail
pub struct SubmissionReviewBundle
pub struct GradingSessionPagination
pub struct ReviewDraftSummary
pub struct SubmissionReviewSummary
pub struct ResultsAnalytics
pub struct SaveReviewDraftRequest
pub struct StartReviewRequest
pub struct ActorActionRequest
pub struct ReleaseNowRequest
pub struct ScheduleReleaseRequest
pub struct UploadIntentRequest
pub struct CompleteUploadRequest
pub enum GradingSessionStatus
pub enum SectionGradingStatus
```

### backend/crates/domain/src/attempt.rs
```
pub struct QuestionValueMutationPayload
pub struct QuestionIdMutationPayload
pub struct QuestionSlotValueMutationPayload
pub struct QuestionSlotIdMutationPayload
pub struct TaskValueMutationPayload
pub struct TaskIdMutationPayload
pub struct PositionMutationPayload
pub struct ViolationMutationPayload
pub struct TelemetryMutationPayload
pub struct ObjectiveAnswers
pub struct WritingAnswers
pub struct QuestionFlags
pub struct StudentIntegrity
pub struct StudentClientPosition
pub struct StudentRecovery
pub struct ViolationSnapshotEntry
pub struct ViolationsSnapshot
pub struct StudentAttempt
pub struct StudentAttemptMutation
pub struct StudentHeartbeatEvent
pub struct StudentSessionQuery
pub struct StudentPrecheckRequest
pub struct StudentBootstrapRequest
pub struct MutationEnvelope
pub struct StudentMutationBatchRequest
```

### backend/crates/infrastructure/src/telemetry.rs
```
pub struct ProcessMemoryProfile
pub struct Telemetry
impl Telemetry
impl Telemetry
impl Telemetry
  pub fn new() → Self
```

### backend/monitoring/prometheus-alert-rules.yml
```
keys: [groups]
```

### backend/migrations/0025_join_storm_admission_queue.sql
```
TABLE student_admission_queue
```

### backend/crates/api/src/routes/schedules.rs
```
pub async fn list_schedules(State(state) → Result<ApiResponse<Vec<Exam...
pub async fn create_schedule(State(state) → Result<ApiResponse<ExamSche...
pub async fn get_schedule(State(state) → Result<ApiResponse<ExamSche...
pub async fn update_schedule(State(state) → Result<ApiResponse<ExamSche...
pub async fn delete_schedule(State(state) → Result<StatusCode, ApiError>
pub async fn get_runtime(State(state) → Result<ApiResponse<ExamSess...
pub async fn apply_runtime_command(State(state) → Result<ApiResponse<ExamSess...
pub async fn create_student_registration(State(state) → Result<ApiResponse<ielts_ba...
```

### backend/crates/application/src/proctoring.rs
```
pub struct ProctoringService
pub struct ProctorSessionDetailOptions
pub struct AutoAdvanceOutcome
pub enum ProctoringError
impl ProctoringService
  pub fn new(pool: MySqlPool) → Self
  pub async fn list_sessions(&self, live_mode_enabled: bool,) → Result<Vec<ProctorSessionSu...
```

### backend/crates/application/src/builder.rs
```
pub struct BuilderService
pub enum BuilderError
impl BuilderService
  pub fn new(pool: MySqlPool) → Self
  pub async fn create_exam(&self, ctx: &ActorContext, req: CreateExamRequest,) → Result<ExamEntity, BuilderE...
```

### backend/crates/api/src/frontend.rs
```
pub async fn serve_frontend(State(state) → Response
```

### backend/crates/infrastructure/src/config.rs
```
pub struct AppConfig
impl AppConfig
  pub fn from_env() → Self
impl AppConfig
```

### backend/crates/api/src/router.rs
```
pub fn build_router(state: AppState) → Router
```

### backend/crates/api/src/routes/auth.rs
```
pub async fn login(State(state) → Result<impl IntoResponse, A...
pub async fn session(State(state) → Result<ApiResponse<SessionR...
pub async fn logout(State(state) → Result<impl IntoResponse, A...
pub async fn logout_all(State(state) → Result<impl IntoResponse, A...
pub async fn activate_account(State(state) → Result<impl IntoResponse, A...
pub async fn request_password_reset(State(state) → Result<ApiResponse<()>, Api...
pub async fn complete_password_reset(State(state) → Result<impl IntoResponse, A...
pub async fn student_entry(State(state) → Result<Response, ApiError>
```

### backend/crates/api/src/routes/answer_history.rs
```
pub struct DetailQuery
pub struct ExportQuery
pub async fn get_overview(State(state) → Result<ApiResponse<ielts_ba...
pub async fn get_overview_by_attempt(State(state) → Result<ApiResponse<ielts_ba...
pub async fn get_target_detail_by_attempt(State(state) → Result<ApiResponse<ielts_ba...
pub async fn get_target_detail(State(state) → Result<ApiResponse<ielts_ba...
pub async fn export_target(State(state) → Result<ApiResponse<ielts_ba...
```

### backend/crates/api/src/routes/student.rs
```
pub struct HeartbeatQuery
pub enum HeartbeatResponseMode
impl ApiMutationCommandPayload
pub async fn get_student_session(State(state) → Result<ApiResponse<StudentS...
pub async fn get_student_static_session(State(state) → Result<ApiResponse<StudentS...
pub async fn get_student_live_session(State(state) → Result<ApiResponse<StudentL...
pub async fn save_precheck(State(state) → Result<ApiResponse<ielts_ba...
pub async fn bootstrap_student_session(State(state) → Result<ApiResponse<StudentS...
pub async fn apply_mutation_batch(State(state) → Result<ApiResponse<StudentM...
pub async fn record_heartbeat(State(state) → Result<ApiResponse<StudentH...
pub async fn record_audit(State(state) → Result<ApiResponse<()>, Api...
pub async fn submit_student_session(State(state) → Result<ApiResponse<StudentS...
```

### backend/crates/application/src/answer_history.rs
```
pub struct AnswerHistoryService
pub enum AnswerHistoryError
impl AnswerHistoryService
  pub fn new(pool: MySqlPool) → Self
  pub async fn resolve_submission_id_from_attempt(&self, attempt_id: Uuid,) → Result<String, AnswerHistor...
  pub async fn get_overview(&self, submission_id: Uuid,) → Result<AnswerHistoryOvervie...
  pub async fn get_overview_by_attempt(&self, attempt_id: Uuid,) → Result<AnswerHistoryOvervie...
```

### backend/crates/application/src/auth.rs
```
pub struct AuthService
pub struct AuthenticatedSession
pub struct SessionIssue
pub struct AttemptAuthorization
pub struct StudentAccess
pub struct StaffAccess
pub enum AuthError
impl AuthService
  pub fn new(pool: MySqlPool, config: AppConfig) → Self
  pub async fn login(&self, req: LoginRequest, user_agent: Option<&str>, ip_address: Option<&str>,) → Result<SessionIssue, AuthEr...
  pub async fn current_session(&self, session_token: &str,) → Result<Option<Authenticated...
impl JoinedSessionRow
impl UserRole
impl User
```

### backend/crates/application/src/delivery.rs
```
pub struct DeliveryService
pub enum DeliveryConflictReason
pub enum MutationBatchResponseMode
pub enum DeliveryError
impl DeliveryConflictReason
  pub fn as_str(self) → &'static str
impl DeliveryError
  pub fn conflict_reason_code(&self) → Option<&'static str>
impl DeliveryService
  pub fn new(pool: MySqlPool) → Self
  pub fn with_auth(pool: MySqlPool, config: AppConfig) → Self
  pub fn with_runtime_tuning(pool: MySqlPool, _idempotency_usable_hours: i64, _submit_idempotency_usable_hours: i64, _violation_idempotency_usable_hours: i64, _heartbeat_min_write_interval_secs: u64,) → Self
  pub fn with_auth_runtime_tuning(pool: MySqlPool, config: AppConfig, _idempotency_usable_hours: i64, _submit_idempotency_usable_hours: i64, _violation_idempotency_usable_hours: i64, _heartbeat_min_write_interval_secs: u64,) → Self
  pub async fn get_session_context(&self, schedule_id: Uuid, wcode: Option<String>, student_key: Option<String>, candidate_id: Option<String>,) → Result<StudentSessionContex...
  pub async fn get_session_context_with_attempt_credential(&self, schedule_id: Uuid, wcode: Option<String>, student_key: Option<String>, candidate_id: Option<String>, principal: &AuthenticatedSession, client_session_id: Option<String>,) → Result<StudentSessionContex...
  pub async fn get_static_session_context(&self, schedule_id: Uuid,) → Result<ielts_backend_domain...
  pub async fn get_live_session_context(&self, schedule_id: Uuid, wcode: Option<String>, student_key: Option<String>, candidate_id: Option<String>,) → Result<ielts_backend_domain...
impl ObjectiveMutationGate
pub async fn finalize_pending_schedule_attempts(pool: &MySqlPool, schedule_id: Uuid, completion_reason: &str, _batch_size: i64,) → Result<(), DeliveryError>
```

### backend/crates/application/src/grading.rs
```
pub struct GradingProjectionRequest
pub struct GradingProjectionReport
pub struct ObjectiveAutoGradingBackfillRequest
pub struct ObjectiveAutoGradingBackfillReport
pub struct GradingService
pub enum GradingError
impl GradingService
  pub fn new(pool: MySqlPool) → Self
  pub fn with_sync_on_read_fallback(pool: MySqlPool, sync_on_read_fallback: bool) → Self
  pub async fn list_sessions(&self, ctx: &ActorContext, limit: u64,) → Result<Vec<GradingSession>,...
  pub async fn get_session_detail(&self, ctx: &ActorContext, session_id: Uuid,) → Result<GradingSessionDetail...
  pub async fn get_session_detail_page(&self, ctx: &ActorContext, session_id: Uuid, page: u64, page_size: u64,) → Result<GradingSessionDetail...
impl ObjectiveExpectedAnswer
```

### backend/crates/application/src/scheduling.rs
```
pub struct SchedulingService
pub enum SchedulingError
impl SchedulingService
  pub fn new(pool: MySqlPool) → Self
  pub async fn create_schedule(&self, ctx: &ActorContext, req: CreateScheduleRequest,) → Result<ExamSchedule, Schedu...
impl ScheduleRegistrationRow
```

### backend/crates/domain/src/answer_history.rs
```
pub struct AnswerHistoryQuestionSummary
pub struct AnswerHistorySectionStat
pub struct AnswerHistorySignal
pub struct AnswerHistoryOverview
pub struct AnswerHistoryCheckpoint
pub struct AnswerHistoryTechnicalLogRow
pub struct AnswerHistoryTargetDetail
pub struct AnswerHistoryExport
pub enum AnswerHistoryTargetType
pub enum AnswerHistoryExportFormat
```

### backend/crates/domain/src/schedule.rs
```
pub struct ExamSchedule
pub struct ScheduleSectionPlanEntry
pub struct RuntimeSectionState
pub struct ExamSessionRuntime
pub struct CohortControlEvent
pub struct CreateScheduleRequest
pub struct UpdateScheduleRequest
pub struct RuntimeCommandRequest
pub struct ProctorPresence
pub struct SessionAuditLog
pub struct SessionNote
pub struct ViolationRule
pub struct StudentSessionSummary
pub struct ProctorAlert
pub struct ProctorSessionSummary
pub struct ProctorSessionDetail
pub struct ProctorPresenceRequest
pub struct ExtendSectionRequest
pub struct CompleteExamRequest
pub struct AttemptCommandRequest
pub struct AlertAckRequest
pub struct DegradedLiveState
pub struct LiveUpdateEvent
pub struct ScheduleRegistration
pub enum DeliveryMode
```

## e2e

### e2e/prod-load/exam-url.ts
```
export interface ParsedExamRegisterUrl
  origin: string
  registerUrl: string
  scheduleId: string
export function parseExamRegisterUrl(input) → ParsedExamRegisterUrl
```

### e2e/prod-load/live-dashboard-server.ts
```
export interface DashboardEvent
  userId: string
  status: string
  phase: string
  imageBase64?: string
  lastSeenAt: string
  error?: string
  metrics?: Record<string, number | string | bo
export function startLiveDashboardServer(port)
```

### e2e/prod-load/README.md
```
h1 Prod Load E2E (Real Exam Day)
h2 Files
h2 Run (manual / controlled window)
h2 Optional Bootstrap (create exam + schedule automatically)
h2 Provision Editor + Proctors via DB (generates unique passwords)
h1 after bootstrap, read scheduleId from e2e/.generated/prod-runtime.json and provision:
h2 Live 100-User Runner (Dashboard)
code-fence bash
code-fence plain
code-fence txt
```

### e2e/prod-load/live-multi-user-runner.ts
```
export interface RunnerConfig
  registerUrl: string
  userCount: number
  usersFile: string
  dashboardPort: number
  screenshotIntervalMs: number
  jpegQuality: number
  headless: boolean
  outputDir: string
export interface ScenarioContext
  origin: string
  scheduleId: string
  config: RunnerConfig
export interface ScenarioPlugin
  prepare: (page: Page, user: VirtualUser, ctx
  waitForStart: (page: Page, user: VirtualUser, ctx
  execute: (page: Page, user: VirtualUser, ctx
  finalize: (page: Page, user: VirtualUser, ctx
```

### e2e/prod-load/user-source.ts
```
export interface VirtualUser
  userId: string
  email: string
  password: string
  candidateId?: string
export function loadUsersFromFile(filePath, requiredCount, userOffset = 0) → VirtualUser[]
```

### e2e/support/prodData.ts
```
export type ProdTarget
export type ProdCreds
export type ProdRuntimeOverride
export function resolveProdTargetPath() → string
export function resolveProdCredsPath() → string
export function resolveProdRuntimePath() → string
export function readProdTarget() → ProdTarget
export function readProdCreds() → ProdCreds
export function readProdRuntimeOverride() → ProdRuntimeOverride | null
export function readEffectiveProdTarget() → ProdTarget
```

## k6

### k6/README.md
```
h1 k6 Prod Load Suite
h2 Scenarios
h2 Data
h2 Safety gate
h2 Common overrides
h2 Typical runs
h2 Notes
code-fence bash
code-fence plain
```

### k6/prod-load-helpers.js
```
export function readJson(path)
export function clampInt(value, min, max)
export function uuidV4()
export function stableHash32(input)
export function computeJitterSeconds(runId, wcode, maxSeconds)
export function resolveBaseUrl(target)
export function resolveScheduleId(target)
export function jsonHeaders(extra)
export function cookieValue(jar, baseUrl, candidates)
export function csrfHeader(jar, baseUrl)
export function pickFirstQuestionId(snapshot)
export function pickFirstWritingTaskId(snapshot)
export function collectObjectiveQuestionIds(snapshot)
export function collectWritingTaskIds(snapshot)
export function selectStaffCandidates(creds, preferEditor = true)
export function loginControlStaff(baseUrl, scheduleId, creds, preferEditor = true)
export function ensureStudentRegistrations(baseUrl, scheduleId, creds, students, preferEditor = true)
export function shouldAutoRegisterStudents()
export function ensureProdRunAllowed()
export function buildStudentSlice(target, count, offset)
export function bootstrapStudentSession(baseUrl, scheduleId, student, jar, clientSessionId)
export function getStudentSession(baseUrl, scheduleId, jar, query, tags)
export function waitForStudentSession(baseUrl, scheduleId, jar, options)
export function sendHeartbeat(baseUrl, scheduleId, jar, attemptId, attemptToken, clientSessionId, tags)
export function sendMutationBatch(baseUrl, scheduleId, jar, attemptId, attemptToken, clientSessionId, mutations, tags)
```

### k6/prod-submit-storm-200.js
```
export function setup()
export function controlFlow(data)
export function studentFlow(data)
export function handleSummary(data)
function progressiveValues(finalValue, steps)
function looksLikeRuntimeAlreadyExistsError(resp)
function normalizeText(value)
function canonicalObjective(value)
function canonicalWriting(value)
function hash12(value)
function findByKeyDeep(root, key)
function objectiveTargetKey(target)
function collectObjectiveTargets(contentSnapshot)
function collectWritingTargets(contentSnapshot)
function buildExpectedAnswerMaps(localRunId, student, objectiveTargets, writingIds)
function buildMutationBatch(stepIndex, stepCount, objectiveTargets, writingIds, expectedObjective, expectedWriting)
function collectMismatches(attempt, objectiveTargets, expectedObjective, writingTargets, expectedWriting)
```

## load-runner

### load-runner/e2e/prod-load/exam-url.ts
```
export interface ParsedExamRegisterUrl
  origin: string
  registerUrl: string
  scheduleId: string
export function parseExamRegisterUrl(input) → ParsedExamRegisterUrl
```

### load-runner/k6/prod-auto-submit-200.js
```
export function setup()
export function controlFlow(data)
export function studentFlow(data)
```

### load-runner/k6/prod-exam-day.js
```
export function controlFlow()
export function studentFlow()
function readJson(path)
function clampInt(value, min, max)
function uuidV4()
function stableHash32(input)
function computeJitterSeconds(runId, wcode, maxSeconds)
function resolveBaseUrl(target)
function resolveScheduleId(target)
function cookieValue(jar, baseUrl, candidates)
function csrfHeader(jar, baseUrl)
function jsonHeaders(extra)
function pickFirstQuestionId(snapshot)
function pickFirstWritingTaskId(snapshot)
```

### load-runner/k6/prod-load-helpers.js
```
export function readJson(path)
export function clampInt(value, min, max)
export function uuidV4()
export function stableHash32(input)
export function computeJitterSeconds(runId, wcode, maxSeconds)
export function resolveBaseUrl(target)
export function resolveScheduleId(target)
export function jsonHeaders(extra)
export function cookieValue(jar, baseUrl, candidates)
export function csrfHeader(jar, baseUrl)
export function pickFirstQuestionId(snapshot)
export function pickFirstWritingTaskId(snapshot)
export function selectStaffCandidates(creds, preferEditor = true)
export function loginControlStaff(baseUrl, scheduleId, creds, preferEditor = true)
export function ensureStudentRegistrations(baseUrl, scheduleId, creds, students, preferEditor = true)
export function shouldAutoRegisterStudents()
export function ensureProdRunAllowed()
export function buildStudentSlice(target, count, offset)
export function bootstrapStudentSession(baseUrl, scheduleId, student, jar, clientSessionId)
export function getStudentSession(baseUrl, scheduleId, jar, query, tags)
export function waitForStudentSession(baseUrl, scheduleId, jar, options)
export function sendHeartbeat(baseUrl, scheduleId, jar, attemptId, attemptToken, clientSessionId, tags)
export function sendMutationBatch(baseUrl, scheduleId, jar, attemptId, attemptToken, clientSessionId, mutations, tags)
export function submitAttempt(baseUrl, scheduleId, jar, attemptId, attemptToken, tags)
function parseScheduleIdFromUrl(url)
```

### load-runner/k6/prod-resume-100.js
```
export function setup()
export function controlFlow(data)
export function studentFlow(data)
```

### load-runner/k6/prod-section-transition-200.js
```
export function setup()
export function controlFlow(data)
export function studentFlow(data)
```

### load-runner/k6/prod-start-exam-200.js
```
export function setup()
export function controlFlow(data)
export function studentFlow(data)
```

### load-runner/k6/prod-submit-storm-200.js
```
export function setup()
export function controlFlow(data)
export function studentFlow(data)
```

### load-runner/Dockerfile
```
FROM mcr.microsoft.com/playwright:v1.59.1-noble
EXPOSE 3366
CMD ["npm", "run", "ui"]
ENV NODE_ENV
ENV HOST
ENV PORT
```

### load-runner/README.md
```
h1 Standalone Load Runner
h2 Local
h2 Railway
code-fence bash
code-fence plain
```

### load-runner/e2e/prod-load/student-answer-capture.ts
```
export interface ExpectedAnswerSnapshot
  answers: Record<string, unknown>
  writingAnswers: Record<string, string>
export function installStudentAnswerCapture(page)
```

### load-runner/e2e/prod-load/user-source.ts
```
export interface VirtualUser
  userId: string
  email: string
  password: string
  candidateId?: string
export function loadUsersFromFile(filePath, requiredCount, userOffset = 0) → VirtualUser[]
```

### load-runner/ui-server.mjs
```
function pushLog(line)
function cleanupGeneratedArtifacts()
function pushDashboardEvent(event)
async function refresh()
function validateForm()
function upsert(m)
```

### load-runner/k6/prod-transition-reconciliation-200.js
```
export function setup()
export function controlFlow(data)
export function studentFlow(data)
function randomCadenceSeconds()
function findFirstQuestionIdInBlocks(blocks)
function pickObjectiveQuestionIdForSection(contentSnapshot, sectionKey)
function pickWritingTaskId(contentSnapshot)
function buildTarget(contentSnapshot, sectionKey)
function readAttemptValue(attempt, targetRef)
function buildMutationCommand(targetRef, value)
function sendTypingMutation(data, jar, attemptId, attemptToken, clientSessionId, revision, targetRef, value, tagSuffix)
```

### load-runner/k6/README.md
```
h1 k6 Prod Load Suite
h2 Scenarios
h2 Data
h2 Safety gate
h2 Common overrides
h2 Typical runs
h2 Notes
code-fence bash
code-fence plain
```

### load-runner/e2e/prod-load/grading-verifier.ts
```
export interface ExpectedAnswerSnapshot
  answers: Record<string, unknown>
  writingAnswers: Record<string, string>
export interface GradingVerifyConfig
  origin: string
  adminEmail: string
  adminPassword: string
  strict: boolean
export interface GradingVerifyResult
  ok: boolean
  mismatches: Array<{ kind: 'objective' | 'writin
  writingComparisons: Array<{ taskId: string
  expected: string | null
  actual: string
  match: boolean
  writingTasksRaw: Array<{ taskId: string
  studentText: string
export async function createGradingVerifier(config) → Promise<
```

### load-runner/e2e/prod-load/live-dashboard-server.ts
```
export interface DashboardEvent
  userId: string
  status: string
  phase: string
  imageBase64?: string
  lastSeenAt: string
  error?: string
  metrics?: Record<string, number | string | bo
  comparison?: { submissionId?: string
export function startLiveDashboardServer(port)
```

### load-runner/e2e/prod-load/live-multi-user-runner.ts
```
export interface RunnerConfig
  registerUrl: string
  userCount: number
  usersFile: string
  dashboardPort: number
  screenshotIntervalMs: number
  jpegQuality: number
  headless: boolean
  outputDir: string
export interface ScenarioContext
  origin: string
  scheduleId: string
  config: RunnerConfig
export interface ScenarioPlugin
  prepare: (page: Page, user: VirtualUser, ctx
  waitForStart: (page: Page, user: VirtualUser, ctx
  execute: (page: Page, user: VirtualUser, ctx
  finalize: (page: Page, user: VirtualUser, ctx
```

## src

### src/components/student/useDragToPan.ts
```
export function useDragToPan(enabled)
```

### src/components/blocks/AcceptedAnswersEditor.tsx
```
component AcceptedAnswersEditor
props AcceptedAnswersEditorProps
hook useState
hook useMemo
export AcceptedAnswersEditor
handler onChange
handler onKeyDown
handler onClick
```

### src/utils/studentObservability.ts
```
export type StudentObservabilityField
export function withStudentObservabilityDimensions(fields, StudentObservabilityField> & StudentObservabilityDimensions = {},) → Record<string, StudentObservab
export function emitStudentObservabilityMetric(name, fields, StudentObservabilityField> = {},) → void
```

### src/features/builder/hooks/useBuilderRouteController.ts
```
export interface BuilderRouteController
  error: string | null
  exam: ExamEntity | undefined
  isLoading: boolean
  state: ExamState | null
  handleArchive: () => Promise<void>
  handleOpenScheduling: () => void
  handlePublish: (notes?: string) => Promise<void>
  handleReturnToAdmin: () => void
export function useBuilderRouteController(examId?,) → BuilderRouteController
```

### src/app/hooks/useLiveUpdates.ts
```
export interface LiveUpdateEvent
  kind: string
  id: string
  revision: number
  event: string
  scheduleId?: string
```

### src/components/blocks/TFNGBlock.tsx
```
props Props
hook useState
export TFNGBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/MatchingBlock.tsx
```
props Props
hook useState
export MatchingBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/MultiSelectMCQBlock.tsx
```
props Props
hook useState
export MultiSelectMCQBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/utils/insertedImages.ts
```
export function supportsInsertedImages(target, 'type'>) → boolean
export function getInsertedImages(block, 'insertedImages'>) → InsertedBlockImage[]
export function coerceInsertedImages(value, fallbackPrefix = 'img',) → InsertedBlockImage[]
```

### src/components/blocks/InsertedImagesEditor.tsx
```
component InsertedImagesEditor
props InsertedImagesEditorProps
hook useEffect
export InsertedImagesEditor
handler onClick
handler onChange
```

### src/utils/referenceImagePlacement.ts
```
export const getReferenceImagePlacement = (block,) =>
export const isInstructionReferencePlacement = (block,) =>
```

### src/components/student/QuestionNavigator.tsx
```
component QuestionNavigator
props QuestionNavigatorProps
export QuestionNavigator
handler onClick
```

### src/utils/examIdCollisionCheck.ts
```
export type ExamIntegrityIssue
export function getExamIdCollisionIssues(state) → ExamIntegrityIssue[]
```

### src/components/blocks/ShortAnswerBlock.tsx
```
component ShortAnswerBlock
props ShortAnswerBlockProps
export ShortAnswerBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/NoteCompletionBlock.tsx
```
component NoteCompletionBlock
props NoteCompletionBlockProps
export NoteCompletionBlock
handler onNumberLabel
handler onSlotOffset
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/MapLabelingBlock.tsx
```
props Props
hook useState
hook useRef
hook useEffect
export MapLabelingBlock
handler onChange
handler onKeyDown
handler onClick
handler onDragStart
handler onDragOver
handler onDrop
```

### src/components/blocks/DiagramLabelingBlock.tsx
```
component DiagramLabelingBlock
props DiagramLabelingBlockProps
hook useState
hook useEffect
export DiagramLabelingBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/FlowChartBlock.tsx
```
component FlowChartBlock
props FlowChartBlockProps
export FlowChartBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/ClassificationBlock.tsx
```
component ClassificationBlock
props ClassificationBlockProps
export ClassificationBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/MatchingFeaturesBlock.tsx
```
component MatchingFeaturesBlock
props MatchingFeaturesBlockProps
export MatchingFeaturesBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/ClozeBlock.tsx
```
props Props
hook useState
export ClozeBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/utils/subAnswerTreeSlots.ts
```
export interface SubAnswerSlotSeed
  slotIndex: number
  numberLabel: string
  prompt: string
  acceptedAnswers: string[]
export function isTreeCapableBlockType(type) → boolean
export function buildSubAnswerSlotSeeds(block, startNumber) → SubAnswerSlotSeed[]
export function healSubAnswerTreeForBlock(block, startNumber, answerTree,) → SubAnswerTreeNode[]
export function appendSubAnswerLeafAtSlot(block, startNumber, answerTree, slotIndex,) → SubAnswerTreeNode[]
```

### src/components/builder/SubAnswerTreeEditor.tsx
```
component NodeRow
component SubAnswerTreeEditor
props SubAnswerTreeEditorProps
props NodeRowProps
hook useState
hook useMemo
hook useEffect
export SubAnswerTreeEditor
handler onChange
handler onUpdateNode
handler onRemoveNode
handler onMoveNode
handler onAddChild
```

### src/components/proctor/ProctorApp.tsx
```
component ProctorApp
hook useAuthSession
hook useState
hook useMemo
hook useEffect
export ProctorApp
handler onChange
handler onClick
handler onSelectScheduleId
handler onUpdateSessions
handler onUpdateAlerts
handler onUpdateNotes
handler onStartScheduledSession
handler onPauseCohort
handler onResumeCohort
handler onEndSectionNow
handler onExtendCurrentSection
handler onCompleteExam
handler onOpenAnswerHistory
```

### src/components/proctor/StudentDetailPanel.tsx
```
component StudentDetailPanel
props StudentDetailPanelProps
hook useState
hook useMemo
export StudentDrawerTab
export StudentDetailPanel
handler onClick
handler onChange
handler onConfirm
```

### src/features/answer-history/routes/AnswerHistoryRoute.tsx
```
component AdminAnswerHistoryRoute
component ProctorAnswerHistoryRoute
hook useNavigate
hook useParams
export AdminAnswerHistoryRoute
export ProctorAnswerHistoryRoute
handler onId
```

### src/features/proctor/routes/ProctorRoot.tsx
```
component ProctorRoot
hook useNavigate
hook useProctorRouteController
export ProctorRoot
handler onError
handler onSelectScheduleId
handler onUpdateSessions
handler onUpdateAlerts
handler onUpdateNotes
handler onStartScheduledSession
handler onPauseCohort
handler onResumeCohort
handler onEndSectionNow
handler onExtendCurrentSection
handler onCompleteExam
handler onOpenAnswerHistory
```

### src/types/grading.ts
```
export interface DrawingAnnotation
  id: string
  taskId: string
  type: 'freehand_draw' | 'arrow' | 'circle
  points: Array<{ x: number
  color: string
  strokeWidth: number
  visibility: CommentVisibility
  createdBy: string
export interface GradingSession
  id: string
  scheduleId: string
  examId: string
  examTitle: string
  publishedVersionId: string
  cohortName: string
  institution?: string | undefined
  startTime: string
export interface StudentSubmission
  id: string
  submissionId: string
  scheduleId: string
  examId: string
  publishedVersionId: string
  studentId: string
```

### src/services/gradingFilters.ts
```
export function mapScheduleStatusToGradingStatus(status,) → GradingSession['status']
export function filterGradingSessions(sessions, filters,) → GradingSession[]
export function filterStudentSubmissions(submissions, filters,) → StudentSubmission[]
```

### src/components/admin/GradingSessionList.tsx
```
component GradingSessionList
props GradingSessionListProps
hook useState
hook useEffect
hook useCallback
hook useMemo
export GradingSessionList
handler onList
handler onChange
handler onClick
```

### src/features/admin/routes/ExamsRoute.tsx
```
component ExamsRoute
hook useAdminContext
hook useNavigate
hook useState
export ExamsRoute
handler onNavigate
handler onEditExam
handler onGoToConfig
handler onGoToReview
handler onCreateExam
handler onCloneExam
handler onCreateFromTemplate
handler onGetVersions
handler onGetEvents
handler onRestoreVersion
handler onRepublishVersion
handler onCompareVersions
handler onBulkPublish
handler onBulkUnpublish
handler onBulkArchive
handler onBulkDuplicate
handler onBulkExport
handler onBulkDelete
handler onDeleteExam
```

### src/components/admin/AdminExams.tsx
```
component ExamGridCard
component ExamListItem
component AdminExams
props ExamGridCardProps
props ExamListItemProps
hook useState
hook useEffect
hook useMemo
export AdminExams
handler onHistory
handler onClick
handler onChange
handler onAddFilter
handler onRemoveFilter
handler onSortChange
handler onClearSelection
handler onBulkPublish
handler onBulkUnpublish
handler onBulkArchive
handler onBulkDelete
handler onBulkDuplicate
handler onBulkExport
handler onToggleSelect
handler onEdit
handler onPreview
```

### src/components/admin/ExamBulkActionBar.tsx
```
component ExamBulkActionBar
props ExamBulkActionBarProps
export ExamBulkActionBar
handler onClick
```

### src/components/admin/ExamSettingsDrawer.tsx
```
component ExamSettingsDrawerComponent
props ExamSettingsDrawerProps
hook useState
hook useRef
hook useMemo
export ExamSettingsDrawer
handler onPlan
handler onWarning
handler onTitle
handler onSummary
handler onType
handler onClick
handler onChange
handler onThreshold
```

### src/components/student/WarningOverlay.tsx
```
component WarningOverlay
props WarningOverlayProps
hook useState
hook useEffect
export WarningOverlay
handler onClick
```

### src/constants/examDefaults.ts
```
export const syncConfigWithStandards = (config) =>
export const normalizeExamConfig = (config?) =>
export const createDefaultConfig = (type, preset) =>
```

### src/features/builder/components/SecurityTab.tsx
```
component SecurityTab
props SecurityTabProps
export SecurityTab
handler onChange
```

### src/components/admin/AdminScheduling.tsx
```
component AdminScheduling
props AdminSchedulingProps
hook useState
hook useMemo
hook useEffect
export AdminScheduling
handler onId
handler onLabel
handler onClick
handler onSubmit
handler onChange
```

### src/components/admin/ScheduleSessionModal.tsx
```
component ScheduleSessionModal
props ScheduleSessionModalProps
hook useState
hook useEffect
hook useMemo
export ScheduleSessionModal
handler onId
handler onClick
handler onSubmit
handler onChange
```

### src/types/domain.ts
```
export interface ExamEntity
  id: string
  slug: string
  title: string
  type: 'Academic' | 'General Training'
  status: ExamStatus
  visibility: ExamVisibility
  owner: string
  createdAt: string
export interface ExamVersionValidationSnapshot
  isValid: boolean
  errorCount: number
  warningCount: number
  lastValidatedAt: string
export interface ExamVersion
  id: string
  examId: string
  versionNumber: number
  parentVersionId: string | null
  contentSnapshot: ExamState
  configSnapshot: ExamConfig
  validationSnapshot?: ExamVersionValidationSnapshot | und
  createdBy: string
export interface ExamVersionSummary
  id: string
```

### src/utils/gradingSeedData.ts
```
export async function seedGradingData()
export async function clearGradingData()
```

### src/components/student/StudentAppWrapper.tsx
```
component StudentAppWrapper
props StudentAppWrapperProps
hook useStudentAppContext
export StudentAppWrapper
handler onExit
handler onRefreshRuntime
```

### src/components/student/StudentHeader.tsx
```
component StudentHeader
props StudentHeaderProps
hook useState
hook useRef
hook useCallback
hook useEffect
export StudentHeader
handler onRef
handler onClick
```

### src/components/student/highlightPalette.ts
```
export interface StudentHighlightPaletteEntry
  id: StudentHighlightColor
  label: string
  swatchClassName: string
  highlightClassName: string
  highlightColorValue: string
export type StudentHighlightColor
export function getStudentHighlightPaletteEntry(color,) → StudentHighlightPaletteEntry
export function getStudentHighlightClassName(color) → string
export function getStudentHighlightColorValue(color) → string
```

### src/components/student/ProtectedInput.tsx
```
component ProtectedInput
hook useOptionalStudentAttempt
hook useRef
hook useEffect
export ProtectedInput
handler onId
handler onChangeRef
handler onInput
handler onChange
handler onBlur
```

### src/components/student/ProtectedSelect.tsx
```
component ProtectedSelect
hook useOptionalStudentAttempt
hook useRef
hook useEffect
export ProtectedSelect
handler onChangeRef
handler onChange
handler onBlur
```

### src/components/student/ProtectedChoiceInput.tsx
```
component ProtectedChoiceInput
hook useOptionalStudentAttempt
hook useRef
hook useEffect
export ProtectedChoiceInput
handler onChangeRef
handler onChange
handler onBlur
```

### src/components/StimulusPane.tsx
```
component StimulusPane
hook useRef
hook useState
hook useMemo
hook useEffect
export StimulusPane
handler onClick
handler onInput
handler onPaste
handler onDrop
handler onSave
```

### src/components/admin/QuestionTracebackPanel.tsx
```
component QuestionStatusBadge
component QuestionTracebackPanel
props QuestionTracebackPanelProps
hook useMemo
export QuestionTracebackPanel
```

### src/components/blocks/SentenceCompletionBlock.tsx
```
component SentenceCompletionBlock
props SentenceCompletionBlockProps
export SentenceCompletionBlock
handler onScoreCount
handler onScoringMode
handler onNumberLabel
handler onCount
handler onSlotOffset
handler onChange
handler onKeyDown
handler onClick
```

### src/components/blocks/TableCompletionBlock.tsx
```
component TableCompletionBlock
props TableCompletionBlockProps
hook useCallback
hook useEffect
hook useMemo
export TableCompletionBlock
handler onChange
handler onKeyDown
handler onClick
```

### src/components/workspaces/WritingWorkspace.tsx
```
component WritingWorkspace
hook useState
hook useMemo
hook useEffect
export WritingWorkspace
handler onChange
handler onThreshold
handler onRubricChange
handler onAddTask
handler onDeleteTask
handler onEditTask
handler onInsert
handler onSaveCustom
handler onUpdateCustom
handler onDeleteCustom
```

### src/utils/completionPromptText.ts
```
export function isSuspiciousCompletionPromptText(value) → boolean
export function trimSuspiciousCompletionPromptText(value, segmentLength = 140) → string
```

### src/utils/imageUrl.ts
```
export const getImageUrlCandidates = (value) =>
export const normalizeImageUrl = (value) =>
```

### src/utils/sanitizeHtml.ts
```
export function sanitizeHtml(html) → string
```

### src/utils/tableCompletion.ts
```
export interface TablePlaceholderSlot
  row: number
  col: number
  placeholderIndex: number
  placeholderCount: number
export interface TablePlaceholderAnalysis
  slots: TablePlaceholderSlot[]
  multiPlaceholderSlots: TablePlaceholderSlot[]
export function isSuspiciousTableCellContent(value) → boolean
export function trimSuspiciousTableCellContent(value, segmentLength = 140) → string
export function analyzeTablePlaceholders(rows, headerCount) → TablePlaceholderAnalysis
export function getCanonicalTableCells(block) → TableCell[]
export function normalizeTableCompletionBlock(block) → TableCompletionBlock
export function normalizeExamStateTableCompletionBlocks(state) → ExamState
```

### src/features/builder/components/ValidationSummary.tsx
```
component ValidationSummary
props ValidationSummaryProps
export ValidationSummary
handler onScope
```

### src/features/builder/hooks/useReviewRouteController.ts
```
export interface ReviewRouteController
  error: string | null
  exam: ExamEntity | undefined
  isLoading: boolean
  state: ExamState | null
  versions: ExamVersionSummary[]
  schedules: ExamSchedule[]
  publishReadiness: PublishReadiness | undefined
  handlePublish: (notes?: string) => Promise<void>
export function useReviewRouteController(examId?,) → ReviewRouteController
```

### src/features/builder/routes/BuilderRoot.tsx
```
component ScoringAside
component BuilderRoot
hook useParams
hook useNavigate
hook useLocation
hook useBuilderRouteController
hook useUndoRedo
hook useState
hook useRef
hook useEffect
hook useMemo
hook useKeyboardShortcuts
export BuilderRoot
handler onThreshold
handler onChange
handler onSubmitGrade
handler onIndex
handler onAction
handler onUpdateState
handler onReturnToAdmin
handler onNavigateToConfig
handler onOpenPreview
handler onDismiss
```

### src/features/builder/routes/ExamReviewRoute.tsx
```
component ExamReviewRoute
hook useParams
hook useReviewRouteController
hook useState
export ExamReviewRoute
handler onNavigateToBuilder
handler onPublish
handler onSchedulePublish
handler onUnpublish
handler onNumber
handler onRestoreVersion
handler onRepublishVersion
handler onCompareVersions
handler onCreateSchedule
handler onClick
```

### src/components/blocks/SingleMCQBlock.tsx
```
component SingleMCQBlock
props SingleMCQBlockProps
export SingleMCQBlock
handler onStem
handler onChange
handler onKeyDown
handler onClick
handler onStemError
```

### src/components/builder/QuestionBankLibrary.tsx
```
component QuestionBankLibrary
component QuestionCard
component QuestionListItem
props QuestionBankLibraryProps
hook useState
hook useEffect
hook useMemo
export QuestionBankLibrary
handler onClick
handler onChange
```

### src/components/builder/QuestionDetailModal.tsx
```
component QuestionDetailModal
component QuestionContentRenderer
props QuestionDetailModalProps
export QuestionDetailModal
handler onClick
```

### src/components/questionbank/QuestionDetailModal.tsx
```
component QuestionDetailModal
props QuestionDetailModalProps
export QuestionDetailModal
handler onClick
```

### src/components/workspaces/ListeningWorkspace.tsx
```
component ListeningWorkspace
props ListeningWorkspaceProps
hook useRef
hook useState
hook useEffect
export ListeningWorkspace
handler onClick
handler onCount
handler onTimeUpdate
handler onLoadedMetadata
handler onPartSelect
handler onPartAdd
handler onPartDelete
handler onPartReorder
handler onPartRename
handler onChange
```

### src/utils/cloneExamContent.ts
```
export function cloneQuestionBlockWithNewIds(block) → QuestionBlock
export function cloneReadingPassageWithNewIds(passage) → Passage
export function cloneListeningPartWithNewIds(part) → ListeningPart
```

### src/utils/examUtils.ts
```
export const getBlockQuestionCount = (block) =>
export const getPassageQuestionCount = (passage) =>
export const getPartQuestionCount = (part) =>
export const getReadingTotalQuestions = (passages) =>
export const getListeningTotalQuestions = (parts) =>
export const validateBlock = (block) =>
export const validatePassage = (passage) =>
export const validateListeningPart = (part) =>
export const validateReadingModule = (passages) =>
export const validateListeningModule = (parts) =>
export const migrateExam = (exam) =>
export const migrateExams = (exams) =>
export const calculateBandScore = (rawScore, table, number>) =>
export const roundOverallBand = (average, method) =>
export const calculateOverallBand = (scores, config) =>
```

### src/utils/validationUtils.ts
```
export interface ValidationError
  field: string
  message: string
export function validateQuestionBlock(block) → ValidationError[]
```

### src/components/student/accessibilityScale.ts
```
export interface StudentTypographyScale
  rootFontSize: string
  lineHeight: number
  fontScale: number
  controlFontSize: string
  chipFontSize: string
  metaFontSize: string
  previewFontSize: string
export type StudentFontSize
export type StudentPassageReadabilityLevel
export function getStudentTypographyScale(fontSize) → StudentTypographyScale
export function getStudentFontSizeLabel(fontSize) → string
export function clampStudentPassageReadabilityLevel(value) → StudentPassageReadabilityLevel
export function canIncreaseStudentPassageReadability(level,) → boolean
export function canDecreaseStudentPassageReadability(level,) → boolean
export function getStudentPassageReadabilityLabel(level,) → string
```

### src/features/student/liveSnapshotFreshness.ts
```
export type FreshnessDimension
export type LiveSnapshotFreshness
export type LiveSnapshotFreshnessMergeMode
export function compareFreshnessDimension(incoming, applied) → number
export function mergeLiveSnapshotFreshness(previous, incoming, mode,) → LiveSnapshotFreshness
export function extractLiveSnapshotFreshness(live) → LiveSnapshotFreshness
```

### src/app/data/studentSessionQueries.ts
```
export type BackendStudentSessionContext
export type BackendStudentStaticSessionContext
export type BackendStudentLiveSessionContext
export type BackendStudentAttemptPayload
export type SavedStudentAttempt
export function fetchStudentStaticSession(scheduleId, candidateId)
export function fetchStudentLiveSession(scheduleId, candidateId)
export function useStudentStaticSession(scheduleId, candidateId, enabled,)
export function useStudentLiveSession(scheduleId, candidateId, enabled,)
```

### src/services/studentAuditService.ts
```
export async function saveStudentAuditEvent(sessionId, event, payload?, unknown>, targetStudentId?,) → Promise<void>
```

### src/services/attemptCredentialAdapter.ts
```
export interface AttemptCredentialRef
  id: string
  scheduleId: string
export function clearAttemptCredential(attempt) → void
export function storeAttemptCredential(attempt, credential,) → void
export function hasAttemptCredential(scheduleId, attemptId) → boolean
export function buildAttemptAuthorizationHeader(attempt) → Record<string, string>
export function tryBuildAttemptAuthorizationHeader(scheduleId, attemptId,) → Record<string, string> | null
export async function refreshAttemptCredentialForAttempt(attempt, clientSessionId,) → Promise<boolean>
```

### src/features/student/hooks/studentSessionMachineAdapters.ts
```
export function runStudentSessionMachineCommands(commands) → void
```

### src/features/student/hooks/studentSessionStateMachine.ts
```
export interface StudentAnswerInvariantRolloutState
  enabled: boolean
  killSwitch: boolean
  cohort: string | null
  configFingerprint: string | null
  source: 'default' | 'runtime'
export interface StudentSessionMachineEventContext
  applyEpoch: number
  currentEpoch: number
  scheduleId: string | null
  attemptId: string | null
  syncState: string
  source: 'refresh' | 'load'
  rollout: StudentAnswerInvariantRolloutState
  incomingFreshness: LiveSnapshotFreshness
export interface StudentSessionMachineDecision
  discardAll: boolean
  applyAttempt: boolean
  applyRuntime: boolean
export interface StudentSessionMetricCommand
  type: 'emit_metric'
  name: string
  dimensions: Record<string, StudentObservability
export interface StudentSessionMachineTransitionResult
  decision: StudentSessionMachineDecision
```

### src/types/answers.ts
```
export type StudentAnswerValue
```

### src/types/studentAttempt.ts
```
export interface StudentAnswerMutationMeta
  interactionType?: 'typing' | 'discrete' | undefined
  slotIndex?: number | undefined
  slotId?: string | undefined
  slotCount?: number | undefined
  slotValue?: string | undefined
export interface StudentPreCheckCheckResult
  id: 'browser' | 'javascript' | 'fullscr
  label: string
  message: string
  required: boolean
  status: 'pass' | 'warn' | 'fail'
export interface StudentPreCheckResult
  completedAt: string
  browserFamily: 'chrome' | 'edge' | 'safari' | 'fir
  browserVersion: number | null
  screenDetailsSupported: boolean
  heartbeatReady: boolean
  acknowledgedSafariLimitation: boolean
  checks: StudentPreCheckCheckResult[]
export interface StudentAttempt
  id: string
  scheduleId: string
  studentKey: string
  examId: string
```

### src/services/backendBridge.ts
```
export function isBackendBuilderEnabled() → boolean
export function isBackendLibraryEnabled() → boolean
export function isBackendSchedulingEnabled() → boolean
export function isBackendDeliveryEnabled() → boolean
export function isBackendProctoringEnabled() → boolean
export function isBackendGradingEnabled() → boolean
export function hasBackendStatusCode(error, statusCode) → boolean
export function isBackendNotFound(error) → boolean
export function rememberExamRevision(id, revision) → void
export function getExamRevision(id) → number | undefined
export function clearExamRevision(id) → void
export function rememberScheduleRevision(id, revision) → void
export function getScheduleRevision(id) → number | undefined
export function clearScheduleRevision(id) → void
export function rememberAttemptSchedule(attemptId, scheduleId) → void
export function getAttemptSchedule(attemptId) → string | undefined
export async function backendGet(endpoint, config?) → Promise<T>
export async function backendPost(endpoint, body?, config?,) → Promise<T>
export async function backendPut(endpoint, body?, config?,) → Promise<T>
export async function backendPatch(endpoint, body, config?,) → Promise<T>
export async function backendDelete(endpoint, config?) → Promise<void>
export function buildCreateExamPayload(exam, 'slug' | 'title' | 'type' | 'visibility'>)
export function buildUpdateExamPayload(exam, 'title' | 'status' | 'visibility'>, revision,)
export function buildCreateSchedulePayload(schedule, | 'examId' | 'publishedVersionId' | 'cohortName' | 'proctorDisplayName' | 'gradingDisplayName' | 'institution' | 'startTime' | 'endTime' | 'autoStart' | 'autoStop' >,)
export function buildUpdateSchedulePayload(schedule, revision)
```

### src/app/forms/useFormHandler.ts
```
export interface UseFormHandlerOptions
  schema: FormSchema<TValues>
  defaultValues?: DefaultValues<TValues>
  onSubmit?: (values: TValues) => Promise<void> 
  onSuccess?: (values: TValues) => void
  onError?: (error: Error) => void
  mode?: 'onSubmit' | 'onBlur' | 'onChange' 
  resetOnSubmit?: boolean
export interface MultiStepFormOptions
  schemas: Array<FormSchema<TValues>>
  defaultValues?: DefaultValues<TValues>
  onSubmit?: (values: TValues) => Promise<void> 
export type UseFormHandlerReturn
export type MultiStepFormReturn
export function useFormHandler({ schema, defaultValues, onSubmit, onSuccess, onError, mode = 'onSubmit', resetOnSubmit = false, }) → UseFormHandlerReturn<TValues>
export function useMultiStepForm({ schemas, defaultValues, onSubmit, }) → MultiStepFormReturn<TValues>
```

### src/components/admin/gradingAnswerUtils.ts
```
export function extractObjectiveAnswerMap(sectionAnswers,) → Record<string, StudentAnswerVa
export function formatAnswerValue(value) → string
export function getQuestionPrompt(descriptor) → string
export function getCorrectAnswerValue(descriptor) → unknown
export function getCorrectAnswerDisplay(descriptor) → string
export function getStudentAnswerDisplay(descriptor, answerMap, StudentAnswerValue | undefined>,) → string
export function isStudentAnswerCorrect(descriptor, answerMap, StudentAnswerValue | undefined>,) → boolean | null
```

### src/services/policies/examStatusTransitions.ts
```
export type ExamTransitionActorRole
export function canTransition(from, to, actorRole,) → boolean
```

### src/services/examLifecycleService.ts
```
export class ExamLifecycleService
  constructor(private repository)
  async createExam(title, type, initialState, owner) → Promise<TransitionRe
```

### src/services/studentAttemptNormalization.ts
```
export function deriveCandidateId(attempt, 'candidateId' | 'scheduleId' | 'studentKey' | 'id'>,) → string
export function deriveProctorStatus(attempt,) → StudentAttempt['proctorStatus'
export function normalizeStudentAttempt(attempt) → StudentAttempt
export function mergeStudentAttemptRecovery(attempt, recovery,) → StudentAttempt
```

### src/services/examRepository.ts
```
export interface IExamRepository
  getAllExamsWithLegacyMigration()
  getAllExams()
  getExamById(id)
  saveExam(exam)
  deleteExam(id)
  getAllVersions(examId)
  getVersionSummaries(examId)
  getVersionById(id)
export class BackendExamRepository
  async getAllExamsWithLegacyMigration() → Promise<ExamEntity[]
  async getAllExams() → Promise<ExamEntity[]
  async getExamById(id) → Promise<ExamEntity |
  async saveExam(exam) → Promise<void>
  async deleteExam(id) → Promise<void>
  async getAllVersions(examId) → Promise<ExamVersion[
  async getVersionSummaries(examId) → Promise<ExamVersionS
  async getVersionById(id) → Promise<ExamVersion 
```

### src/services/studentIntegrityService.ts
```
export interface StudentIntegritySecurityPolicy
  heartbeatIntervalSeconds: number
  heartbeatMissThreshold: number
  pauseOnOffline: boolean
  bufferAnswersOffline: boolean
  requireDeviceContinuityOnReconnect: boolean
  allowSafariWithAcknowledgement: boolean
export function getStudentIntegritySecurityPolicy(config?,) → StudentIntegritySecurityPolicy
export function getHeartbeatIntervalMs(config?,) → number
export function getHeartbeatLossTimeoutMs(config?,) → number
export function getHeartbeatEnforcementThresholds(config?,)
export function hasDeviceContinuityMismatch(previousHash, nextHash,) → boolean
```

### src/features/admin/contracts/index.ts
```
export interface AdminRootProps
  onNavigate: (mode: 'builder' | 'student' | 'adm
  exams: Exam[]
  examEntities: ExamEntity[]
  schedules: ExamSchedule[]
  defaults: ExamConfig
  setDefaults: (config: ExamConfig) => void
export interface ExamOperationCallbacks
  onEditExam: (id: string) => void
  onCreateExam: ( title: string, type: 'Academic' |
  onCloneExam?: (examId: string, newTitle: string) 
  onCreateFromTemplate?: (templateId: string, newTitle: stri
export interface VersionManagementCallbacks
  onGetVersions: (examId: string) => Promise<ExamVer
  onGetEvents: (examId: string) => Promise<ExamEve
  onRestoreVersion: (versionId: string) => Promise<void
  onRepublishVersion: (versionId: string) => Promise<void
  onCompareVersions: (versionIdA: string, versionIdB: st
export interface ScheduleManagementCallbacks
  onCreateSchedule: (schedule: ExamSchedule) => Promise
  onUpdateSchedule: (schedule: ExamSchedule) => Promise
  onDeleteSchedule: (scheduleId: string) => Promise<voi
  onStartScheduledSession: (scheduleId: string) => Promise<voi
export interface BulkOperationCallbacks
  onBulkPublish?: (examIds: string[]) => Promise<Bulk
```

### src/features/admin/hooks/useAdminRootController.ts
```
export function useAdminRootController() → AdminRootController
```

### src/routes/index.tsx
```
component RouteLoadingFallback
component NotFoundRoute
component AdminIndexRedirect
component StudentRegisterRedirect
hook useAuthSession
hook useParams
handler onRoute
```

### src/components/student/answerUndoRedoGuard.ts
```
export type UndoRedoKind
export function registerAnswerUndoRedoGuard(options)
```

### src/features/builder/services/previewRuntimeSessionService.ts
```
export interface PreviewRuntimeSession
  module: ModuleType
  scheduleId: string
  studentId: string
export function isPreviewRuntimeCohortName(cohortName) → boolean
export async function resolvePreviewRuntimeSession(options,) → Promise<PreviewRuntimeSession>
```

### src/services/studentMutationOutbox.ts
```
export interface StudentMutationOutbox
  flushNow: () => Promise<boolean>
export type DurablePersistTriggerSource
export type PendingMutationDurableWriteMode
export type PendingMutationFlushKind
export class PendingMutationDurabilityMirror
  reset() → void
  cancelDebouncedPersist() → void
  getPendingMutations() → StudentAttemptMutati
  finalizeAfterSuccessfulClear(attemptId) → void
  setPendingMutations(nextMutations, options?,) → Promise<boolean> | v
  flushAnswerDurableMirrorNow(source) → void
export function isAnswerMutationType(type) → boolean
export function getMutationCoalesceKey(mutation) → string | null
export function coalescePendingMutations(pending, nextMutation,) → StudentAttemptMutation[]
export function writeAnswerSyncCheckpoint(attemptId, mutationVersion, mutations,) → boolean
export function readAnswerSyncCheckpoint(attemptId) → StudentAttemptMutation[]
export function buildQueuedMutationUpdate(args, 'id' | 'scheduleId' | 'currentModule'>; pending)
```

### src/services/gradingService.ts
```
export interface GradingServiceResult
  success: boolean
  data?: T
  error?: string
export interface SessionQueueSummary
  totalSessions: number
  totalStudents: number
  pendingManualReviews: number
  inProgressReviews: number
  finalizedReviews: number
  overdueReviews: number
export class GradingService
  async buildGradingSessions() → Promise<GradingServi
  async getSessionQueue(filters?) → Promise<GradingServi
  async getSessionQueueSummary() → Promise<GradingServi
  async getSessionStudentSubmissions(sessionId, filters?) → Promise<GradingServi
```

### src/features/builder/routes/ExamPreviewRoute.tsx
```
component ExamPreviewRoute
component RuntimePreviewSurface
hook useParams
hook useNavigate
hook useSearchParams
hook useAuthSession
hook useBuilderRouteController
hook useState
hook useMemo
hook useEffect
hook useStudentSessionRouteData
export ExamPreviewRoute
handler onModuleChange
handler onChange
handler onExit
handler onRuntimeRefresh
```

### src/services/authService.ts
```
export interface AuthSessionUser
  id: string
  email: string
  displayName?: string | null | undefined
  role: AuthUserRole
  state: AuthUserState
export interface AuthSession
  user: AuthSessionUser
  csrfToken: string
  expiresAt: string
  idleTimeoutAt?: string | undefined
export interface StudentQueuedAdmission
  state: 'queued'
  ticketId: string
  scheduleId: string
  wcode: string
  position: number
  pollAfterMs: number
  queuedAt: string
export type AuthUserRole
export type AuthUserState
export type StudentEntryResult
class AuthService
  async getSession() → Promise<AuthSession 
  async login(payload) → Promise<AuthSession>
```

### src/features/auth/authSession.tsx
```
component AuthSessionProvider
hook useState
hook useRef
hook useEffect
hook useCallback
hook useMemo
hook useAuthSession
hook useContext
export AuthSessionProvider
handler onContext
handler onRef
```

### src/app/data/proctorQueries.ts
```
export type ProctorSessionSummaryPayload
export type ProctorSessionDetailPayload
export function buildDashboardDetailEndpoint(scheduleId)
export function fetchProctorSessionSummaries()
export function fetchProctorSessionDetail(scheduleId)
export function useProctorSessionSummaries(refetchInterval)
```

### src/types.ts
```
export interface SubAnswerTreeNode
  id: string
  label?: string
  acceptedAnswers?: string[]
  children?: SubAnswerTreeNode[]
  required?: boolean
export interface TFNGQuestion
  id: string
  statement: string
  correctAnswer: 'T' | 'F' | 'NG' | 'Y' | 'N' | 'NG'
export interface ClozeQuestion
  id: string
  prompt: string
  correctAnswer: string
  acceptedAnswers?: string[]
export interface MatchingQuestion
  id: string
  paragraphLabel: string
  correctHeading: string
export interface MapQuestion
  id: string
  label: string
  correctAnswer: string
  x: number
  y: number
```

### src/components/proctor/ProctorDashboard.tsx
```
component ProctorDashboard
props ProctorDashboardProps
hook useState
hook useMemo
hook useStudentFilters
hook useEffect
hook useCallback
hook useStudentAttempt
hook useCohort
hook useDisabledReason
export ProctorDashboard
handler onCount
handler onConfirm
handler onTabChange
handler onAction
handler onOpenAnswerHistory
handler onSaveNote
handler onToggleNote
handler onClick
handler onChange
handler onEnabled
handler onToggleSelection
```

### src/components/proctor/ExamGroupCard.tsx
```
component ExamGroupCard
props ExamGroupCardProps
export ExamGroupCard
handler onClick
```

### src/features/proctor/contracts/index.ts
```
export interface ProctorScheduleMetrics
  studentCount: number
  activeCount: number
  joinReadyCount?: number | undefined
  joinTotalCount?: number | undefined
  alertCount: number
  violationCount: number
  degradedLiveMode: boolean
export interface ProctorData
  schedules: ExamSchedule[]
  runtimeSnapshots: ExamSessionRuntime[]
  scheduleMetrics: Record<string, ProctorScheduleMetri
  sessions: StudentSession[]
  alerts: ProctorAlert[]
  auditLogs: SessionAuditLog[]
  notes: SessionNote[]
export interface ProctorOperationCallbacks
  onExit: () => void
  onUpdateSessions: (sessions: StudentSession[]) => voi
  onUpdateAlerts: (alerts: ProctorAlert[]) => void
  onUpdateNotes: (notes: SessionNote[]) => void
  onNavigate?: (mode: 'builder' | 'student' | 'adm
  onStartScheduledSession: (scheduleId: string) => Promise<voi
  onPauseCohort: (scheduleId: string) => Promise<voi
  onResumeCohort: (scheduleId: string) => Promise<voi
```

### src/components/student/StudentFooter.tsx
```
component StudentFooter
props StudentFooterProps
export StudentFooter
handler onClick
handler onId
```

### src/components/student/TableCompletionSlotCell.tsx
```
component TableCompletionSlotCell
export TableCompletionSlotCellProps
export TableCompletionSlotCell
handler onSlotCellProps
handler onChange
handler onId
```

### src/components/student/browserParityPolicy.ts
```
export interface SplitPaneBoundsPolicy
  minMaterialWidthPx: number
  minAnswerWidthPx: number
  dividerWidthPx: number
  dividerConsumesSpace: boolean
export function shouldLockViewportForExamSession(tabletMode) → boolean
export function getSplitPaneBoundsPolicy(isTabletMode, dividerWidthPx, dividerConsumesSpace,) → SplitPaneBoundsPolicy
```

### src/components/student/useSplitPaneResize.ts
```
export function useSplitPaneResize({ isTabletMode, materialPaneWidthProperty, answerPaneWidthProperty = '--question-pane-width', defaultLeftWidth = DEFAULT_LEFT_WIDTH, dividerMode = 'consumes-space', })
```

### src/utils/deviceFingerprinting.ts
```
export interface DeviceFingerprintComponents
  timezone: string | null
  language: string | null
  platform: string | null
  hardwareConcurrency: number | null
  deviceMemory: number | null
  screenResolution: string | null
  colorDepth: number | null
  canvasHash: string | null
export interface DeviceFingerprint
  components: DeviceFingerprintComponents
  hash: string
export async function collectDeviceFingerprintComponents() → Promise<DeviceFingerprintCompo
export async function hashFingerprint(components) → Promise<string>
export function clearCachedDeviceFingerprintForTesting()
export async function getDeviceFingerprint() → Promise<DeviceFingerprint>
```

### src/utils/htmlText.ts
```
export function htmlToPlainText(content) → string
export function htmlToPlainTextPreserveLineBreaks(content) → string
```

### src/components/proctor/AuditLogPanel.tsx
```
component AuditLogPanel
props AuditLogPanelProps
hook useState
hook useMemo
export AuditLogPanel
handler onContent
handler onClick
handler onChange
handler onIcon
```

### src/components/student/providers/StudentNetworkProvider.tsx
```
component StudentNetworkProvider
props StudentNetworkProviderProps
hook useStudentRuntime
hook useStudentAttempt
hook useMemo
hook useState
hook useRef
hook useEffect
hook useCallback
hook useStudentNetwork
hook useContext
export StudentNetworkProvider
```

### src/components/student/providers/blockingStateMachine.ts
```
export interface BlockingMachineState
  flags: BlockingFlags
  current: ManagedBlockingReason | null
export type ManagedBlockingReason
export function createBlockingMachineState(initial?) → BlockingMachineState
export function transitionBlockingMachine(previous, reason, active = true,) → BlockingMachineState
export function syncProctorBlockingMachine(previous, proctorStatus,) → BlockingMachineState
```

### src/components/student/StudentZoomableMedia.tsx
```
component StudentZoomableMedia
hook useMemo
hook useState
hook useRef
hook useEffect
export StudentZoomableMedia
handler onRef
handler onScale
handler onClick
handler onContextMenu
handler onError
handler onDragStart
handler onLoad
```

### src/features/admin/routes/GradingRoute.tsx
```
component GradingRouteContent
component GradingRoute
hook useNavigate
hook useAdminShell
export GradingRoute
handler onSelect
handler onSessionSelect
handler onId
handler onBack
handler onStudentSelect
handler onAnswerHistorySelect
```

### src/components/admin/StudentReviewWorkspace.tsx
```
component StudentReviewWorkspace
props StudentReviewWorkspaceProps
hook useState
hook useRef
hook useEffect
hook useCallback
hook useMemo
export StudentReviewWorkspaceProps
export StudentReviewWorkspace
handler onLoadSeq
handler onId
handler onAdd
handler onDelete
handler onSubmission
handler onAnswers
handler onCount
handler onTaskBySlot
handler onBands
handler onClick
handler onSub
handler onChange
handler onAnnotationAdd
handler onAnnotationUpdate
handler onAnnotationDelete
handler onDrawingAdd
```

### src/services/gradingRepository.ts
```
export interface IGradingRepository
  sectionSubmissionId: string, ): Promise<WritingTaskSubmi
  getAllSessions()
  getSessionById(id)
  getSessionsBySchedule(scheduleId)
  saveSession(session)
  deleteSession(id)
  getAllSubmissions()
  getSubmissionById(id)
class BackendGradingRepository
export function rememberReviewDraftRevision(id, revision) → void
export function getReviewDraftRevision(id) → number | undefined
```

### src/components/student/providers/StudentKeyboardProvider.tsx
```
component KeyboardProvider
props KeyboardProviderProps
hook useStudentRuntime
hook useStudentAttempt
hook useStudentUI
hook useProctoring
hook useRef
hook useEffect
hook useKeyboard
export KeyboardProvider
handler onId
handler onPolicy
handler onIndex
```

### src/components/student/useDeferredSelectionHighlight.ts
```
export function useDeferredSelectionHighlight({ enabled, containerRef, applySelection, applySelectionFromSnapshot, touchApplyDelayMs = DEFAULT_TOUCH_APPLY_DELAY_MS, touchApplyRetryIntervalMs = DEFAULT_TOUCH_APPLY_RETRY_INTERVAL_MS, touchApplyMaxRetries = DEFAULT_TOUCH_APPLY_MAX_RETRIES, })
```

### src/utils/boldMarkdown.ts
```
export type BoldSegment
export type RichSegment
export type ToggleBoldResult
export function parseBoldMarkdown(text) → BoldSegment[]
export function stripBoldMarkdown(text) → string
export function parseRichMarkdown(text) → RichSegment[]
export function toggleBoldMarkers(value, selectionStart, selectionEnd,) → ToggleBoldResult
export function isBoldHotkey(event) → boolean
```

### src/components/student/providers/StudentProctoringProvider.tsx
```
component ProctoringProvider
props ProctoringProviderProps
hook useStudentRuntime
hook useStudentAttempt
hook useRef
hook useCallback
hook useExam
hook useEffect
hook useProctoring
hook useContext
export ProctoringProvider
handler onTextTypes
handler onDeniedAtRef
handler onCountsRef
handler onAt
handler onCooldownMs
handler onCount
```

### src/components/student/providers/StudentRuntimeProvider.tsx
```
component StudentRuntimeProvider
props StudentRuntimeProviderProps
hook useMemo
hook useReducer
hook useState
hook useRef
hook useEffect
hook useCallback
hook useStudentRuntime
hook useContext
export ExamPhase
export StudentAnswer
export BlockingReason
export StudentRuntimeProvider
handler onKey
handler onId
handler onRuntimeSectionDurationMinutes
handler onRuntimeSectionDurationSeconds
handler onExtensionMinutes
handler onIncreased
handler onMonotonicJump
handler onBlocking
```

### src/services/studentAttemptRepository.ts
```
export interface StudentLocalCachePolicy
  submittedReceiptTtlMs: number
  staleUnfinishedAttemptTtlMs: number
  maxPendingMutationsPerAttempt: number
  maxPendingMutationBytesPerAttempt: number
export interface StudentAttemptReceipt
  attemptId: string
  scheduleId: string
  submittedAt: string
  submissionId: string
  lastServerAcceptedSeq: number
  compactedAt: string
export interface StudentAttemptCachePruneResult
  compactedAttempts: number
  purgedAttempts: number
  purgedReceipts: number
export interface StudentAttemptLocalCacheStats
  approximateBytes: number
  attemptCount: number
  receiptCount: number
  pendingMutationCount: number
  heartbeatEventCount: number
export interface IStudentAttemptRepository
  getAttemptByScheduleId(scheduleId, studentKey)
  getAllAttempts()
```

### src/services/studentSessionTransport.ts
```
export interface StudentSessionTransport
  readonly paths: { session: (scheduleId: string, can
  staticSession: (scheduleId: string, candidateId: s
  liveSession: (scheduleId: string, candidateId: s
  credentialRefresh: ( scheduleId: string, candidateId: 
  precheck: (scheduleId: string) => string
  bootstrap: (scheduleId: string) => string
  mutationsBatch: (scheduleId: string) => string
  heartbeat: (scheduleId: string, responseMode?:
```

### src/components/student/resolveObjectiveAnswerUpdate.ts
```
export function resolveObjectiveAnswerUpdate(currentValue, answer, meta?,) → StudentAnswerValue
```

### src/components/student/providers/StudentUIProvider.tsx
```
component StudentUIProvider
props UIProviderProps
hook useState
hook useCallback
hook useStudentUI
hook useContext
export StudentUIProvider
handler onReason
```

### src/components/student/answerMutationDebug.ts
```
export function isAnswerMutationDebugEnabled() → boolean
export function emitAnswerMutationDebugLog(stage, context, unknown>,) → void
```

### src/components/student/QuestionRenderer.tsx
```
component QuestionRenderer
props QuestionRendererProps
export QuestionRenderer
handler onId
handler onChange
```

### src/components/student/providers/StudentAttemptProvider.tsx
```
component StudentAttemptProvider
props StudentAttemptProviderProps
hook useStudentRuntime
hook useState
hook useRef
hook useCallback
hook useEffect
hook useMemo
hook useStudentAttempt
hook useContext
hook useOptionalStudentAttempt
export StudentAttemptProvider
handler onSignal
handler onRef
handler onPersistenceError
handler onId
handler onType
```

### src/components/student/StudentWriting.tsx
```
component StudentWriting
props StudentWritingProps
hook useOptionalStudentAttempt
hook useState
hook useRef
hook useSplitPaneResize
hook useCallback
hook useEffect
export StudentWriting
handler onId
handler onTaskId
handler onContextMenu
handler onDragStart
handler onDrop
handler onMouseDown
handler onTouchStart
handler onChange
handler onPaste
handler onCopy
handler onCut
handler onClick
```

### src/components/student/StudentApp.tsx
```
component StudentApp
props StudentAppProps
hook useStudentRuntime
hook useStudentAttempt
hook useStudentUI
hook useStudentTabletMode
hook useZoomScrollAnchoring
hook useState
hook useMemo
hook useCallback
hook useRef
hook useEffect
export StudentApp
handler onReason
handler onRef
handler onVerified
handler onViolation
handler onWarning
handler onLocked
handler onKey
handler onPolicy
handler onComplete
handler onExit
handler onStart
handler onClick
```

### src/components/student/StudentExamPreview.tsx
```
component PreviewModal
component StudentExamPreviewInner
component StudentExamPreview
props StudentExamPreviewProps
hook useNavigate
hook useSearchParams
hook useStudentUI
hook useStudentTabletMode
hook useZoomScrollAnchoring
hook useMemo
hook useState
export StudentExamPreview
handler onClick
handler onChange
handler onExit
handler onClearHighlights
handler onHighlightModeToggle
handler onHighlightColorChange
handler onOpenNavigator
handler onAnswerChange
handler onId
handler onNavigate
handler onToggleFlag
handler onWritingChange
handler onSubmit
```

### src/app/data/answerHistoryQueries.ts
```
export function useAnswerHistoryOverviewBySubmission(submissionId)
export function useAnswerHistoryOverviewByAttempt(attemptId)
export function useAnswerHistoryTargetDetail(args)
export function useAnswerHistoryTargetDetailByAttempt(args)
```

### src/app/data/queryClient.ts
```
export function createQueryClient() → QueryClient
export function invalidateQueries(key) → void
export function invalidateAllQueries() → void
export function cancelQueries(key) → void
export function resetQueries(key) → void
export function setQueryData(key, data) → void
export function getQueryData(key) → T | undefined
```

### src/components/QuestionBuilderPane.tsx
```
component QuestionBuilderPane
hook useState
hook useEffect
export QuestionBuilderPane
handler onFromBank
handler onFromDetail
handler onToBlock
handler onClick
handler onChange
handler onSelectQuestion
handler onAddToExam
```

### src/components/admin/GradingExportButtons.tsx
```
component GradingExportButtons
props GradingExportButtonsProps
export GradingExportButtons
handler onClick
```

### src/components/admin/GradingSessionDetail.tsx
```
component GradingSessionDetail
props GradingSessionDetailProps
hook useState
hook useRef
hook useEffect
export GradingSessionDetail
handler onBadge
handler onContext
handler onClick
handler onChange
```

### src/components/admin/gradingReviewUtils.ts
```
export interface CsvColumn
  key: string
  label: string
export interface ExportSessionContext
  sessionId: string
  examTitle: string
export interface ObjectiveTracebackItem
  numberLabel: string
  questionId: string
  prompt: string
  studentAnswer: string
  correctAnswer: string
  correctness: boolean | null
  awardedScore: number | null
  maxScore: number | null
export interface ObjectiveTracebackGroup
  groupId: string
  groupLabel: string
  items: ObjectiveTracebackItem[]
export interface ObjectiveExportRowInput
  session: ExportSessionContext
  submission: StudentSubmission
  sectionSubmission: SectionSubmission
  examState: ExamState | null
  moduleType: 'reading' | 'listening'
```

### src/components/answer-history/AnswerHistoryPage.tsx
```
component AnswerHistoryPage
hook useAnswerHistoryOverviewBySubmission
hook useAnswerHistoryOverviewByAttempt
hook useState
hook useMemo
hook useEffect
hook useAnswerHistoryTargetDetail
hook useAnswerHistoryTargetDetailByAttempt
export AnswerHistoryPage
handler onId
handler onQuery
handler onBadgesByTarget
handler onBadge
handler onClick
handler onChange
```

### src/components/student/FormattedText.tsx
```
component FormattedText
hook useMemo
hook useRef
hook usePersistedStudentHighlightHtml
hook useState
hook useCallback
hook useDeferredSelectionHighlight
hook useEffect
export FormattedText
handler onIntentAtRef
handler onFromSnapshot
handler onIntentAt
handler onClick
handler onMouseUp
handler onTouchStart
handler onTouchEnd
handler onKeyUp
```

### src/components/student/RichTextHighlighter.tsx
```
component RichTextHighlighter
props RichTextHighlighterProps
hook useRef
hook useState
hook useMemo
hook usePersistedStudentHighlightHtml
hook useCallback
hook useDeferredSelectionHighlight
hook useEffect
export RichTextHighlighter
handler onIntentAtRef
handler onFromSnapshot
handler onIntentAt
handler onClick
handler onMouseUp
handler onTouchStart
handler onTouchEnd
handler onKeyUp
```

### src/components/student/StudentListening.tsx
```
component StudentListening
props StudentListeningProps
hook useMemo
hook useState
hook useRef
hook useSplitPaneResize
hook useEffect
export StudentListening
handler onContainerRef
handler onGroup
handler onTimeUpdate
handler onLoadedMetadata
handler onClick
handler onChange
handler onMouseDown
handler onTouchStart
handler onId
handler onToggleFlag
handler onAnswerChange
handler onEntries
```

### src/components/student/StudentReading.tsx
```
component StudentReading
props StudentReadingProps
hook useMemo
hook useRef
hook useSplitPaneResize
hook useEffect
export StudentReading
handler onContainerRef
handler onMouseDown
handler onTouchStart
handler onId
handler onToggleFlag
handler onAnswerChange
handler onEntries
handler onClick
handler onChange
```

### src/components/student/SubAnswerTreeQuestionList.tsx
```
component SubAnswerTreeQuestionList
props SubAnswerTreeQuestionListProps
export SubAnswerTreeQuestionList
handler onChange
```

### src/components/student/highlightSelection.ts
```
export interface HighlightSelectionSnapshot
  startNodePath: number[]
  startOffset: number
  endNodePath: number[]
  endOffset: number
  selectedText: string
  signature: string
export interface HighlightApplyResult
  html: string | null
  reason: HighlightPolicyReason | null
export type HighlightPolicyReason
export function escapeHtml(text) → string
export function applySelectionHighlight(container, selection, highlightClassName = 'rounded-sm bg-yellow-200/80 text-gray-900',) → string | null
export function applySelectionHighlightWithPolicy(container, selection, highlightClassName = 'rounded-sm bg-yellow-200/80 text-gray-900',) → HighlightApplyResult
export function applyHighlightFromSnapshot(container, snapshot, highlightClassName = 'rounded-sm bg-yellow-200/80 text-gray-900',) → string | null
export function applyHighlightFromSnapshotWithPolicy(container, snapshot, highlightClassName = 'rounded-sm bg-yellow-200/80 text-gray-900',) → HighlightApplyResult
export function removeHighlightAtIndex(container, highlightIndex) → string | null
export function createHighlightSelectionSnapshot(container, selection,) → HighlightSelectionSnapshot | n
```

### src/components/student/normalizeReadingPassageText.ts
```
export function normalizeReadingPlainTextForDisplay(content) → string
export function normalizeReadingContentForHighlightText(content) → string
export function normalizeReadingContentForHighlightedFormattedText(content) → string
```

### src/features/answer-history/contracts/index.ts
```
export interface AnswerHistorySignal
  signalType: string
  severity: 'low' | 'medium' | 'high' | 'critic
  message: string
  evidence: Record<string, unknown>
export interface AnswerHistoryQuestionSummary
  targetId: string
  label: string
  module: string
  targetType: AnswerHistoryTargetType
  revisionCount: number
  answered: boolean
  finalValue: unknown
export interface AnswerHistorySectionStat
  module: string
  totalRevisions: number
  editedTargets: number
export interface AnswerHistoryOverview
  submissionId?: string | null | undefined
  attemptId: string
  scheduleId: string
  examId: string
  examTitle: string
  candidateId: string
  candidateName: string
```

### src/features/proctor/hooks/useProctorRouteController.ts
```
export interface ProctorRouteController
  alerts: ProctorAlert[]
  auditLogs: SessionAuditLog[]
  error: string | null
  isLoading: boolean
  notes: SessionNote[]
  runtimeSnapshots: ExamSessionRuntime[]
  schedules: ExamSchedule[]
  scheduleMetrics: Record<string, ProctorScheduleMetri
export function useProctorRouteController() → ProctorRouteController
```

### src/features/student/hooks/useStudentSessionRouteData.ts
```
export interface StudentAnswerInvariantRollout
  enabled: boolean
  killSwitch: boolean
  cohort: string | null
  configFingerprint: string | null
  source: 'default' | 'runtime'
export function useStudentSessionRouteData(scheduleId?, studentId?,) → StudentSessionRouteData
```

### src/features/student/routes/StudentEntryRoute.tsx
```
component StudentEntryRoute
hook useParams
hook useNavigate
hook useSearchParams
hook useAuthSession
hook useMemo
hook useState
hook useEffect
export StudentEntryRoute
handler onSubmit
handler onChange
```

### src/features/student/routes/StudentSessionRoute.tsx
```
component StudentSessionRoute
hook useParams
hook useNavigate
hook useAuthSession
hook useStudentSessionRouteData
export StudentSessionRoute
handler onExpired
handler onLabel
handler onAction
handler onExit
handler onRuntimeRefresh
```

### src/services/answerHistoryService.ts
```
export async function fetchAnswerHistoryOverviewBySubmission(submissionId)
export async function fetchAnswerHistoryOverviewByAttempt(attemptId)
export async function fetchAnswerHistoryTargetDetail(args)
export async function fetchAnswerHistoryTargetDetailByAttempt(args)
export async function fetchAnswerHistoryExport(args)
```

### src/services/examAdapterService.ts
```
export interface StudentQuestionDescriptor
  id: string
  blockId: string
  groupId: string
  groupLabel: string
  isMulti: boolean
  correctCount: number
  answerKey: string
  answerIndex?: number
export function getEnabledModules(config) → ModuleType[]
export async function getExamStateFromEntity(entity, repository, 'getVersionById'>,) → Promise<ExamState>
export async function adaptExamEntityToLegacyExam(entity, repository, 'getVersionById'>,) → Promise<Exam>
export async function adaptExamEntitiesToLegacyExams(entities, repository, 'getVersionById'>,) → Promise<Exam[]>
export function createInitialExamState(title, type, preset, baseConfig?,) → ExamState
export function hydrateExamState(state) → ExamState
export function getStudentQuestionsForModule(state, moduleType,) → StudentQuestionDescriptor[]
export function getFirstQuestionIdForModule(state, moduleType,) → string | null
export function countAnsweredQuestions(questions, answers, StudentAnswerValue | undefined>,) → number
export function countQuestionSlots(questions) → number
export function getQuestionStartNumber(questions, questionId,) → number | null
export function getQuestionNumberLabel(questions, questionId,) → string
export function getQuestionAnswer(question, answers, StudentAnswerValue | undefined>,) → StudentAnswerValue | undefined
export function getAnsweredSlotCount(question, answers, StudentAnswerValue | undefined>,) → number
export function isQuestionAnswered(question, answers, StudentAnswerValue | undefined>,) → boolean
export function isQuestionFullyAnswered(question, answers, StudentAnswerValue | undefined>,) → boolean
```

### src/utils/acceptedAnswers.ts
```
export interface AcceptsAlternativeAnswers
  correctAnswer: string
  acceptedAnswers?: string[] | undefined
export function normalizeAnswerForMatching(value) → string
export function sanitizeAcceptedAnswers(acceptedAnswers) → string[]
export function resolveAcceptedAnswers(entry) → string[]
export function buildAcceptedAnswerFields(acceptedAnswers)
export function syncAcceptedAnswers(entry) → T
```

### src/utils/subAnswerTree.ts
```
export interface SubAnswerTreeValidationIssue
  field: string
  message: string
export interface SubAnswerTreeRootDescriptor
  rootId: string
  rootNodeId: string
  rootLabel: string
  rootNumber: number
  numberLabel: string
  leafQuestionIds: string[]
  requiredLeafQuestionIds: string[]
export interface SubAnswerTreeLeafDescriptor
  id: string
  rootId: string
  rootNodeId: string
  rootLabel: string
  rootNumber: number
  numberLabel: string
  nodeId: string
  prompt: string
export interface FlattenSubAnswerTreeResult
  roots: SubAnswerTreeRootDescriptor[]
  leaves: SubAnswerTreeLeafDescriptor[]
  nextRootNumber: number
export function normalizeSubAnswerTree(answerTree,) → SubAnswerTreeNode[]
```

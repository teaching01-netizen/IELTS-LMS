# Phase 06 — Final Integration + Regression (Verification Only)

## Objective

Prove that Phases 01–05 together satisfy every Apple-design audit finding with zero regressions: full SAT staff suite green, type/contrast/density/dark/copy contracts verified by grep + computed (not eyeballed) checks, scope contained to `src/products/sat/**` + `.sat-product` / `--sat-staff-*` CSS in `src/index.css`, and a signed-off verification log. This phase makes NO source edits; any failure is repaired by routing a precise defect ticket back to the owning phase (01–05) and re-running the gate.

## Dependencies

- **Phase 01 (Type floor + contrast + density) — REQUIRED, frozen first.** Delivers: 6-step type scale, zero `text-[8px]`/`text-[9px]`/`text-[10px]` in shipped TSX (floor `11px`), neutral dot `#9b9a97` → `#6e6e73`, chevron `slate-300` → `slate-400` (remapped to tertiary), clear-button / chip target sizes, `satOutcomeTone` single table. Phase 06 blocks until Phase 01 contracts (below) are frozen; Waves 2–3 must not start verification.
- **Phase 02 (Status language + search + writing) — REQUIRED.** Delivers: `SatResultsRoute.tsx:118` 8px duplicate outcome line DELETED (single pill per row), search placeholders → `"Search name, ID, exam, cohort"`, Title Case buttons, primary `"Create SAT"` (replacing `"New SAT"` drift), jargon `"Module identifiers as delivered."` (`SatResultDetailRoute.tsx:127`) DELETED, roster/results rows ≤2 meta lines.
- **Phase 03 (Dirty-guard dialogs + interaction safety) — REQUIRED.** Delivers: confirm-on-dirty via `SatConfirmDialog` on both creation surfaces — `SatExamLibraryRoute` New SAT dialog and `SatSessionsRoute` New Session sheet; pristine-always-closes + Escape/backdrop preserved.
- **Phase 04 (Session Room ops hierarchy) — REQUIRED.** Delivers: `SatSessionRoomRoute` roster/detail compression (roster `InfoRow` `text-[8px]` → floor, detail `dt` `text-[9px]` → floor, ≤2-line rows), attention filter + 36px stage clock intact.
- **Phase 05 (Dark appearance + system-preference answers) — REQUIRED.** Delivers: `.sat-product` dark semantic variants in `src/index.css` only, system-following via `prefers-color-scheme`, NO toggle, dark+contrast+transparency matrix preserved. Phase 06 must run alone after 05 (same token block).
- **Overall plan `plans-sat-remediation/overall-plan.md` §§5–6.** The 8 exit criteria and frozen contracts are the sign-off checklist; do not reinterpret them.
- **Tooling baseline.** `vitest`, `tsc --noEmit`, `eslint`, `vite build`, `e2e:sat-a11y` (`playwright.sat-a11y.config.ts`) all runnable from repo root `/Users/rd-cream/Downloads/remix_-ielts-proctoring-system`.

## Affected / new files (with line refs where known)

> Phase 06 OWNS NO SOURCE FILES. Every path below is READ-ONLY (inspect / grep / test). Any edit is routed to the owning phase listed in parens.

- **This plan (only file Phase 06 writes):** `plans-sat-remediation/phase-06-final-integration-regression.md`.
- **Verification log (produced, not a source edit):** paste the full command outputs into the plan's Verification section or a CI comment — do NOT create `src/**` log files. If a durable artifact is required, write `plans-sat-remediation/phase-06-verification-log.md` (plans dir only, never `src/`).
- **Read-only inspection set (pre-remediation line refs from audit; post-phase line numbers may shift — grep by pattern, not by number):**
  - `src/index.css` — `.sat-product` block (~lines 1150–2030: tokens `--sat-staff-*`, `.sat-search-clear` ~1735, `.sat-route-fade` + `prefers-reduced-motion` guard, slate remaps ~1704–1710, dark `prefers-color-scheme` block added by Phase 05) (owners: 01 + 05).
  - `src/products/sat/ui/SatPage.tsx` — eyebrow `text-[10px]` (:33), pill `text-[10px]` (:199), stat label/hint `text-[10px]` (:283, :285), release tag `text-[10px]` (:391), `SatEyebrow` `text-[10px]` (:404), `satOutcomeTone` (:166) (owner: 01).
  - `src/products/sat/ui/ConfirmDialog.tsx` — eyebrow `text-[10px]` (:167, :192), dirty-guard helper added by Phase 03 (owner: 03).
  - `src/products/sat/SatRoot.tsx` — role label `text-[9px]` (:123), badge `text-[9px]` (:142), bottom-tab `text-[9px]` (:177), avatar `text-[10px]` (:120), workspace `text-[10px]` (:73) (owner: 01; bottom-tab deferrable to 04 per overall plan).
  - `src/products/sat/routes/SatResultsRoute.tsx` — DELETE `text-[8px]` outcome line (:118: `<span className="mt-0.5 block text-[8px] font-semibold uppercase tracking-[0.1em] text-slate-400">{outcomeLabel(result.outcomeStatus)} · Practice · {result.releaseStatus}</span>`), 2-line rows (:113–114 `text-[10px]` → floor) (owners: 01 type, 02 delete).
  - `src/products/sat/routes/SatResultDetailRoute.tsx` — outcome `text-[9px]` (:96), route `text-[9px]` (:110), Raw/Practice `text-[8px]` (:111–112), module `text-[10px]` (:120), DELETE jargon (:127 `"Module identifiers as delivered."`), footnote `text-[10px]` (:175), header `text-[10px]` (:91) (owners: 01 + 02).
  - `src/products/sat/routes/SatSessionRoomRoute.tsx` — header cohort `text-[9px]` (:162), Overrun/Reconnecting/attention `text-[9px]` (:163–165), students meta `text-[9px]` (:176), filter chips `text-[10px]` + `min-h-8` (:179–180), eyebrows `text-[9px]` (:211, :227), stage `text-[25px]`/:212 + clock `text-[36px]`/:212 (KEEP 36 hero), `text-[8px]` roster section/status (:273), detail `dt` `text-[9px]` (:285), violation `text-[9px]` (:286), `InfoRow` `text-[8px]` (:290 → floor), primary button `text-[10px]` (:253) (owners: 01 + 04).
  - `src/products/sat/routes/SatSessionsRoute.tsx` — cohort/time `text-[10px]` (:158–159), form labels `text-[11px]` (KEEP, :235), error/hint `text-[10px]` (:235–236 → floor `11px`) (owners: 01 + 03).
  - `src/products/sat/routes/SatExamLibraryRoute.tsx` — archive chip `text-[10px]` (:137), row meta `text-[10px]` (:148), helper `text-[10px]` (:174) (owners: 01 + 02/03).
- **Explicitly OUT OF SCOPE (any diff here FAILS the gate):** `src/features/student-delivery/**`, any `.sat-ui` selector, IELTS surfaces, backend, auth, stores/network.

## Contracts / interfaces (frozen inputs from Phase 01 if relevant; what you must not break)

Freeze source: overall plan §5. Phase 06 enforces; it never redesigns.

1. **Type scale (6 steps, floor 11px — nothing below 11px ships):** H1 `30px / tracking -0.045` · hero `52px / -0.045 / tabular-nums` (ResultDetail total ONLY) + `36px / -0.045 / tabular-nums` stage clock (SessionRoom ONLY) · H2 `16–17px / -0.025` · title/body `13–14px / -0.012` · control/hint `12px semibold` · floor `11px medium` · eyebrow `11px caps / tracking 0.12–0.14em`. Mechanical map enforced: `text-[8px]` → DELETE (Results :118, jargon :127) or → `text-[11px]`; `text-[9px]` → `text-[11px]` (eyebrows keep `uppercase tracking-[0.12em]`); `text-[10px]` body/meta → `text-[11px]` or `text-[12px]` per owner plan. Do not reintroduce `text-[8px]`, `text-[9px]`, `text-[10px]` in `src/products/sat/**`.
2. **Status tones (single table, no forks):** `satOutcomeTone` in `src/products/sat/ui/SatPage.tsx:166` is the ONLY outcome mapping (`scored→ready`, `pending→pending`, `invalidated_proctor/invalidated_timeout→invalidated`, default `→neutral`); `sessionStatusTone` (`SatSessionsRoute.tsx:45`) and `roomStatusTone` (`SatSessionRoomRoute.tsx:36`) alias the same pill classes. Pill markup keeps dot + label (`SatStatusPill` renders `span[aria-hidden="true"].rounded-full` + text; `live` pulse via `.sat-live-dot` only). Color never carries state alone.
3. **Contrast floor (computed):** all text ≥4.5:1, dots ≥3:1. Fixed values: tertiary `#6e6e73` 5.07 PASS, secondary `#515154` 7.91 PASS, info-text `#0067c9` 5.55 PASS, success-text `#067647` 5.69 PASS, warning-text `#92400e` 7.09 PASS, danger `#b42318` 6.57 PASS, white-on-accent `#ffffff` on `#0071e3` 4.70 PASS; success dot 3.77 / warning dot 3.19 (3:1 ok); neutral dot `#9b9a97` 2.81 FAIL → `#6e6e73` 5.07. Chevron `slate-300` → `slate-400` remapped to `var(--sat-tertiary-label)` (`src/index.css` ~1704). Do not add new hex literals in TSX — tokens only.
4. **Targets:** primaries/rows keep 44px (`min-h-11`/`min-h-12`/`min-w-[72px]`); search-clear ≥28px (existing contract `satContractsCss.test.ts` enforces `min-block-size:32px !important` + `min-inline-size:32px !important` + `top:50% !important` + `transform:translateY(-50%)` — the STRICTER 32px wins, do not regress to 28); filter chips `min-h-7` (28px) minimum — audit `"min-h-8→28px"` means old `min-h-8` (32px) → new `min-h-7` (28px), never below 28px.
5. **Motion / appearance / behavior freeze:** capped entrance stagger (first 6, 150–200ms), instant under `prefers-reduced-motion`; glass collapses under `prefers-reduced-transparency`; contrast + `forced-colors` mappings preserved; `.sat-route-fade` keeps `animation:none` + reduced-motion `opacity:1 !important` gate; dark appearance system-following (`prefers-color-scheme`), NO toggle; titles <15 chars; one prominent action/view; Cancel-focused alerts; skeleton-XOR loading; empty-state next actions; radiogroup segmented + listbox roster keyboard; 4 system-preference answers. Filtering, polling (4000ms sessions), proctor actions, confirm flows, routing, polling intervals IDENTICAL except mandated type/delete/dirty-guard/dark changes. No new stores/network/auth/persistence.
6. **Copy freeze (Phase 02):** search placeholder exact string `"Search name, ID, exam, cohort"` on all 3 list searches + room; Title Case buttons; primary exact `"Create SAT"` (exam library) / `"Create"` dialog confirm; dirty-guard copy via `SatConfirmDialog`; jargon deleted. Do not reintroduce `"New SAT"` primary, sentence-case buttons, or `"Module identifiers as delivered."`.

## Step-by-step implementation (ordered, each step names exact file + exact change: old class/token → new class/token, old string → new string)

> All steps are CHECKS. Run in order; stop and route on first red gate (see repair routing in Step 10). Record every command + exit code + hash in the verification log.

1. **Step 1 — Establish clean baseline (`git status` + `git diff --stat`).** In repo root, run `git status --porcelain` (expect: only `plans-sat-remediation/phase-06-*.md` untracked/modified; no staged `src/` surprises) and `git diff --stat` / `git diff --name-only`. PASS = every changed source path matches `src/products/sat/**` or `src/index.css`; any path matching `src/features/student-delivery/**`, `*.sat-ui*`, or containing `.sat-ui` FAILS → route to offending phase immediately. Capture base commit SHA.
2. **Step 2 — Sub-minimum type gate (`src/products/sat/**`: `text-[8px]`/`text-[9px]` → gone; `text-[10px]` → gone).** Run the two greps in Verification §A. PASS = zero matches for `text-\[8px\]` and `text-\[9px\]`; `text-\[10px\]` must also be zero unless the owning phase documented a named exception with line ref (none expected — floor is 11px). Spot-check the audit hot spots: `SatResultsRoute.tsx:118` deleted (old `<span className="mt-0.5 block text-[8px] ...">{outcomeLabel...} · Practice · ...</span>` → absent), `SatResultDetailRoute.tsx:111-112` old `text-[8px]` Raw/Practice → new `text-[11px] font-semibold uppercase tracking-[0.1em]`, `SatSessionRoomRoute.tsx:290` old `InfoRow dt text-[8px]` → new `text-[11px]`, `SatRoot.tsx:123,142,177` old `text-[9px]` → new `text-[11px]`. Any hit → route to Phase 01 (mechanical swap) or Phase 02 (:118 delete).
3. **Step 3 — Forbidden-string / copy gate (`src/products/sat/**`).** Grep exact strings in Verification §B. PASS = zero matches for `"Module identifiers as delivered."`; zero `"New SAT"` as a primary button label (only acceptable inside prose/history — primary must read `"Create SAT"`); placeholder `"Search name, ID, exam, cohort"` present on exam/sessions/results searches; no sentence-case button regressions (`grep` for `>Create session<`, `>Add filter<` style offenders per Phase 02 list). Any hit → route to Phase 02.
4. **Step 4 — Token / contrast recompute (`src/index.css` `.sat-product` block).** Run the Node contrast script in Verification §C against the exact pairs in Contracts §3. PASS = recomputed ratios match the table within ±0.05 (tertiary `#6e6e73` on `#ffffff` = 5.07, secondary `#515154` = 7.91, `#0067c9` = 5.55, `#067647` = 5.69, `#92400e` = 7.09, `#b42318` = 6.57, `#ffffff` on `#0071e3` = 4.70) AND `grep` confirms `--sat-staff-*` dark `@media (prefers-color-scheme: dark)` block exists AND no `#9b9a97` remains in `src/products/sat/**` or `.sat-product` rules AND `.sat-search-clear` keeps `min-block-size:32px !important` / `min-inline-size:32px !important`. Any drift → route to Phase 01 (light tokens) or Phase 05 (dark block).
5. **Step 5 — Unit + contract suite (`vitest run src/products/sat`).** Run Verification §D exactly. PASS = all files in Tests § pass, including `ui/__tests__/satContractsCss.test.ts` (F-A6 32px clear target, F-A12 `.sat-route-fade` reduced-motion gate) and `ui/__tests__/SatPage.test.tsx` (`satOutcomeTone`, `SatStatusPill` dot+label, live-only pulse, search-clear `onChange('')`). No `--update`, no snapshot blind-accept. Any failure → route to owning phase per failing file (SatPage→01, Dialogs→03, room→04, dark→05).
6. **Step 6 — Typecheck (`tsc --noEmit`).** Run Verification §E. PASS = exit 0, zero errors. Any error in `src/products/sat/**` → route to the phase that touched that file (type-only swaps should be type-neutral; dirty-guard helper signature changes → Phase 03).
7. **Step 7 — Lint (`eslint`).** Run Verification §F on `src/products/sat` then full `eslint .` if scoped pass is green. PASS = zero errors/warnings on SAT paths (especially `jsx-a11y`: alertdialog naming, `aria-pressed` chips, listbox `role="option"` + `aria-selected`, focus-visible rings). Any new warning vs. baseline → route to owning phase.
8. **Step 8 — Production build (`vite build`).** Run Verification §G. PASS = exit 0, no new chunks over baseline ±5%, no CSS `@media` or token typos surfacing as build warnings. Any failure → route to Phase 01/05 (CSS) or the phase owning the breaking import.
9. **Step 9 — Accessibility E2E (`e2e:sat-a11y` + staff workspace spec).** Run Verification §H: `npm run e2e:sat-a11y` (config `playwright.sat-a11y.config.ts`: chromium + webkit + touch-chromium) and, if available, `e2e/sat-product-workspace.spec.ts`. Note: current `testMatch` is `sat-student-accessibility.spec.ts` (student surface) — Phase 06 must document which spec actually exercised the STAFF workspace and treat a student-only run as INSUFFICIENT (require staff-workspace run or documented equivalent per overall plan §6.8). PASS = zero axe/playwright-a11y violations on Exams/Sessions/Room/Results/Detail incl. dark + `prefers-reduced-motion` + `forced-colors` projects if configured. Any violation → route to owning phase (type/contrast→01, dialog focus→03, roster keyboard→04, dark→05).
10. **Step 10 — Scope-leak + behavior diff review (`git diff`).** Run Verification §I. PASS = (a) `git diff --name-only` ⊆ {`src/products/sat/**`, `src/index.css`, `plans-sat-remediation/**`} (b) `git diff | grep -c "\.sat-ui"` = 0 (c) human review of each hunk confirms only: class swaps (`text-[8px]/[9px]/[10px]` → `text-[11px]/[12px]`, `slate-300` → `slate-400`, `min-h-8` → `min-h-7` on chips), token swaps (`#9b9a97` → `#6e6e73`), string swaps (placeholder, Title Case, `"Create SAT"`, jargon/8px-line deletes), dirty-guard wiring, dark `@media` block. Any logic change (polling interval ≠4000, new fetch/store, route change, timing/scoring) → FAIL, route to owner and re-review.
11. **Step 11 — Repair routing + re-verify loop.** For each red gate, file a defect note with: failing step + command + exit code + file:line + old→new expectation + owning phase (01 type/tokens/tones, 02 copy/rows, 03 dirty-guard, 04 room hierarchy, 05 dark). Do NOT fix source in Phase 06. After the owner lands a fix, re-run Steps 1–10 from the top (full loop, not just the failed step) and append both runs to the log.
12. **Step 12 — Sign-off.** Fill Definition of Done checklist with commit SHA, command outputs, and contrast table. Only when every box is measurably green, declare Phase 06 complete.

## Key code / pseudocode (dirty-guard logic, token blocks, row markup — only what your phase needs)

> Reference snippets for VERIFYING, not implementing. Compare source against these shapes; do not edit source to match by hand — route mismatches.

- **A. Zero-subminimum gate (expected: no output).** See Verification §A. Post-phase, these audit lines must read as: `SatSessionRoomRoute` `InfoRow` → `<dt className="text-[11px] font-semibold uppercase tracking-[0.1em] ...">`; roster meta/status → `text-[11px]`; `SatResultDetailRoute` Raw/Practice labels → `text-[11px] font-semibold uppercase tracking-[0.1em]`; `SatRoot` role/badge/tabs → `text-[11px]`; `SatResultsRoute:118` 8px span → ABSENT (pill at :119 `tone={satOutcomeTone(result.outcomeStatus)}` is the single status signal).
- **B. Row-density shape (≤2 meta lines).** Results row PASS shape: line 1 = name + pill, line 2 = `{studentId} · {cohortName}` (`text-[11px]`), line 3 = `{examTitle} · Version {n} · {date}` — wait, that is 3 lines: Phase 02/04 MUST have merged to ≤2 (e.g., line 2 = `{studentId} · {cohortName} · {examTitle}`, line 3 DELETED or folded). Phase 06 check: count `block truncate text-[11px]` meta spans per row ≤2; roster option `#sat-room-student-{id}` (`role="option"`) contains exactly name-line + ONE meta line + time/status — third `text-[8px]`/`text-[9px]` line must be gone.
- **C. Dirty-guard truth table (verify in `ConfirmDialog.tsx` + both creation surfaces).** `pristine (no field touched) + dismiss (Cancel|Escape|backdrop) → onClose() with NO confirm` · `dirty (any field touched) + dismiss → SatConfirmDialog (Cancel-focused alertdialog, Escape→cancel)` · `confirm discard → onClose()` · `cancel → keep form + values intact`. Verify pristine path never traps and Escape/backdrop handlers from Dialogs tests still call `onCancel` once.
- **D. Dark token block shape (`src/index.css`, Phase 05).** Expect: `@media (prefers-color-scheme: dark) { .sat-product { --sat-staff-canvas:…; --sat-staff-surface:…; --sat-staff-text-primary:…; --sat-staff-text-secondary:…; --sat-staff-text-tertiary:…; … } }` + dark `@media (prefers-contrast: more)` overrides + `forced-colors: active` `ButtonText/CanvasText` mappings. No `.sat-ui` selector inside. No JS toggle (grep `data-theme|setTheme|useTheme` in `src/products/sat/**` → zero hits expected).
- **E. Repair-routing decision (pseudocode).** `if fail in type/size/chevron/dot/tone → owner=01; elif fail in placeholder/TitleCase/CreateSAT/jargon/8px-delete/row-lines → owner=02; elif fail in dirty-guard/Escape/pristine/focus → owner=03; elif fail in room-roster/clock/attention/keyboard → owner=04; elif fail in dark/contrast-matrix/transparency → owner=05; elif fail in .sat-ui/leak-or-behavior → owner=offender + escalate to Main Agent; Phase06 never edits.`

## Edge cases (empty states, loading/error, polling refresh, keyboard, reduced-motion/transparency/contrast, forced-colors, 320px, 200% text)

- **Empty states (next action preserved at 11px+).** Exams/Sessions/Results empty → title + `text-[11px+]` guidance + next-action button (`"Create SAT"` / `"Go to Exam Library"` / clear-filters). Verify no empty state relies on deleted 8px line or jargon; `SatResultsRoutes.test.tsx` empty-match explanation still passes.
- **Loading (skeleton-XOR, header never blanks).** Loading → header (`Results`, `Sessions`) + `role="status"` skeleton (`"Loading SAT results"`, `"Loading published SAT exams…"` at ≥11px). Verify `renders header plus skeleton` tests pass and skeleton has no sub-11px text.
- **Error / stale (alert vs status tones).** Error banner `role="alert"` danger `#b42318`; success `role="status"`; stale `"Data may be out of date."` warning `#92400e` + `Retry` (≥11px). Verify `tones error banners as alerts` test and reload-reject path; dirty-guard never masks an error banner.
- **Polling refresh (4000ms, no state loss).** Sessions `useProctorSessionSummaries(4_000, 'sat')` interval unchanged; reload during dirty form must NOT wipe inputs (dirty-guard covers dismiss, polling must not reset `examId/cohort/start/end` state); verify `SatSessionsRoute.test.tsx` SAT-only boundary assertions still pass.
- **Keyboard (full operability).** Skip link → `#sat-main` (`tabindex=-1`, focus verified); segmented radiogroup arrows; roster `role="option"` listbox arrows + `aria-selected` + `aria-label="Open {name}"`; `SatMenu` items; dialogs trap + Escape→cancel; chips `aria-pressed`; search-clear reachable + named `"Clear {label}"`. Run tab-only pass on each route + e2e keyboard project.
- **Reduced-motion.** OS reduce → `.sat-route-fade` `opacity:1 !important`, stagger `animation:none`, live-dot pulse off (`sat-live-dot` animation disabled), no smooth-scroll. Contract `F-A12` test is the automated gate; manual OS-toggle check on Room clock.
- **Reduced-transparency + contrast.** Transparency reduce → glass `backdrop-blur-*` collapsed to solid surface (`.sat-product aside/header/nav` rules); contrast-more → border/text darkened variants present in both light and dark blocks. Verify CSS greps, not eyeballing.
- **Forced-colors (`forced-colors: active`).** Dots/pills map to `CanvasText/ButtonText/Highlight` (no color-alone meaning); focus rings use `Highlight`; verify forced-colors media block in `src/index.css` covers new `--sat-staff-*` tokens.
- **320px viewport.** Bottom tabs (`min-w-[72px]`, `text-[11px]`) wrap without overlap; roster/detail stack (`grid sm:grid-cols-3` → single column); 36px clock truncates gracefully (`tabular-nums`, no horizontal scroll on main); search + chips wrap. Check via touch-chromium project / 320px emulation.
- **200% text / large type.** 11px floor at 200% ≈ 22px effective — verify no clipped rows (rows grow, ≤2-line rule counts logical lines not pixels), dialogs scroll internally, pills never truncate status text, eyebrows (`11px caps`) remain ≥4.5:1 after scaling.

## Tests (which existing tests must pass; which new unit/contract tests to add, with file paths and assertion sketches)

**A. Existing tests that MUST stay green (run via Verification §D; zero skips):**

- `src/products/sat/ui/__tests__/satContractsCss.test.ts` — F-A6 (`.sat-search-clear` 32px centered: `top:50% !important`, `translateY(-50%)`, `min-block-size/inline-size:32px !important`) + F-A12 (`.sat-route-fade` + reduced-motion `opacity:1 !important`). Frozen; do not weaken 32px to 28px.
- `src/products/sat/ui/__tests__/SatPage.test.tsx` — `satOutcomeTone` mapping (`scored→ready`, `pending→pending`, `invalidated_*→invalidated`, default `→neutral`), `SatStatusPill` dot+label + live-only pulse, `SatSearchField` clear→`onChange('')` + hidden-when-empty, stats/rows/skeleton contracts.
- `src/products/sat/ui/__tests__/Dialogs.test.tsx` — `SatConfirmDialog` alertdialog naming, Cancel answer, Escape→`onCancel`, `SatFormDialog` pristine-close + dirty-confirm paths (Phase 03 additions included).
- `src/products/sat/ui/__tests__/Menu.test.tsx` + `SegmentedControl.test.tsx` + `ui/__tests__/useSatListParams.test.tsx` — menu keyboard, segmented radiogroup, list-param filtering intact.
- `src/products/sat/__tests__/SatRoot.test.tsx` — 3 destinations only, IELTS switch, skip-link focus to `#sat-main`, mobile sign-out→`logout` (sign-out stays bottom per KEEP).
- `src/products/sat/routes/__tests__/SatResultsRoutes.test.tsx` — practice-score rendering (no IELTS band language), skeleton-XOR, count announcement, score-availability filter + empty-match explanation.
- `src/products/sat/routes/__tests__/SatSessionRoomRoute.test.tsx` — provider boundary (`providerKey:'sat'`), per-action pending isolation, error/status banner tones, roster selection + attention filter, stage clock label `"Server-authoritative session clock"`.
- `src/products/sat/routes/__tests__/SatSessionsRoute.test.tsx` — SAT-only session+exam boundaries, 4000ms polling, schedule validation + New Session sheet.
- `src/products/sat/routes/__tests__/SatExamLibraryRoute.test.tsx` — SAT boundary (no IELTS render, no provider selector), `"New SAT"`→`"Create SAT"` primary rename safe (update selector if renamed: `getByRole('button',{name:'Create SAT'})`), create-directly flow, skeleton-XOR.
- `src/products/sat/routes/__tests__/scheduleValidation.test.ts` — start<end, timezone-safe rules unchanged.
- E2E: `e2e/sat-product-workspace.spec.ts` (staff workspace) + `npm run e2e:sat-a11y` (must cover STAFF routes; student-only `sat-student-accessibility.spec.ts` run alone is insufficient — document equivalence or extend).

**B. New regression tests to ADD (Phase 06 specifies; implementation routed — type/contrast→Phase 01, dark→Phase 05; land before sign-off):**

1. `src/products/sat/ui/__tests__/satTypeFloor.test.ts` (owner: 01) — assert `readdir(src/products/sat)` TSX contains no `text-[8px]` / `text-[9px]` / `text-[10px]`: sketch: `const files = walk('src/products/sat'); for (const f of files.filter(f=>f.endsWith('.tsx'))) expect(readFileSync(f,'utf8')).not.toMatch(/text-\[(8|9|10)px\]/);` plus named assertions: `expect(SatResultsSource).not.toContain('outcomeLabel(result.outcomeStatus)} · Practice')` (8px line deleted) and `expect(SatDetailSource).not.toContain('Module identifiers as delivered.')`.
2. `src/products/sat/ui/__tests__/satContrastTokens.test.ts` (owner: 01) — assert `src/index.css` contains `--sat-staff-text-tertiary` `#6e6e73`, zero `#9b9a97` in `.sat-product` scope, `slate-400→tertiary` remap present; WCAG recompute spot-check: `contrast('#6e6e73','#ffffff') >= 4.5` and `contrast('#ffffff','#0071e3') >= 4.5`: sketch with inline relative-luminance helper (no new dep).
3. `src/products/sat/ui/__tests__/satDarkAppearance.test.ts` (owner: 05) — assert `@media (prefers-color-scheme: dark)` + `.sat-product` block defines dark `--sat-staff-surface/text-*`, no `data-theme|useTheme|next-themes` import in `src/products/sat/**`, dark `prefers-contrast` + `forced-colors` blocks present: sketch: `expect(css).toMatch(/@media\s*\(prefers-color-scheme:\s*dark\)[\s\S]*?\.sat-product/)` + `expect(satSources.join()).not.toMatch(/data-theme|useTheme/)`.

## Verification (exact commands: vitest paths, tsc, eslint, grep gates, contrast recompute)

Run from repo root. Append stdout + exit codes to the log. Do not edit source between Steps 1–10.

- **§A — Sub-minimum gates (expect zero output, exit 0 with no matches):**
  - `rg -n "text-\[8px\]" src/products/sat || echo "GATE-A8: zero text-[8px] PASS"`
  - `rg -n "text-\[9px\]" src/products/sat || echo "GATE-A9: zero text-[9px] PASS"`
  - `rg -n "text-\[10px\]" src/products/sat || echo "GATE-A10: zero text-[10px] PASS"`
- **§B — Copy gates:**
  - `rg -n "Module identifiers as delivered\." src/products/sat || echo "GATE-B1: jargon deleted PASS"`
  - `rg -n "New SAT" src/products/sat; echo "(review: primary must be 'Create SAT'; history/prose only)"`
  - `rg -n "Search name, ID, exam, cohort" src/products/sat` (expect 4+ hits: 3 lists + room)
  - `rg -n "#9b9a97" src/products/sat src/index.css || echo "GATE-B2: neutral-dot hex gone PASS"`
  - `rg -n "\.sat-ui" src/products/sat src/index.css || echo "GATE-B3: no .sat-ui leak PASS"`
- **§C — Contrast recompute (Node, no deps):**
  - `node -e "const lum=h=>{const c=h.replace('#','');const f=i=>{const v=parseInt(c.substr(i,2),16)/255;return v<=0.03928?v/12.92:Math.pow((v+0.055)/1.055,2.4)};const L=r=>0.2126*f(0)+0.7152*f(2)+0.0722*f(4);const C=(a,b)=>{const x=L(a),y=L(b);return (Math.max(x,y)+0.05)/(Math.min(x,y)+0.05)};[['#6e6e73','#ffffff',5.07],['#515154','#ffffff',7.91],['#0067c9','#ffffff',5.55],['#067647','#ffffff',5.69],['#92400e','#ffffff',7.09],['#b42318','#ffffff',6.57],['#ffffff','#0071e3',4.70]].forEach(([a,b,e])=>{const r=C(a,b);console.log(a+' on '+b+' = '+r.toFixed(2)+' (expect ~'+e+') '+(Math.abs(r-e)<0.06?'PASS':'FAIL'))})"`
  - `rg -n "prefers-color-scheme:\s*dark" src/index.css` (expect dark `.sat-product` block — Phase 05)
- **§D — Unit + contracts:** `npx vitest run src/products/sat --reporter=verbose` (must include `satContractsCss.test.ts`, `SatPage.test.tsx`, `Dialogs.test.tsx`, `SatRoot.test.tsx`, all `routes/__tests__/*`).
- **§E — Typecheck:** `npx tsc --noEmit` (aliases: `npm run typecheck`).
- **§F — Lint:** `npx eslint src/products/sat --max-warnings 0` then `npx eslint . --max-warnings 0` (aliases: `npm run lint`).
- **§G — Build:** `npm run build` (expects `vite build` exit 0).
- **§H — A11y E2E:** `npm run e2e:sat-a11y` (config `playwright.sat-a11y.config.ts`: chromium, webkit, touch-chromium) + `npx playwright test e2e/sat-product-workspace.spec.ts --project=chromium` if present. Document which spec covered STAFF routes.
- **§I — Scope diff:** `git diff --name-only` · `git diff --stat` · `git diff -- src/index.css src/products/sat | head -400` · `git diff | rg -c "\.sat-ui" || echo "GATE-I: zero .sat-ui hunks PASS"` · `git status --porcelain`.

## Definition of done (checklist, measurable)

- [ ] **D1 — Zero sub-minimum type.** `GATE-A8/A9/A10` all PASS (zero `text-[8px]`/`text-[9px]`/`text-[10px]` in `src/products/sat/**`); audit hot spots spot-checked (`SatResultsRoute:118` absent, `InfoRow`, eyebrows, pills at floor).
- [ ] **D2 — Contrast signed off (computed).** §C script prints 7× PASS within ±0.06 of table (5.07 / 7.91 / 5.55 / 5.69 / 7.09 / 6.57 / 4.70); `#9b9a97` zero hits; chevron `slate-400→tertiary` remap present.
- [ ] **D3 — Single status signal.** `SatResultsRoute:118` 8px duplicate line deleted; one `SatStatusPill tone={satOutcomeTone(...)}` per result row; no forked tone tables (`sessionStatusTone`/`roomStatusTone` alias).
- [ ] **D4 — Dirty-guard shipped safely.** Both creation surfaces confirm-on-dirty via `SatConfirmDialog`; pristine-close + Escape/backdrop verified by `Dialogs.test.tsx` green; no trap reports.
- [ ] **D5 — Room density + ops intact.** Roster/detail ≤2 meta lines per row; attention filter + 36px stage clock + `"Server-authoritative session clock"` present; per-action pending isolation test green.
- [ ] **D6 — Dark appearance.** System-following dark `.sat-product` block present, no toggle (`data-theme|useTheme` zero hits in SAT), dark+contrast+transparency+forced-colors matrix grepped.
- [ ] **D7 — Copy exact.** Jargon zero hits; `"Create SAT"` primary present / `"New SAT"` primary absent; placeholder `"Search name, ID, exam, cohort"` 4+ hits; Title Case buttons; clear ≥32px contract + chips ≥28px.
- [ ] **D8 — Full suite green.** `vitest run src/products/sat` exit 0 (all 12 suites incl. contracts) · `tsc --noEmit` exit 0 · `eslint` exit 0 · `vite build` exit 0 · `e2e:sat-a11y` (+ staff workspace spec) zero violations or documented equivalent · `git diff` shows zero `.sat-ui` hunks and zero non-SAT `src/` files · verification log appended with SHAs, commands, exit codes, contrast table.

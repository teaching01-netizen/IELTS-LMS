# 2026-09-11

## Created skill: `ai-planning-workflow` (user-level)

Installed at `~/.workbuddy-ai/skills/ai-planning-workflow/`. Encodes the Main Agent → Phase Planning
Agents → Implementation Agents → Main Agent Verification pipeline, derived from the real plan tree at
`plans/authoring-read-perf/` in this repo.

Files:
- `SKILL.md` — pipeline, 6 hard rules (evidence/`path:line`, contract-freeze with AT IDs, wave gating,
  planning-only, additive-then-cutover, verify-the-diff), stage instructions, failure-mode table.
- `references/overall-plan-template.md` — 8-section overall plan skeleton.
- `references/phase-template.md` — 14-section phase skeleton + variants (measurement / foundation /
  cutover / verification) and the global `<phase>.<n>` TODO numbering rule.
- `references/worked-example.md` — `plans/authoring-read-perf/` annotated per rule.
- `scripts/scaffold_plan_tree.py` — generates overall + phase stubs; supports nested (`--initiative`)
  and flat layouts; validated and tested in a temp dir.

Validation: `skill-creator/quick_validate.py` → valid; `package_skill.py` → zip built (14KB).

## Plan-tree layouts in this repo (both exist)

- Flat: `plans/overall-plan.md` + `plans/phase-01-tokens.md` … `phase-05-verification.md` (+ 04a/04b
  split, plus a separate `phase-05-verification-log.md`). UI design-system initiative.
- Nested: `plans/authoring-read-perf/` (overall + phase-01..05). Read-path perf initiative.

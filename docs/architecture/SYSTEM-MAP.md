# System Map — IELTS/SAT/ACT Proctoring Platform (V1 FROZEN)

> Status: V1 FROZEN by L0 on 2026-09-10. Awaiting L4 independent verification.
> Route: ROADMAP — L0 -> 6x L3 lanes -> L0 integrate (this file, V1) -> L4 verify -> repair -> recheck -> complete.
> No L1/L2 were used: lanes ran from clear read-only contracts; L0 integrated directly.
> Mapping only: zero code changes, no secret leakage (.env key names only, values never recorded).

## Success card
- Goal: one accurate, file-cited map of system flow + software architecture + business logic + /routed-work-orchestration usage.
- Expected result: this doc, frozen as V1, L4-checked.
- Must preserve: read-only mapping; stale .context/coverage labeled non-governing; inferences marked INFERRED.
- Required checks: L4 verifies route correctness, file-path accuracy (spot-check), six-lane coverage, no secrets.
- Done when: V1 (or repaired V2) passes L4; findings repaired in smallest scope only.

## 0. Route record
- Chosen route: ROADMAP (6 independent scopes, sequencing + integration risk, cross-feature coordination).
- Flow: L0 -> 6x L3 lanes -> L0 integrate (freeze V1) -> L4 verify -> repair -> recheck -> complete.
- Lane reports: frontend V1-lane, backend V1-lane, business-logic V1-lane (4 parts), flows V1-lane, orchestration/docs V1-lane, infra/data/testing V1-lane. Full texts held by L0; this file is the integrated frozen version.

---

## 1. Frontend architecture (React 19 + Vite + TS)

### 1.1 Entry & shell
- `src/main.tsx` (39 lines): `StrictMode > ErrorBoundary > MotionConfig(reducedMotion=user) > AuthSessionProvider > QueryClientProvider(queryClient) > RouterProvider(router)`. Resets scroll+blur on navigation. Imports `./index.css`.
- `src/App.tsx`: stub returning null; routing lives in AppShell/routes.
- `src/routes/index.tsx`: re-exports `{appRoutes, router}` from `src/app/router/createRouter.tsx`.
- `src/components/AppShell.tsx`: thin `<Outlet/>` only; init delegates to `features/admin/routes/AdminRoot.tsx`, schedules to AdminRoot/ProctorRoot, polling to ProctorRoot/StudentSessionRoute.
- No global Header/Sidebar; chrome is per-workspace: builder (`src/components/Header.tsx` + `Sidebar.tsx` in `features/builder/routes/BuilderRoot.tsx`); IELTS admin shell (`features/admin/routes/AdminRoot.tsx` + `AdminContext.tsx` + `useAdminRootController`); SAT product shell (`src/products/sat/SatRoot.tsx`, role nav + SatMenu); proctor (`src/components/proctor/ProctorApp.tsx` wrapping `ProctorDashboard.tsx`); student IELTS (`src/components/student/layout/StudentExamShell.tsx` + `StudentExamViewport.tsx`); student SAT (`src/features/student-delivery/ui/SatExamShell.tsx`).
- Global UX: `GlobalToast.tsx`, `ui/Toast.tsx`, `CommandPalette.tsx`, `ConfirmModal.tsx`, `ui/AppLoadingSkeleton.tsx / LoadingSurface.tsx / ErrorSurface.tsx`.

### 1.2 Router & route table
Source: `src/app/router/createRouter.tsx` (533 lines, `createBrowserRouter(appRoutes)`) + canonical `src/app/router/route-manifest.ts`. All routes lazy + `Suspense(AppLoadingSkeleton)`, per-route `src/routes/RouteErrorBoundary.tsx`, auth via `withAuth(el, roles)` -> `features/auth/RequireAuth.tsx`. Parent `/` = AppShell.

| Path | Component / file | Roles |
|---|---|---|
| `/login`, `/` index | `features/auth/LoginPage.tsx` | public |
| `/activate` | `features/auth/ActivateAccountPage.tsx` | public |
| `/password/reset`, `/password/reset/complete` | `PasswordResetRequestPage/PasswordResetCompletePage.tsx` | public |
| `/admin` (+ index redirect via `resolvePostLoginPath`) | `features/admin/routes/AdminRoot.tsx` | admin,builder,grader |
| `/admin/exams` | `features/exam-authoring/routes/ExamsRoute.tsx` | admin,builder |
| `/admin/library` | `features/content-library/routes/LibraryRoute.tsx` | admin,builder |
| `/admin/scheduling` | `features/scheduling/routes/SchedulingRoute.tsx` | admin,builder,grader |
| `/admin/grading` | `features/grading/routes/GradingRoute.tsx` (+ `components/admin/StudentReviewWorkspace.tsx`) | admin,grader |
| `/admin/results` | `features/results/routes/ResultsRoute.tsx` -> `components/admin/AdminResults.tsx` | admin,grader |
| `/admin/settings` | `features/preferences/routes/SettingsRoute.tsx` | admin,builder |
| `/admin/answer-history/:submissionId` | `features/answer-history/routes/AnswerHistoryRoute.tsx` (Admin) | admin,grader |
| `/sat` (+ index redirect proctor->sessions, grader->results else exams) | `products/sat/SatRoot.tsx` | admin,builder,proctor,grader |
| `/sat/exams`, `/sat/sessions`, `/sat/results`, `/sat/results/:resultId`, `/sat/sessions/:scheduleId` | `products/sat/routes/SatExamLibraryRoute/SatSessionsRoute/SatResultsRoute/SatResultDetailRoute/SatSessionRoomRoute.tsx` | admin+role split |
| `/sat/exams/:examId`, `/builder/:examId` | `features/exam-authoring/routes/ProviderBuilderRoute.tsx` (sat->SatAuthoringRoute else ExamConfigRoute) | admin,builder |
| `/sat/exams/:examId/release`, `/builder/:examId/review` | `ProviderReviewRoute.tsx` | admin,builder |
| `/sat/exams/:examId/preview`, `/builder/:examId/preview` | `ProviderPreviewRoute.tsx` | admin,builder |
| `/sat/exams/:examId/access` | `products/sat/routes/SatAccessRoute.tsx` | admin,builder |
| `/builder/:examId/builder` | `features/builder/routes/BuilderRoot.tsx` | admin,builder |
| `/builder/:examId/answer-key` | `features/builder/routes/ExamAnswerKeyRoute.tsx` | admin,builder |
| `/proctor` | `features/proctor/routes/ProctorRoot.tsx` (re-exported by `features/proctoring/routes/ProctorRoot.tsx`) | admin,proctor |
| `/proctor/answer-history/:attemptId` | `AnswerHistoryRoute.tsx` (Proctor) | admin,proctor |
| `/join/:accessLinkId` | `features/student-delivery/routes/StudentAccessLinkEntryRoute.tsx` | public |
| `/student/:scheduleId`, `/student/:scheduleId/register` | `StudentRegistrationRoute.tsx` | public |
| `/student/:scheduleId/:studentId` | `features/student/routes/StudentSessionRoute.tsx` -> IELTS `StudentAppWrapper` or SAT `SatStudentSessionRoute` | all incl. student |
| `/__dev/highlight-selection`, `/__dev/sat-accessibility` | `app/router/dev/*.tsx` | DEV only |
| `*` | `NotFoundRoute` | public |

Pre-check/lobby/exam/complete are internal phases of `/student/:scheduleId/:studentId` (`StudentExamPhaseRenderer.tsx`), not routes.

### 1.3 State management
- Zustand: `src/app/store/userStore.ts` (persist `user-storage`); `src/app/store/uiStore.ts` barrel -> canonical `src/store/useUIStore.ts` (persist `ui-storage`: sidebar, modals, toasts MAX 5 FIFO, theme, globalLoading); `src/app/store/notificationStore.ts` (MAX 20, 5s/8s); `src/store/useAuthStore.ts` (persist `auth-storage` v1, expiry-aware, cross-tab sync). Scoped vanilla stores: `features/student/application/exam-session/studentExamStoreFactory.ts` via `StudentExamSessionProvider.tsx`; SAT `satReadingPreferencesStore/satResponseOutboxStore/satToolGeometryStore.ts`.
- React Query: `src/shared/api/queryClient.ts` (barrel `src/app/data/queryClient.ts`): stale 5m/gc 10m, retry 3 exp backoff, `shouldRetryQuery` skips 429, `liveQueryPolicy{15s,2m}`, `staticQueryPolicy{30m,60m}`, `queryKeys{exams,schedules,library,sessions,grading,students,proctoring,answerHistory}`.
- Query hooks: `src/app/data/examQueries.ts` (exams/versions/events/schedules/runtime 30s+jitter); `features/student/api/studentSessionQueries.ts` (static/live via facade); `features/proctor/api/proctorQueries.ts`; `features/grading/api/gradingQueries.ts` (30s+jitter); `features/results/api/{resultsQueries,satResultsQueries,ieltsResultDetail}.ts`; `features/scheduling/api/scheduleQueries.ts`; `features/content-library/api/libraryQueries.ts`; `features/answer-history/api/answerHistoryQueries.ts`; `features/exam-authoring/api/{examQueries,assessmentQueries}.ts`.
- Live/polling: `src/shared/hooks/useLiveUpdates.ts` (WS `/api/v1/ws/live`, 250ms debounce, `STUDENT_ROLE_SOCKET_RETIRED`); `src/shared/hooks/useAsyncPolling.ts` (1s default, backoff, in-flight guard). Student socketless: `features/student/hooks/useStudentSessionRouteData.ts` (962 lines) + `studentRuntimePoll(Loop).ts` (2s fast/25s steady, 410 terminal) + `studentRealtimeCoordinator.ts` + `studentSessionStateMachine.ts`; writes via `src/shared/durability/DurableResponseEngine.ts` (1760 lines) + `studentMutationOutboxAdapter.ts` + `responseDurabilityTransport.ts`. Proctor: `features/proctor/hooks/useProctorRouteController.ts` (902 lines) + WS proctor-observer. SAT: `features/student-delivery/hooks/useSatExamController.ts` (1052 lines) + interaction/persistence/integrity/deadline-clock hooks.

### 1.4 Feature / product modules
- `features/auth/`: pages, `RequireAuth.tsx`, `authSession.tsx`, gateways, `validation/authForms`.
- `features/admin/`, `features/builder/` (tabs, route controllers, autosave, version load), `features/exam-authoring/` (facade/gateways, composers, import sheets, SAT provider, `ui/AuthoringWorkspace.tsx` 1468 lines, spine/release/access-links UI), `features/exam-rendering/`, `features/content-library|grading|results|scheduling|answer-history|preferences/` (route + queries + gateway + ui each), `features/proctor|proctoring/` (facade, queries, gateway, route controller), `features/student/` (exam-session application/domain/contracts/infrastructure/api/hooks/routes), `features/student-delivery/` (SAT: reducer/selectors, timing/tools/interaction domain, controllers, gateways incl. Desmos, `SatStudentSessionRoute` 501 lines, shell/renderers/review/transitions/tools/annotations).
- `products/sat/`: `SatRoot.tsx`, 6 routes + scheduleValidation, `ui/SatPage/Menu/SegmentedControl/ConfirmDialog`.
- `components/admin/`: exams/scheduling/grading/results/settings, drawers/filters/history/audit/compare, `StudentReviewWorkspace.tsx` (1918 lines), traceback, writing canvas, report preview, objective panels, export builder + per-student export, providers.
- `components/proctor/`: `ProctorDashboard.tsx` (1364 lines: cohort controls, cards, panels), `ProctorApp.tsx`, `PresenceIndicator`.
- `components/student/` (~204 files): `StudentApp.tsx` (1136 lines), `StudentAppWrapper.tsx`, `StudentExamWorkspace.tsx`, phase renderer, `QuestionRenderer`, providers/layout/highlight/blocks.
- `components/blocks/` (TFNG/cloze/matching/map/diagram/flowchart/table/note/completion/short-answer/MCQ/classification), `components/workspaces/` + root `Workspace.tsx` + `QuestionBuilderPane.tsx` (931 lines), `components/{passage,listening,questionbank,answer-history,ui}` (40+ shadcn-based primitives), `components/scoring/` (workspace/rubric/bands), `components/results/` (raw tables).

### 1.5 Services & API client
- Canonical `src/shared/api/apiClient.ts` (719 lines; barrel `src/app/api/apiClient.ts`): baseURL `/api`, 30s timeout, same-origin creds, JSON/FormData, per-request token/csrf, CSRF double-submit (cookie `VITE_AUTH_CSRF_COOKIE_NAME`||`__Host-csrf`||`csrf`), Bearer, AbortController, 3 retries, 401 unauthorized-handler, `ApiError` envelope `{success,data,error,metadata}`.
- `src/shared/error-codes.ts`: ConflictKind + CODE_MAP (LEASE_FENCED/CONTROL_EPOCH_STALE/VERSION_COLLISION/DEADLINE_EXPIRED/ATTEMPT_PROCTOR_BLOCKED/TERMINALIZATION_CONFLICT/IDEMPOTENCY_KEY_REUSED/.../RATE_LIMIT_EXCEEDED).
- Legacy `src/services/*.ts`: examRepository, examLifecycleService (+examStatusTransitions policy), examDeliveryService, examAdapterService, gradingService/Repository, studentAttemptRepository (+normalization), studentMutationOutbox, studentSessionTransport, authService, passage/questionBank services, answerHistoryService, backendBridge, entryQueueRetry, attemptCredentialAdapter.
- Feature gateways (via apiClient) + queryKey invalidation per domain.

### 1.6 Shared primitives
UI (`shared/ui` + `components/ui/*` + SAT ui + `constants/uiConstants.ts`); zod validation (`app/validation/schemas.ts` 269 lines + `shared/lib/validateApiResponse.ts` + auth forms + `useFormHandler.ts`); error (`app/error/ErrorBoundary.tsx` root + `routes/RouteErrorBoundary.tsx` + errorTypes + errorLogger + useErrorHandler/withErrorHandling); perf (`app/performance/lazyLoad.tsx`, memoize, performanceTracker, `app/monitoring/performanceMonitor.ts`); durability hooks; types (`src/types.ts` 908 lines + domain/grading/attempt/answers/utility) + `constants/examDefaults.ts` + `utils/*`.

### 1.7 Render-path data flows
STUDENT-EXAM (`/student/:scheduleId/:studentId`): main -> Router -> AppShell -> StudentSessionRoute -> useStudentSessionRouteData (static facade 30m + versioned runtime poll 2s/25s, WS retired 410, state machine + freshness) -> providerKey switch -> IELTS (StudentAppWrapper -> session provider -> providers -> StudentApp -> PhaseRenderer -> shell/workspace -> module renderers; answers via DurableResponseEngine -> outbox -> POST batch/submit -> 409 reconcile) / SAT (SatStudentSessionRoute -> useSatExamController -> SatExamShell -> renderers/review/transitions).
ADMIN-AUTHORING (`/builder/:examId/builder`, `/sat/exams/:examId`): main -> Router -> withAuth(admin,builder) -> ProviderBuilderRoute (useExamQuery -> providerKey) -> SAT (SatAuthoringRoute -> AuthoringWorkspace -> assessmentQueries -> spine/autosave -> review/releaseApi -> publish; preview via SatExamShell) / IELTS (BuilderRoot -> route controller + autosave -> Header/Sidebar/Workspace -> StimulusPane + QuestionBuilderPane; answer-key/review/preview routes; facade lifecycle; version history/compare/audit).

---

## 2. Backend Go architecture

### 2.1 Process layout
- Binaries under `backend/go/cmd/`: `api` (chi HTTP), `worker` (background), `migrate` (schema runner); helpers `e2e_seed`, `e2e_provision_staff`, `preprovision`. Prebuilt: `backend/go/bin/{api,api-clone,migrate,worker}`.
- Shared graph `backend/go/internal/app/app.go` (`shared.Build`) constructs ALL domain services once (SAT/ACT Completer switch, presence posture, terminal scorer); `cmd/api/main.go:BuildApp` embeds into HTTP-edge `App` (limiter, SessionCache, EntryGate, Versions, RuntimeSnapshots, LiveBus/Hub/Leases/Admission, Secret, Tx, Outbox); nil pool => degraded 503 App (`main.go:155-158`).
- api startup: config.Load+ValidateForRuntime -> db.OpenRole(RoleAPI) -> VerifyRuntimeSchema fail-closed -> BuildApp -> BuildRouter -> ListenAndServe (httpx timeouts + HTTP_WRITE_TIMEOUT_SECS) -> graceful shutdown (stop forwarder + reaper, drain 30s, close pool).
- worker (`cmd/worker/main.go`, Jobs list): hot cycle (default 10s; 5s min with projection) = DrainOutbox (<=20 x <=100, 60s lease) + RunGradingProjection; slow cycle (300s floor 60s) = SAT reconcile, terminal repairs, invariant audit, retention + media cleanup. RoleWorker pool split.
- migrate (`cmd/migrate/main.go`): lexical `backend/go/migrations/*.sql` vs `schema_migrations(filename PK)`; GET_LOCK 300s; refuses legacy non-empty DBs without history unless warn mode; crash-retry safe (row after ALL succeed); checksum + guards; `--validate-only` / MIGRATE_VALIDATE_ONLY.
- `backend/go/api` is a prebuilt binary, not a source package; canonical spec at repo-root `api/openapi/openapi.yaml`, enforced by `openapi_drift_test.go:TestOpenAPICoversRouter`. `backend/go/integration/` DB-backed contract tests.

### 2.2 HTTP surface
Router `BuildRouter` (`main.go:317-629`, chi); every route wrapped by `authorize(key,h)` + `route()` stamping `httpx.WithRoute` (low-cardinality logs + authz key).
Middleware (spec-pinned): `CORS(same-origin) > Recovery > RequestID > Trace > SecurityHeaders > BodyLimit(64MiB global; tighter per-group) > authMiddleware > csrfMiddleware > TierBackstop > limitTier(tier) > authz.MiddlewareFor > handler > AccessLog(trailing; outer-layer 429/413/403/500 bypass it)`.
Handlers (`backend/go/cmd/api/handlers_*.go`): auth (558l cookie-session boundary); admin (761l exams CRUD/lifecycle + schedules); accesslinks (357l); authoring (873l SAT/IELTS + `authoring_operation_total`); delivery (237l SAT attempt-bearer + bootstrap ETag); domain (761l V1 student sessions + proctor commands, 10s query timeouts, static ETag); v1_writes (225l mutations/audit/submit); v2 (969l response protocol batch|submit|takeover + snapshot, bearer choke `verifyAttemptBearer`, v2Resolver, RuntimeLockerFor); runtime_poll (157l versioned poll, weak ETag `W/"rt-{schedule}-{rev}"`, pollAfterSecs 2s/25s); ws (390l staff-only, lease admission, hub subscribe, forwarder); proctor (337l) + proctor_notes (235l) + proctor_roster (cursor pagination); grading (1079l) + grading_sessions (255l dual-shape list); release (38l); library (855l + answer-history projections); media (98l, 16MB cap). Probes: `/healthz` (no DB), `/readyz` (DB+schema, 5s budget, pool gauges), `/metrics` (private bearer/fail-closed). 404 stable NOT_FOUND; 405 Allow via route-tree walk.
Edge files: `entry_gate.go` (per-schedule bucket 500/s+2000, fastPathAttempt, fail-open to mint), `ws_admission.go`, `etag.go`, `query_timeout.go` (10s, MapDBError->503), `student_context.go`, `authoring_authorization.go`, `v2locker_snapshot.go`, `txhook.go` (->db_deadlocks_total), `sat_workbook_template.go`.
Tiers: auth-critical, anon-auth, authed-reads, writes, polling, heartbeat, backstop global.

### 2.3 Internal packages
- `accesslinks` (1276l): link domain + ResolveEntry; no auth (handlers gate).
- `act` (712l): ACT science scoring (Compute/Seal/Score/Report).
- `answerhistory` (728l): read-only V1-mutation replay projections.
- `app` (149l): shared Services+Deps+Build; SAT/ACT Completer, presence, terminal scorer.
- `assessscore` (257l stdlib, cycle-free): read-path scoring helpers + v2read.
- `attempts` (697l + submit/validate/materialize/canonicaljson/types): V2 write algorithm (lock -> verify -> exact-replay -> epoch fence -> runtime gate -> idempotency/version -> ledger+projection -> bump); ClosingGrace 30s; lock order attempt->runtime->section.
- `auth` (872l + attemptverify/sessioncache/sessionlookup): cookie sessions (staff 30m idle / student 60m / 12h absolute; argon2/bcrypt), CSRF, HMAC attempt bearers (15m TTL/60s touch/5m refresh), strict vs stateless modes.
- `authoring` (1871l + mutation_contracts/readiness/workbook): SAT/IELTS reads+writes, adaptive roles none|base|lower|higher, routing gate.
- `authz` (authz.go 217l + middleware/tables): fail-closed route table; Policy{MinRoles,Public,Bearer,Scope}; unlisted denies 403.
- `delivery` (1942l + start_submit/reconcile/versioncache): bootstrap assembler (never scores on read); live modes db vs direct; VersionCache.
- `exams` (1289l + sat_initialization): thin CRUD; providers ielts|sat|act; SAT draft validation delegation.
- `grading` (1513l + projection/result_read/review_read/sessions): IELTS state machine + overrides/drafts/checkpoints.
- `library` (844l): passages/questions/defaults/export-profiles; org scope; revision fence.
- `liveupdates` (bus 523l/admission 213l + hub/leases): bus table + in-process Hub + Admission caps 600/5/600 + lease repo.
- `maintenance` (716l): retention batches, media GC, invariant audit, grading projection, checkpoints.
- `media` (469l): upload intents over media_assets + objectstore; 16MB; 25MP image guard.
- `outbox` (511l + dlq/metrics): transactional outbox (claim 100/60s lease; backoff 5s*2^n cap 300s, terminal >=8); only auto_submit executable, rest wakeups; DLQ atomic park + single-use requeue; partitions via MOD(CRC32); exec-only mode.
- `platform/*`: answerblobs (B3 materializer), apperrors (stable envelopes), clock, config (896l env catalogue), crypto (HMAC), db (UTC pools, OpenRole split, VerifyRuntimeSchema), httpx (7 tiers + backstop, body limits 256KiB/2MiB/64MiB, shed), objectstore, pagination, shutdown (30s), telemetry (registry, bounded labels), tx (runner, RR/RC, retry hook).
- `proctor` (1084l + sessions/reconcile/rollup/rosterpage/notes_rules): commands (warn|pause|resume|extend|terminate via terminalization; end/extend/complete + auto-submit; presence; ack); service-layer auth; control_epoch bumps + outbox.
- `release` (339l): read-only SAT release state.
- `results` (897l): scored outcomes; ready-to-release gate; snapshot re-materialization.
- `runtime` (655l + poll/snapshot): cohort clock (statuses, revision+section fencing 409, epochs, grace); SnapshotCache 1s TTL; PollView{rev,status,section,pollAfterSecs}.
- `sat` (1064l): split-brain completion (provisional receipt then CompleteAssessment/ReconcileProvisional watchdog); PolicyScorer fail-closed.
- `schedules` (1020l): CRUD + revision-fenced commands + registrations (wcode+email idempotent replay); protocol_version=2 mints.
- `student` (288l + v1_write/readthrough/presence): V1 compat boundary + PresenceMap + rowFirst read-through.
- `terminalization` (1185l): immutable terminal fact (T1 PK attempt_id + UNIQUE terminalization_id; T2 seal_attempt_in_tx ONLY writer; T3 find+insert; T4 outcome-only replay); vocab outcome/reason/actor; SAT outcome_status mapping; in-tx outbox only; RepairMissingReceipts; B3 materializeBlobsForSeal.

### 2.4 Persistence & migrations
MySQL/InnoDB via go-sql-driver, UTC, role-split pools (api ~40, worker ~10). Tx: platform/tx.Runner, global lock order attempt->runtime->section. Outbox in-tx on every commit. Receipt immutability: `0056` + T1-T4, only seal writes `attempt_terminalizations`. Lineage 58 files 0001-0058: roles -> rls_helpers -> exam_core -> library -> scheduling/access -> delivery -> proctoring -> grading/results -> media/cache/outbox -> auth/security -> outbox trigger -> registration/presence/hardening/idempotency -> prod/exam-day hardening -> display names -> finalization + submission ledger -> sort/projection/admission queue -> access-code relax -> grading overrides/source + release precision -> outbox retry -> export profiles -> provider-neutral SAT -> SAT runtime/authoring -> access links -> autosave durability -> revision updated_by -> runtime + SAT timing -> provider identity -> workbook recovery -> WS leases -> terminalizations -> result outcomes -> heartbeat idempotency -> admin observer -> identity names -> ts precision -> V2 durability -> ACT science -> drop triggers (split-brain) -> ACT identity -> user_id backfill -> ACT key heal -> DLQ -> receipt immutability -> repair guard -> authoring op keys. Policy `MIGRATION_POLICY.md` (additive-only, backup+row-guard deletes, named heals, CHECK-by-name, UNIQUE-vs-INDEX table rule). Runtime guard: VerifyRuntimeSchema + --validate-only + engine/lineage/repair tests.

### 2.5 Realtime & background
WS admission (ws_admission.go + liveupdates/admission + leases): memory (zero-SQL) vs db (singleton-row tx) via WS_ADMISSION; caps 600/5/600, TTL 60s/beat 30s, 429 CodeLeaseAcquireFailed; same-host origin; ping/lease tickers. Livebus: kinds schedule_runtime|schedule_roster|schedule_alert|attempt; AppendInTx (db) ; forwarder 200 rows/250ms excl. own origin; Hub drop-slow + ShouldForward role filter; LIVE_BUS=direct = Hub-only post-commit. Students poll (304 + 2s/25s), staff hold WS. Outbox drain: <=20 x 100 under 60s token; partitions MOD(CRC32); wakeups relayed to bus, executable auto_submit fan-out (cursor 500, per-attempt seal, poison->DLQ, permanent conflicts acked, transient retried with backoff); MarkPublished|MarkFailed(terminal>=8+DLQ). Other jobs: ReconcileExpiredSections/Timeouts, SAT module/provisional reconcile (250-batch), RepairMissingTerminalReceipts, invariant audit, grading projection, retention, media GC, presence flush 60s, pool stats, job histograms.

### 2.6 Cross-cutting
Auth: cookie session LRU+touch-coalesce (unknown->anon, DB-down->503) -> requireSession/Role + authz.MiddlewareFor + handler boundary checks; CSRF cookie+header+origin on session writes (403); attempt bearers via verifyAttemptBearer (strict vs stateless; reads always touch); roles admin|admin_observer|builder|proctor|grader|student. Ratelimit 7 tiers (per-min + DB counter + 2x local prefilter + in-mem bucket; keying user|attempt|IP; RATE_LIMIT_MODE local|dual; SHED_MODE exam rebalances to writes, denials labeled); 429 RATE_LIMIT_EXCEEDED + metrics (AccessLog never sees 429s). ETag: writeETagOrNotModified (exact/weak/* =>304) on runtime poll, bootstrap VersionETag, V1 static; VersionCache invalidated on commit/publish. Query timeout 10s herd paths -> 503. Entry gate per-schedule bucket + RetryAfter/QueuePosition. Postures default-off fail-closed (SESSION_CACHE/ATTEMPT_VERIFY/LIVE_BUS/WS_ADMISSION/PRESENCE/VERSION_CACHE/RUNTIME_SNAPSHOT/ROW_FIRST/OUTBOX_EXEC_ONLY/pool-split/partitions/shed). Metrics/alert parity tests; /proctor/live-mode degraded mirror. Error contract: stable envelopes, unknown->500 masked + log hook, 405 Allow, pagination contract, body limits, sanitized request IDs.

### 2.7 Request-lifecycle diagram
migrate (GET_LOCK + 0001..0058, fail-closed) -> MySQL (UTC, role pools, lock order, terminalizations PK-immutable, outbox+DLQ, live events, WS leases) -> api chi (shared.Build graph) + worker (hot drain+project / slow reconcile+repair+audit+retention). Middleware: CORS>Recovery>ReqID>Trace>SecHdr>BodyLimit>auth>csrf>Backstop>limitTier>authz>timeout>handler: cookie path (role->service tx attempt->runtime->section -> in-tx outbox/AppendInTx -> commit -> Hub -> JSON+ETag) | bearer path (verifyAttemptBearer + URL-vs-claim + in-tx fence) | poll path (snapshot/version cache -> PollView -> ETag 304|200) | WS path (lease -> role-filtered subscribe; db-mode forwarder fans peer rows). Worker drain: ClaimBatch -> wakeup publishWakeup vs executeOutboxEvent (autosubmit fan-out, seal via terminalization, DLQ/backoff) -> MarkPublished|MarkFailed.

---

## 3. Business logic

### 3.1 Exams lifecycle
States `exams/service.go:42-52`: draft|in_review|approved|rejected|scheduled|published|archived|unpublished; vis private|organization|public; types Academic|General Training|ACT; providers ielts|sat|act (blank->ielts; EffectiveProviderKey ACT wins). Sections: IELTS listening/reading/writing/speaking; SAT reading-writing/math; ACT +science; SectionKeyAllowlist. Create->draft + version skeleton (SAT initializeSATDraftTx else uuid; rev 0; exam_events create). All mutations revision-fenced; publish adds ExpectedDraftVersionID/Revision + OperationKey replay via claimPublishOperationKey; ValidationReport{CanPublish,Errors,Warnings} gates publish. Authoring: Shell + RoutingPolicy{base,lower,higher,minimumCorrectForHigher}; roles none|base|lower_branch|higher_branch; import previewed|committed|undone|expired; touch*Draft requires current-draft + is_draft + NOT published else 409. Readiness (`readiness.go`): ready|incomplete|error + BlockingIssueCount; SAT blueprint RW 32m/27q/2pretest, Math 35m/22q/2pretest + calculator/reference_sheet, break 10m/0m; SAT-only gate. Objectives: precedence override > sealed key > zero; OverrideObjectiveQuestion 0..100, 409 if released. FE: examQueries, exam-authoring api/facade, audit/override/integrity panels, types/domain.ts.

### 3.2 Attempts / submit
Vocab (`attempts/types.go`, `runtime/service.go:42-81`): delivery running|paused|submitted|terminated|locked|cancelled; phase pre-check|lobby|exam|post-exam; proctor active|warned|paused|terminated; lease_epoch/control_epoch/responseRevision/deadline_at/closing_grace +30s; runtime not_started|live|paused|completed|cancelled; section locked|live|paused|completed. Envelopes (`validate.go`): batch<=100 cmds, WriteID<=64, QuestionID<=255, eliminated<=16, annot<=64, SAT sat-annotations v2<=200; dup writeId rejected pre-tx. Write order (fencing_order_test pin): lock -> verify -> exact-replay -> lease/control fence BEFORE runtime lock -> runtime gate (DB time, section live) -> idempotency/version -> ledger+projection -> bump; RC + 3x retry. lease mismatch LeaseFenced; stale control; revision 409; same-writeId-diff-hash WriteIDConflict; same-bytes replay. Writability: paused|post-exam|submitted_at|proctor terminated|paused => not-writable; pause bumps control_epoch+1. Submit (`submit.go`): receipt probe first (same ID+hash replay; diff 409 SubmissionReplayMisuse + ownership check); SAT provisional two-phase (NULL seal) vs immediate seal submitted/student_submit; protocol_version=2 only. Terminal (`terminalization/service.go`): PK attempt_id + UNIQUE terminalization_id; ONLY sealAttemptInTx writes; immutable find+insert; outcome-only compat; outcome submitted|terminated; reasons student_submit|sat_complete|time_expired|auto_stop|proctor_{complete|end|force_submit|terminate}|legacy_unknown; SAT invalidated_proctor|invalidated_timeout|pending; only attempt_terminalized outbox; lock order held. FE durability: studentExamStore, StudentAttemptController, answer/submission commands, submissionCoordinator, reconcileServerSnapshot, evaluateSubmissionBarrier (phase EXAM + pending 0 + durabilityReady + verified), blockingStateMachine, studentSessionQueries.

### 3.3 Grading / scoring
IELTS machine (`grading/service.go`): objective -> writing pending|auto_graded|needs_review|in_review|finalized -> submission submitted|in_progress|grading_complete|ready_to_release|released|reopened -> release draft|grading_complete|ready_to_release|released|reopened (+results invalidated). ProjectIELTSAttempt snapshot-only idempotent upsert; preserves in_review/finalized/reopened. MarkComplete/MarkReadyToRelease/ReleaseNow (stamps drafts+submissions+results+events)/ScheduleRelease (future, rechecked)/ReopenReview (fans both tables). Queue excludes __preview_runtime__; grader scoping via schedule_staff_assignments; clamps 200/500, page 100. Bands buildSectionBands/averageBand/overallBandOf. Scorers: `assessscore/score.go` (SAT single/SPR + tolerance; ACT canonical-JSON equality; v2read envelope strip); SAT split-brain provisional->CompleteAssessment + watchdog ReconcileProvisional, PolicyScorer exact-table fail-closed; ACT ComputeScienceScore->{total,max,%} + seal-merge, client score 400. Module submitted|locked. FE: scoring/*, gradingQueries (30s+jitter), export buttons/builder/per-student export (filename/objective/writing/zip), WritingAnnotationCanvasLegacy, StudentReportPreview, grading_export_profiles.

### 3.4 Proctoring
Owner `proctor/service.go|sessions.go|notes_rules.go|reconcile.go|rosterpage.go`. authorizeWrite Admin|Proctor + CSRF + live assignment (Builder preview-only) else 404 collapse; reads Admin|Observer|Proctor. Warn = PROCTOR_WARNING + warned (non-blocking); Pause/Resume protocol-aware (+control_epoch, deadline shift); ExtendAttempt SAT-module only (provisional 409, shared-clock 400); Terminate ONLY via terminalization; end-section-now rejects SAT/adaptive + IELTS-authentic; ExtendSection cohort + sync_v2; complete-exam -> auto-submit. ReconcileExpiredSections (live + autoSubmit + deadline+30s, 250-batch, per-schedule tx, SECTION_END/START/SESSION_END audits + wakeup). Roster 200. Presence join|heartbeat|leave. AckAlert. Notes author=actor. Violation-rules CRUD ownership-checked. DTOs SessionSchedule/Runtime/StudentSessionSummary/ProctorAlert/SessionNote/SessionAuditLog. FE: proctorQueries (?mode=dashboard&auditLimit=200&alertLimit=100, degradedLiveMode), facade mappers, useProctorRouteController/ProctorRoot, ExamGroupCard/overview sessions; student warning visibility + blocking-machine sync.

### 3.5 Library / media
Library (`library/service.go`): passages/questions/defaults/export-profiles; shared(NULL-org)+tenant visibility; tenant/platform writes; revision fence 409; easy|medium|hard; limit 100/500. FE libraryQueries + content-library api + libraryConcurrency. Media (`media/service.go`): intent/complete/get over media_assets + objectstore; 16MB; pending|finalized|orphaned|deleted; 24h orphan via maintenance; 25Mpx/8192 image guard; path-safe names + owner check + presigned URLs.

### 3.6 Schedules / access-links / entry
Schedules (`schedules/service.go`): scheduled|live|completed|cancelled; cmds start|pause|resume|complete (+aliases, 400 pre-authz); V2-only mints; CreateRegistration wcode+email single funnel, same-user replay, cross-user CONFLICT + revision fence. Clock (`runtime/service.go`): idempotent Start; Pause/Resume; double fence revision+section 409; control bump; sync_v2 deadline +30s grace (proto2 non-terminal). Links (`accesslinks/service.go`): audience anyone|cohort|selected_students; mode student_code|open; lifecycle active|paused|revoked; Status lifecycle-first; revision-fenced CRUD/lifecycle/duplicate; ResolveEntry single-tx lifecycle/roster-identity/window gates. Entry ADR 0001 (POST /api/v1/auth/student/entry): closed-by-default, pre-mint gate (zero rows on reject), only accessLinkId->ModeOpen open path, 404 collapse, gaps explicit (captcha unverified, metadata enumerable, no open flag; Round 2 planned). 30/min email+IP + tier/entry-gate kept; mint chain user->registration->attempt+bearer->session (student-scoped). Admission: liveupdates/admission in-mem WS gate + bus lease lock; outbox families attempt_terminalized|runtime_changed|roster_changed|attempt_changed|autoSubmit. FE: scheduleQueries, studentAccessLinkQueries, StudentRegistrationRoute, Lobby/PreCheck.

### 3.7 Results / analytics
`results/service.go` reads-only: outcome scored|pending|invalidated_proctor|invalidated_timeout; release draft|grading_complete|ready_to_release|released|reopened|invalidated. Released rebuilt from snapshot, never recomputed. IELTS<-student_results; SAT/ACT<-assessment_results. isCorrect null = pretest/unanswered/no-key/pending/invalidated (never render as incorrect). Analytics total/released/ready/averageOverallBand. FE: resultsQueries (?provider=), satResultsQueries (route lower|higher, adaptiveRole), ieltsResultDetail, ModuleRawTable/QuestionRawTable.

### 3.8 Cross-domain rules
Submit->grading->release->results: seal -> IELTS projection -> auto-grade -> human review -> grading_complete -> ready_to_release -> released snapshot -> results; SAT bypasses queue (provisional->Complete); ACT seal-hook; gate ready_to_release only; released blocks overrides. Runtime gates all writes (live cohort+section, phase exam, deadline+grace, delivery running, proctor active|warned); pause fences via control_epoch + clock shift. warn record-only; pause blocks; resume shifts; extend-section cohort; extend-attempt SAT-only; terminate/complete terminal->read-only. Chain: published pin -> bootstrap (never scores on read) -> terminal snapshot -> grading -> release -> results. Client scores never trusted. Fencing everywhere (revision, runtime-double, lease/control/writeId/revision, submissionId+hash, operationKey; 409s fail closed).

### 3.9 Product variants (IELTS / SAT / ACT)
Sections/providers/types as above; SAT adaptive base+lower/higher + routing + minimumCorrectForHigher + math tools + hidden pretest (IsPretest never crosses); timing RW 32m/27q + Math 35m/22q + break 10m/0m; ACT clock INFERRED same as IELTS. Submit: IELTS immediate; SAT provisional->Complete; ACT INFERRED immediate. Scoring: IELTS auto+human bands; SAT exact-table scaled; ACT seal-merge %. UI: IELTS full queue; SAT detail-only; ACT science-detail. Sources: IELTS student_results, SAT/ACT assessment_results.

### 3.10 Open questions (INFERRED-but-unconfirmed)
Band mean vs official rounding (INFERRED mean); ScheduleRelease from grading_complete (auto-promote or drift?); IELTS-authentic definition; start authority split (INFERRED runtime=clock, delivery=bootstrap); prod flag posture; V1 write-drain sunset; ACT weights / SAT tolerance storage split (INFERRED); extend-attempt SAT-only?; export scoping + scheduled-release executor (not in Go); student_context resolved: flows lane evidences `backend/go/cmd/api/student_context.go` (loaders + bootstrap) — business-lane item 9 superseded.

---

## 4. End-to-end system flows

### (a) Student entry -> submit -> receipt
Actor: student (anonymous -> RoleStudent + attempt-bearer). Preconditions: live schedule + published version; invite code OR open link; EntryGate/SHED posture.
A1 check-in form (`StudentEntryRoute/StudentRegistrationRoute.tsx`; e2e queue-admission/student-workflow) -> GET schedules/{id} metadata only (`handlers_v2.go:956`). A2 POST /auth/student/entry (`handlers_v2.go:750`): email fail-fast -> ResolveEntry (link) OR verifyDirectEntryCode (direct, zero-row reject) -> 30/min email+IP bucket -> entryGate.Allow (500/s+2000, 429+Retry-After/queuePosition) -> user mint -> CreateRegistration -> fastPathAttempt else CreateScheduleAttempt mint tx -> IssueAttemptToken (lease 1, clientSessionId) + session + cookies. A3 precheck (POST .../precheck, `handlers_domain.go:222`, RecordPrecheck + fingerprint). A4 queue/lobby (GET sessions/{id} + .../live; runtime revision drives waiting vs started). A5 bootstrap: SAT POST .../bootstrap (`handlers_delivery.go:18`, If-None-Match, VersionETag, 304) / IELTS V1 POST .../bootstrap + GET .../static (conditional). A6 timer: useAuthoritativeDeadlineClock (server deadlineAt+offset; 1s precise vs 15s coarse; cohort_runtime merge). A7 modules: SAT start/submit-module (`:114/:155`, claimWriterSessionTx); IELTS proctor advance moves sections. A8 autosave: SAT PATCH responses/{id} / V2 POST .../responses:batch (`handlers_v2.go:387`, lease+control+resolver+locker) / V1 POST .../mutations:batch; acks {attemptRevision,serverTime,acknowledgements}; "Saved" = server ack ONLY (I4). Client keys response-checkpoint:v2:*, v2_attempt_*, ledger/outbox, v2_quarantine:*; server response_revision + attempt_mutations_v2 (writeId idempotent, version unique, monotonic). A9 annotations ride the batch envelope (sat-annotations, no separate endpoint). A10 network recovery: poll sinceRevision; heartbeat piggyback (nextHeartbeatSecs=90, coalesce skip); same-writeId replay -> exact_replay/retried_accepted. A11 submit: V1 POST .../submit (PrepareSubmitInTx + TerminalizeInTx submitted/student_submit + audit + replay-misuse check) / V2 POST .../submit (Attempts.Submit + terminalSealer -> {submissionId,status,digest,submittedAt,acknowledgements}) / SAT POST .../submit (SAT.CompleteAssessment true-terminal; provisional first, scores later); proctor auto-submit seals final section (proctor_end). State: student_attempts(delivery_status,submitted_at,digest), student_submissions, audit STUDENT_SUBMIT, outbox attempt_terminalized.
Failures: 400 email/shape; 404-collapse codes/links; 429 entry (honor Retry-After, render queue); 401 ATTEMPT_TOKEN_INVALID (revoked/takeover); 409 VERSION_COLLISION/write_id_conflict/superseded (reconcile as NEW writeId, never cross-epoch replay); 410 STUDENT_WS_RETIRED; terminal ATTEMPT_SUBMITTED/not_writable post-submit (e2e-05 proven: late flush kept locally, never merged); SUBMISSION_ID_MISUSE; 503 query budget (10s, shed-don't-storm); 304 = clean reconnect.

### (b) Authoring -> release
Actor: Admin/Builder (TierWrites). B1 shell/draft (SatAuthoringRoute/ProviderBuilderRoute/AuthoringWorkspace; exam-builder specs) -> shell/open (`handlers_authoring.go:139,158`) + exams CRUD (`handlers_admin.go`). B2 question CRUD (create 437 / batch 463 / get 524 / patch 543 fenced / delete 580 / duplicate 646 / order 598 / bulk 686 / revision 758 / delivery-settings 808; operationKey replay; authoring_operation_total). B3 workbook: template 44 (SAT-only) / preview 245 / commit 300 / undo-state 333 / undo 356 / sample 219. B4 readiness/preview: preview 200 / validate 856 + examsValidation; media upload intent/bytes/complete (70/108). B5 versions/events (`handlers_admin.go:270,290,235`). B6 scheduling (SchedulingRoute; scheduling-workflow spec; PATCH cannot move immutable published version). B7 release (SatDeliveryReleaseRoute; releaseStateHandler `handlers_release.go:17`; examsPublishHandler; runtime kick schedulesRuntime/RuntimeCommand handlers; access-links handlers). Failures: version_collision (refetch); operationKey replay; 422 provider/schedule payload; undo committed-only; publish blocked until canPublish.

### (c) Grading -> export
Actor: Admin|Grader (+Admin-only transitions; Results reads also Proctor/Student single). C1 queue (GradingRoute, useGradingSessions 2m/summary 60s; grading-workflow + prod-smoke specs) -> GET /grading/sessions (dual paginated|legacy, limit default 200 clamp 500, fail-closed 400). C2 detail (30s+jitter submissions; session/submission/sections/writing-tasks/result-events handlers; 22-col scan; corrupt -> 502 envelope). C3 annotations/drafts/scores (start-review/draft merge/override per-question + schedule objective-overrides/source/integrity/regrade/complete/ready/release-now/schedule-release(422 bad RFC3339)/reopen). C4 export (client zip/pdf builders + tests; POST /results/export {submissionIds,profileId}; results reads dashboard/analytics/sat/act; answer-history overview/targets/export). Failures: out-of-scope grader -> NOT_FOUND (not 403); 429 poll never blind-retry; corrupt projection 502 + server log; sat_finalize_total{completed|replayed|rejected} alert on rejected (twin 409-retry + flat completed = healthy convergence).

### (d) Proctor live
Actor: Admin|Observer|Proctor (assignment-scoped; per-attempt scope check). D1 roster (useProctorRouteController, fetchSummaries/Detail mode=dashboard&auditLimit=200&alertLimit=100; dashboard/workflow specs) -> GET /proctor/sessions (?providerKey else 422) + /{id} + /roster (cursor updated_at,id, limit 200 cap 1000, status filter). D2 alerts/audit/live-mode (header Live; live-mode/alerts/violation-rules specs) -> GET /proctor/live-mode ({degraded,reason?}; static when disabled else stale-outbox >15s) + ack (`handlers_domain.go:749`). D3 advance/intervene (end/extend-section/complete-exam + warn|pause|resume|extend|terminate + presence; expectedRevision+sectionKey; proctorControls helpers; flush-race e2e-05) -> cohort handlers (:531/:551/:589) + per-attempt (:613-:719, scope-checked, Proctor.* service) + presence (:494); revision/control bumps; pollAfterSecs=2 60s fast-lane. D4 notes/rules (notes + violation-rules CRUD). Failures: stale revision/section -> refetch + re-issue; out-of-scope -> 404; degraded -> keep polling, no WS storm; proctor advance wins server-side over student flush.

### (e) Recovery / resume / time-extension
Reload resume (epoch-guarded loadStudentData: static -> live -> rebootstrap-on-mismatch -> freshness-gated apply -> reconciled save; student-recovery spec). Offline->online (saved_locally checkpoints, same-writeId replay -> exact_replay; CONTROL_EPOCH_STALE -> fresh snapshot + NEW writeId). Takeover (takeOverResponseDurabilityLease -> POST .../takeover `handlers_v2.go:547` -> lease_epoch bump + bearer rotation; old device LEASE_FENCED never-replay vs timing CONTROL_EPOCH_STALE reconcile; quarantine archive-before-delete; student-multi-device spec). Time extension (proctor extend -> revision bump + 2s fast-lane; student sees deadlineAt <=30s; gates enforce immediately). Submit rescue (bootstrap carrying result recovers submitting->complete; loadV2SubmitResponse reread; twin seals idempotent replayed). Status vocabulary (never violate): saving / saved_locally (never "saved") / blocked_attention / conflict / saved (acked ONLY, I4) / error.

### (f) Transport matrix
Student live = versioned runtime poll (GET .../runtime?sinceRevision, 304 steady, {revision,status,activeSection,pollAfterSecs}; ETag W/"runtime-{sched}-{rev}"; client createStudentRuntimePoll(Loop) in useAsyncPolling; 2s fast-lane 60s post-command, 25s steady min 1s; 304 target >90%; TierPolling 240/min, 60 shed; 404 parks, 410 terminal). Student socket = DEAD (STUDENT_WS=gone -> 410 {use:runtime-poll}; zero student sockets = 1M enabler). Staff = WS kept (GET /ws/live?scheduleId&attemptId&lastSeenRuntimeRevision, 16K buffers, same-host origin, 64K reads; proctor-observer burst queue newest-wins 250ms debounce, backoff 500*2^n<=10s+150jitter, fatal UNAUTHORIZED/FORBIDDEN/SCHEDULE_CAPACITY stops; db-mode LiveBus.PollNew->Hub forwarder; lease memory|db, 429 LeaseAcquireFailed; ping/lease 25s/30s). Writes = mutation + piggyback (TierWrites 120 NEVER shed; heartbeat 120/30 shed; nextHeartbeatSecs=90 coalesce; bodies 256KiB/2MiB; 10s per-query budget). Staff reads = React Query poll (TierAuthedReads 300/150 shed; AnonAuth 30; AuthCritical 120; Backstop 3000; shouldRetryQuery refuses 429, retry<=once after Retry-After). Control fan-out bound: revision bump + write-gate enforcement; visibility <=30s (2s fast-lane), correctness never waits for poll.

### Sequence diagrams
(a) student submit (V2; V1/SAT inline): Browser --batch{lease,control,cmds}--> api SaveResponses (snapshot|FOR UPDATE lock; fence lease/control/ver; dup->exact_replay; new-ver->collision) --{revision,acknowledgements}--> Browser; Browser --submit{submissionId,lease,control,final,expectedRev}--> api Attempts.Submit + terminalSealer (V1 Prepare+Terminalize RC tx; SAT CompleteAssessment) --seal submitted_at+digest+row+audit--> DB --attempt_terminalized--> worker (materialize; SAT seals later); receipt <--{submissionId,status,digest,submittedAt,acknowledgements}--; poll --since--> 304|delta; late flush -> ATTEMPT_SUBMITTED kept locally.
(b) proctor intervene: Browser+WS --WS /ws/live--> api acquireWSLease (429 on cap) <--connected+snapshot--; --GET sessions--> roster cursor page; --POST control/end-section-now{expectedRev,section}--> EndSectionNow tx --revision++/advance--> runtime (pollAfter 2s); --POST attempts/{a}/pause--> Pause (warn/resume/extend/terminate same gate) --controlEpoch++--> ; WS burst per (kind,id) newest-wins; student poll --since--> delta; student batch STALE epoch --> fence CONTROL_EPOCH_STALE (reconcile NEW writeId) / takeover LEASE_FENCED / sealed ATTEMPT_SUBMITTED; ack alerts; notes CRUD.

---

## 5. /routed-work-orchestration + docs/ops

Source: /routed-work-orchestration skill v1.0.0 per L0 lane report (SKILL.md + references/routes.md + references/verification.md; snapshot held by L0 outside repo). Principle: smallest sufficient route, then produce -> freeze Vn -> independent check -> repair smallest scope -> recheck -> complete. Never done with failing checks. Not for trivial reads/pure Q&A.
Routes: DIRECT (L0->L3->L4, no L1/L2/plan, 1-2 workers; known path + isolated work; repo ex: CSS token fix, eslint-rule tweak, runbook typo, backup-rehearsal flag). LIGHT (L0->L3(plan+workers)->L4, no L1/L2, <=15-bullet plan; some uncertainty + few independent changes; repo ex: SAT highlight eraser, queue-rail micro-interactions, one diagnostics script). ROADMAP (L0->L1->[L2]->L3->L4; dependencies + arch + sequencing + integration risk; repo ex: durability repair WP0-WP8, MySQL scale Phases A-E, SAT audit HOLD remediation, authoring spine + Bluebook Phases 0-11 AT-01..15).
Roles: L0 owns route/card/reqs/handoffs/completion; L1 (ROADMAP only) independent scopes; L2 optional for shared evolving context only; L3 works from goal+context+boundaries+deps+acceptance; L4 independent, default ONE combined checker (+plan/work/goal/integrated for ROADMAP), 2nd only for named risk; never duplicate, never self-approve.
Loop: success card (Goal/Expected/Must-preserve/Required-checks/Done-when) -> freeze V1 -> L4 on frozen V1 -> findings? smallest-scope repair -> V2 -> recheck; full restart only if arch/route invalidated.
Anti-patterns: roadmap for simple fix; L1/L2 in DIRECT/LIGHT; managers with nothing to manage; many workers for coupled task; duplicate reviews; self-approval; moving-target review; re-plan-all for local finding; plan==done; done with open checks; agent-count optimizing.
Docs inventory: ADR 0001 (closed-by-default entry, pre-mint gate, ModeOpen-only open path, 404 collapse; Round 2: allow_open_entry, metadata hardening, captcha); single-deploy-mysql-scale-plan (one process + InnoDB, G1 1M/day staggered, G2 0-SQL polls/~5 stmts per autosave, G3 no-loss, G4 flag-gated; ceilings: students poll, ~5-15k write rps); sat-hardening + baseline (Bluebook tokens, CSS-only rule, 69 files/314 pass + sat-a11y 77+7skip); state-loss audit (CONFIRMED revert, F1-F5); durability repair plan (ROADMAP V1, I1-I6, WP0-WP8); SAT exam-day audit (HOLD: P0 V2-vs-legacy scoring disconnect, P1 dual session identity); superpower plans (authoring spine ?spine=1 AT-01..15; queue-rail M1-M4; highlight-eraser TDD 6+5 files; bluebook Phases 0-11); .context/* stale non-governing; coverage artifact only; scripts (diagnostics contract, backup-rehearsal fail-closed + readyz probe, authoring eslint guardrail, shot helper).
Runbooks net: exam-day-mysql-scale (my.cnf 16GB, pools API40/worker10, 10s budget + shed-don't-retry, SHED_MODE off|exam priority submit>autosave>bootstrap>poll>heartbeat>proctor>admin, 15-flag matrix, DLQ, sat_finalize_total semantics, quarterly game day + k6 gates) + student-answer-durability (WP9 Dogfood->Pilot->Full, rollback=client redeploy, reason-coded counters, preserve-first triage, honest save vocab) + sat-student-tools-exam-day (presentation-only tools, live-timer veil, proctor-pause vs student-break, Desmos coexist, per-schedule:attempt Display/Reader, Bluebook chrome, modal-gated shortcuts).

---

## 6. Infra / data / testing

Deploy: single-image single service — `backend/Dockerfile` (node:22 frontend builder -> golang:1.26 api|migrate|worker -> debian-slim runner + start.sh runs migrate then supervises api+worker, EXPOSE 4000; worker never BACKGROUND-gated; MALLOC tuning). `railway.json` one service (DOCKERFILE, /healthz, ON_FAILURE x10). Local-only `backend/docker-compose.yml` (tidb + minio). `.cloudflare/` EXISTS BUT EMPTY. `deploy/leapcell-load/` = load-harness deploy (playwright+k6 image, run.sh, ui-server :3366). `load-runner/` separate service (:3366 UI). Env key names only: APP_URL/ENVIRONMENT/VITE_BACKEND_API_URL/VITE_API_BASE_URL/RESOURCE_PROFILE/BACKGROUND_*/DB_POOL_IDLE/API_MALLOC_*/API_OTEL_*/RATE_LIMITER_BUCKET_CAP/RETENTION_*/VITE_DESMOS_* + backend 206-line catalogue (pools, worker/outbox/autosubmit, object storage, LIVE/GRADING_PROJECTION/SYNC_ON_READ/PROMETHEUS/METRICS, AUTH/SESSION/CSRF/TTLs, caps, per-tier limits, single-deploy flags, STORM_ADMISSION/MASTER_KEY, FEATURE_USE_BACKEND_*).
Data: MySQL/InnoDB >=8.0.16 (TiDB local-dev only; DSN normalized at Go boundary). Migrations 0001..0058 lexical via cmd/migrate vs schema_migrations PK (crash-retry safe; MIGRATION_POLICY.md additive-only + guards; backup-rehearsal gate: scratch restore -> --validate-only -> boot + /readyz). Domains 0001 roles ... 0058 authoring op keys (~70 tables: identity, exam/authoring, scheduling/access, delivery incl. V2 ledger + terminalizations, proctoring, grading/results, SAT-neutral, infra/outbox/DLQ/leases/queues). Guarantees: receipt-FIRST terminalization (0056 + triggers, outcome-only replay), V2 durability (0049 write_id + lease/question/version UNIQUEs, exact-replay, fencing), ledgers (submission 0022, authoring 0058 7d TTL, finalization 0021, mutation-id 0016, violation-id 0019, heartbeat 0045), DLQ 0055 (atomic park + single-use requeue), repair 0057 (keep-earliest dedup, re-runnable).
Observability: backend telemetry counters/gauges -> /metrics (bearer/fail-closed; OTEL removed, X-Trace-Id echo); 12 prometheus alerts (answer-integrity PAGE, replay-incomplete, grace-rejected spike, outbox age, SAT legacy/mixed fallback, finalize-rejected spike, ratelimit denials, readyz 5xx, 5xx rate, authoring conflict/rejection burn); frontend performanceMonitor (cap 100, >1s warn, P95/export) + performanceTracker (DEV) + logger + durability telemetry + sat persistence; telemetry-verification spec; prod evidence e2e/.generated (live-runner logs/summaries, progress jsonl).
Tests & load: vitest (jsdom; SAT reducer/timing, DurableResponseEngine, authoring/delivery APIs, validation, monitor) + prod-load unit config; playwright default (Go-backed local: api + worker + Vite :3000, 7 projects incl. mobile/tablet, workers 1, global-setup); prod-load (sharded real exam-day: 00-control-plane + 05-entry-smoke + 10-student-shard; prod-target.json + untracked creds); prod-smoke (grading-sessions vs Railway); remote (full vs E2E_REMOTE_BASE_URL); sat-a11y (focused + /__dev/sat-accessibility). k6 API-level (K6_CONFIRM_PROD gate): exam-day full + start-exam/section-transition/submit-storm/resume/auto-submit scenarios + scale-proof 0..4 (5k wave ENTRY_GATE 200+429-only p99<5s zero-dup; 2k herd p99<2s 304s; staggered/arrival; 500 contend zero-deadlock; 1000 pollers >90% 304 p99<0.5s; staff WS soak) + node twin backend/scripts/load-test.js. live-runner real browsers (csv sharding, MAX_CONCURRENT 10-20, dashboard :3333/:3360, summaries/diffs) via e2e:live-runner|live-with-k6|leapcell-ui. 55 specs (53 hand-written + 2 generated under e2e/.generated/): student delivery 20, proctor 7, authoring/admin/grading 9, resilience/perf/compat 9+, prod 4. Artifacts: coverage-final.json, test-results/.last-run.json (passed), playwright-report/, e2e/.generated/.
Diagrams:
Railway single service (/healthz) -> Dockerfile image (frontend dist served by Go; migrate once; api+worker supervised :4000) -> MySQL/InnoDB (>=8.0.16; 0001..0058; receipt seal; V2 ledger; outbox+DLQ) + S3-compat/MinIO; local-only compose tidb+minio; /metrics -> Prometheus -> exam-day runbook; frontend PerfMonitor (DEV tracker). Load separate: load-runner/Leapcell (k6 storm + real browsers, sharded offsets) -> logs/summaries -> WS dashboard.
Pyramid: e2e playwright (7 projects local) / prod shards + prod-smoke + remote / k6+live storms + scale-proof / vitest unit-integration / Go tests (seal/ratelimit/middleware/schemaguard).

---

## 7. Verification record
- V1 frozen by L0; L4 independent verification returned PASS-WITH-NITS (independent checker, 40+ spot-checked paths exact: middleware order, tier quotas, handler line refs, status vocabularies, fencing rules, migration count 58; zero secrets; all six lanes complete; ROADMAP justified, no L1/L2 needed).
- Findings: F1 minor (§1.4 AuthoringWorkspace missing ui/ segment) + F2 minor (§2.1 backend/go/api wording) — both repaired in V2 (two one-line edits). F3 (§4a truncation marker) not present in file — L4 read-window artifact, no repair needed.
- V2 recheck: F1/F2 lines confirmed repaired; F3 confirmed absent (raw byte read: grep count 0, line 193 = 2276 bytes ending "...outbox attempt_terminalized." — the marker existed only in the read-tool preview window, never in the file). Checks pass.
- V3 dual-review repair (Reviewer A §§1-3 + Reviewer B §§4-6, both PASS-WITH-NITS, no majors): A1 degraded-App ref 158-225->155-158; A2 BuildRouter 317-628->317-629; A3 openapi path qualified repo-root; A4 authz clarified as authz.go 217l; B1 wire code SUBMISSION_REPLAY_MISUSE->SUBMISSION_ID_MISUSE (errors.go:40 pins wire string; Go identifier is CodeSubmissionReplayMisuse); B2+B2b ack wire keys acks->acknowledgements (handlers_v2.go:416 batch + :503 submit + sequence diagram); B3 ETag claim WITHDRAWN by Reviewer B (code emits W/"runtime-..." exactly as doc states — no fix); B3-skill provenance softened (snapshot held by L0 outside repo); B4 spec count 53->55 (53 hand-written + 2 generated design-system specs). V3 recheck: all tokens grepped post-edit. Done.

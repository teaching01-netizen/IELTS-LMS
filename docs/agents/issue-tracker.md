# Issue tracker: GitHub

Issues and PRDs for this repo live as GitHub Issues. Use the `gh` CLI for issue operations when the user explicitly requests issue-tracker work.

The repository remote is `https://github.com/teaching01-netizen/IELTS-LMS`.

Do not create, edit, comment on, close, or label issues unless the user explicitly requests that action.
